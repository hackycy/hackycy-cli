# Tunnel SQLite 与 Ent 重构 Goal Runbook

## Source Baseline

| Path | SHA-256 |
| --- | --- |
| `.scratch/tunnel-sqlite-model-first/baseline.md` | `ed3da64ae9b7e2e9f89b37c7ec8903a98ca0683b6560152c9de3cd5896dc4c17` |
| `.scratch/tunnel-sqlite-model-first/implementation-plan.md` | `246413ad39367cbf87a7d2b6c848729f161465477b7d10a95b965aafd6947c26` |
| `.scratch/tunnel-sqlite-model-first/issues/02-state-isolation.md` | `8bf747456de2d200553727ea53bad575c0185fb16ba74ebce5961e3b411f0cf2` |
| `.scratch/tunnel-sqlite-model-first/issues/03-orm-prototype.md` | `11f54f1379d9bf45e5d912b5cf04957a85397d5e0688d8eb8fa5215d1657578d` |
| `.scratch/tunnel-sqlite-model-first/issues/04-orm-choice.md` | `f6401a55cdee760e9848b387ce4847106ce2d0b980c6272287f06ac6b181a6a2` |
| `.scratch/tunnel-sqlite-model-first/issues/05-server-invariants.md` | `405b36c9c815df48a1bf86ba807f158e1c4ee8d6b3066956265011461fffb3ff` |
| `.scratch/tunnel-sqlite-model-first/issues/06-node-state-model.md` | `9b41cf85d98f538576da639f0db85317f4b8995b4d48bde7ad76021359ae25d4` |
| `.scratch/tunnel-sqlite-model-first/issues/07-data-access-boundary.md` | `b249554751b2b2b5a1359fef8f66faf7522067329d3e4dbee917646399149992` |
| `.scratch/tunnel-sqlite-model-first/issues/08-schema-lifecycle.md` | `dfe2fe89cd4faa93ee2810475097cadea00099b3275cd77384409ca2ebffe02f` |
| `.scratch/tunnel-sqlite-model-first/issues/09-rollout-acceptance.md` | `47f07b1cbef550634e15c2202be0b3a2afc84eeab21b3c4a7986f15aefab0266` |
| `.scratch/tunnel-sqlite-model-first/map.md` | `cea9340b091b9f62de206e6a7e6fa2657aab4acacd2dd577514b8ac897e8935b` |
| `.scratch/tunnel-sqlite-model-first/prototype/ent-sqlite/README.md` | `8d9658f8dd144b03c23200ed6d697b26df559d57574ad998f294f0e7d216a021` |
| `.scratch/tunnel-sqlite-model-first/sqlite-maintenance-constraints.md` | `dbbe0fe549b55e42f1b0e85e48e7a6aae46fa41a3b306c61adbfb394f4e30801` |
| `CLAUDE.md` | `35a0f66a6531e61b35dc7288884da35eeef79ad1039f5ffdfd29295cee8d566e` |
| `ENGINEERING.md` | `13fe46814b45138d227a301eccda03a5f5ae81dece71a8ec738e4ef1257f6172` |
| `Makefile` | `656fd137023b4bd8653af1cdb8efa880692027934ff0c833bc8440306278a9e4` |

## State Rules

- `implementation-plan.md` 是 Gate 合同的唯一来源；本账本只记录状态和证据。
- 一次只执行 Goal Ledger 中唯一 `active` 的 Gate。
- 每轮向对应 Gate 的 Progress Log 追加 slice、修改、验证结果、风险和下一动作。
- `passed` 需要计划中每条 Exit condition 的明确证据；普通实现或验证失败保持 `active`。
- `blocked` 只用于计划声明的 Stop condition，并记录阻塞与恢复条件。
- 人工验收待确认是一次 Goal 终止交接，必须记录在 Progress Log；它不是 `blocked`，当前 Gate 必须保持 `active`，不得通过重复日志表示等待。
- 当前 Gate 通过后只激活直接后继并结束本次 Goal；直接后继虽为 `active`，但必须由新的 Goal 执行。最后一个 Gate 通过后记录 effort 完成并结束本次 Goal。

## Goal Ledger

| Gate | Status | Depends on | Plan contract | Unlock evidence |
| --- | --- | --- | --- | --- |
| G0: 建立 v1 SQL/Ent 基础 | passed | none | `implementation-plan.md` -> `G0: 建立 v1 SQL/Ent 基础` | commit `b133d73`; all G0 Exit conditions verified below |
| G1: 完成 Server 持久化层 | passed | G0 | `implementation-plan.md` -> `G1: 完成 Server 持久化层` | G1-1 through G1-53 and G1 Exit evidence below |
| G2: 通过 Server 外部行为门禁 | active | G1 | `implementation-plan.md` -> `G2: 通过 Server 外部行为门禁` | G1 passed; activated, implementation not started |
| G3: 完成 Node 持久化与管理状态 | planned | G2 | `implementation-plan.md` -> `G3: 完成 Node 持久化与管理状态` | G2 pending |
| G4: 通过 Node 恢复与最终交付门禁 | planned | G3 | `implementation-plan.md` -> `G4: 通过 Node 恢复与最终交付门禁` | G3 pending |

## Progress Log

### G0: 建立 v1 SQL/Ent 基础

- 2026-09-26: initialized as `active`; implementation has not started.
- 2026-09-26: slice G0-1 (依赖与生成入口): 修改 `go.mod`、`go.sum`、`ent/generate.go`，固定 Ent v0.14.5 并声明 Server/Node 两个生成入口；`GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test ./ent` 通过。风险：生成器引入的间接依赖待生成后整理；下一步只建立 Server v1 SQL 结构资产与 Ent Schema。
- 2026-09-26: slice G0-2 (Server 结构): 新增 `pkg/cmd/tunnel/server/migrations/001_v1.sql`、`ent/server/schema/schema.go`、生成的 `ent/server/**` 和 `ent/schema_test.go`；SQL 纳入 Node candidates/observations，去除旧触发器与延迟复合外键，Ent 生成器运行通过，临时空库结构/关键约束测试 `go test -count=1 ./ent` 及 `CGO_ENABLED=0 go test ./ent/server/...` 通过。风险：新路由 ID 和附属表内部 ID 需 G1 调用方适配；本 Gate 不改业务调用。下一步只建立 Node v1 SQL 结构与 Ent Schema。
- 2026-09-26: slice G0-3 (Node 结构): 新增 `pkg/cmd/tunnel/node/migrations/001_v1.sql`、`ent/node/schema/schema.go` 与生成的 `ent/node/**`，扩展 `ent/schema_test.go`；`go generate ./ent` 与 `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./ent/...` 通过，临时空库验证三张表、列、CHECK 和非法身份/修订/所有权写入。风险：G3 需把旧 Node 状态调用改接新表，G0 不改业务路径；下一步接入 Server 空库初始化辅助代码。
- 2026-09-26: slice G0-4 (Server 空库入口): 新增 `pkg/cmd/tunnel/server/database_v1.go`、`database_v1_test.go`，使用嵌入 SQL 在空 `server-state-v1` 创建数据库并由 ncruces 连接池构造 Ent client；`go mod tidy` 补齐新依赖校验和，定向 `CGO_ENABLED=0 go test -count=1 ./pkg/cmd/tunnel/server -run '^TestOpenEmptyServerV1Database$'` 通过，覆盖 Ent 查询、非空目录拒绝及旧 `go-v1` 文件不变。风险：现有 `OpenState` 仍走旧路径，按 G1 范围再接入；下一步只实现 Node 空库入口。
- 2026-09-26: slice G0-5 (Node 空库入口): 新增 `pkg/cmd/tunnel/node/database_v1.go`、`database_v1_test.go`，在空 `node-state-v1` 先创建私有标记，再用嵌入 SQL 建库并接入 ncruces/Ent；定向 `CGO_ENABLED=0 go test -count=1 ./pkg/cmd/tunnel/node -run '^TestOpenEmptyNodeV1Database$'` 通过，覆盖 Ent 查询、非空目录拒绝及旧 `node.sqlite` 文件不变。风险：身份和运行行的原子业务初始化留给 G3；下一步完成结构复核与 G0 收尾门禁。
- 2026-09-26: slice G0-6 (Server 路由约束修正): 修改 `pkg/cmd/tunnel/server/migrations/001_v1.sql`、`ent/server/schema/schema.go`、`ent/schema_test.go`，将路由小写 CHECK 显式改为 `COLLATE BINARY` 并加入大写 hostname 拒绝测试；同时增强 SQL/Ent 列类型与非空核对、两端 Ent 实际写入测试。`go generate ./ent` 和定向 `CGO_ENABLED=0 go test -count=1 ./ent ./pkg/cmd/tunnel/server ./pkg/cmd/tunnel/node -run 'Test(ServerV1SQLMatchesEnt|NodeV1SQLMatchesEnt|OpenEmptyServerV1Database|OpenEmptyNodeV1Database)$'` 通过。风险：旧业务入口尚未切换新 v1，属后续 Gate；下一步按计划顺序运行 G0 Repository verification。
- 2026-09-26: slice G0-7 (架构清单同步): 修改 `internal/architecture/architecture_test.go`，显式登记 G0 新增的 Ent 源包与生成包；首次架构测试仅因固定 package inventory 缺项失败，更新后 `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test ./internal/architecture` 通过，未跳过或弱化检查。风险：未来模型增加包时须继续维护显式清单；下一步完成最终结构复核和门禁。
- 2026-09-26: slice G0-8 (路由 collation 同步): 修改 `ent/server/schema/schema.go`、`ent/schema_test.go`，将 `hostname` 的 `NOCASE` collation 同步到 Ent 字段，并检查 SQLite 唯一索引实际 collation；`go generate ./ent`、两端结构/空库定向测试、`git diff --exit-code -- ent`、`GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test ./internal/architecture` 与同环境 `go vet ./...` 均通过。`CGO_ENABLED=0 go test -count=1 ./pkg/cmd/tunnel/server/... ./pkg/cmd/tunnel/node/...` 通过（Server 99.556s，Node 64.631s）。风险：G1/G3 仍需分别迁移当前业务状态入口；下一步提交 G0 资产并核对 Exit conditions。
- 2026-09-26: G0 Exit 逐项验收：① Server/Node `migrations/001_v1.sql`、`ent/server/schema`、`ent/node/schema` 与生成代码已在 `b133d73` 提交；`TestServerV1SQLMatchesEnt` / `TestNodeV1SQLMatchesEnt` 在 ncruces 临时空库中核对表、列类型/非空、索引、外键、CHECK 与关键约束拒绝场景，通过。② `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go generate ./ent` 通过，再生后 `git diff --exit-code -- ent` 无差异。③ 同环境定向 `go test -count=1 ./ent ./pkg/cmd/tunnel/server ./pkg/cmd/tunnel/node -run 'Test(ServerV1SQLMatchesEnt|NodeV1SQLMatchesEnt|OpenEmptyServerV1Database|OpenEmptyNodeV1Database)$'`、两端包完整 `go test -count=1 ./pkg/cmd/tunnel/server/... ./pkg/cmd/tunnel/node/...`、`go test ./internal/architecture`、`go vet ./...` 全通过，覆盖 CGO=0 的 Ent/ncruces 编译与写入。④ 新 v1 空库入口只访问固定 `server-state-v1` / `node-state-v1` 空子目录并执行嵌入 SQL；定向测试确认旧 `go-v1` / `node.sqlite` 文件不变，新增生产初始化辅助代码未调用 Ent Auto Migration、旧数据库读取或 migration runner。G0 判定 `passed`；G1 已激活，尚未开始实施，本次 Goal 到此结束。

### G1: 完成 Server 持久化层

- 2026-09-26: initialized as `planned`; no implementation evidence.
- 2026-09-26: slice G1-1 (会话状态目录与根锁): 修改 `internal/filesession/store.go`、`store_test.go`，为 Server 接入固定子目录和 data root 进程锁提供显式选项，默认旧调用行为不变；`GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/filesession -run '^TestOpenUsesSelectedStateDirectoryAndBaseLock$'` 通过。风险：Server `OpenState` 尚未接线，现有业务仍访问旧目录；下一步只迁移 Server 状态打开与初始化。
- 2026-09-26: slice G1-2 (Server v1 状态打开): 修改 `pkg/cmd/tunnel/server/state.go`、`database_v1.go`、`state_test.go`，首次启动在 `server-state-v1` 建库、用 Ent 事务写身份元数据及本地 Node；重开先检查副本中的 SQLite 完整性、SQL 结构、外键、身份与本地 Node，缺失/损坏新状态拒绝，旧 `go-v1` 测试文件保持原样。`GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./pkg/cmd/tunnel/server -run '^Test(OpenState|OpenDatabase)'` 通过。全包 `go test -count=1 ./pkg/cmd/tunnel/server/...` 未通过：旧路由调用仍访问已移除的 `hostname_owners` / `tunnel_http_routes.node_id`；风险：其余业务 SQL 尚未完全迁移到 Ent，当前 Gate 保持 active；下一步只迁移 HTTP 路由资源调用簇。
- 2026-09-26: slice G1-3 (Client hostname 资源读取): 修改 `pkg/cmd/tunnel/server/database_v1.go`、`server_clients.go`、`server_clients_test.go`，在 immediate 专用连接上创建短生命周期 Ent client，沿 v1 路由与 Tunnel 关系读取 hostname 和归属；`TestClientHostnamesOnConnectionUsesV1RouteRelation` 通过。`TestServerNodeTokenStageKeepsActiveClientCredentialUntilConfirmation` 仍因同一迁移函数旧写入引用已删除的 `node_id` 列失败。风险：换 Node 写入及路由预留尚未迁移；下一步只迁移 Client 换 Node 写入。
- 2026-09-26: slice G1-4 (Client 在线换 Node 写入): 修改 `database_v1.go`、`server_clients.go`、`server_client_assignment_test.go`，在 immediate 专用连接上用 Ent 更新 Client/Tunnel 并复核归属；Ent 内部关系写入所需的子事务由 no-op 驱动封装，实际提交/回滚仍由外层拥有。`TestMigrateClientNodeUpdatesV1OwnershipAtomically` 通过；旧 HTTP 路由预留尚未迁移时 Client 分配综合测试仍因 `hostname_owners` 失败。风险：其余持久化 CRUD 仍存在原生 SQL；下一步迁移 HTTP 路由预留写入。
- 2026-09-26: slice G1-5 (HTTP 路由预留): 修改 `server_tunnels.go`、`server_client_assignment_test.go`、`server_node_resources_test.go`，在同一 immediate 连接上用 Ent 检查 hostname 所属 Node 并写带 ID 的 v1 路由；测试从路由关联 Tunnel 推导归属，保留竞争与回滚断言。定向 `Test(ServerControlPlaneReservesHTTPRoutesTransactionally|ClientNodeAssignment|NodeResourceTransactionsEnforcePortAndHostnameOwnership|ConcurrentHTTPRoutesCannotSplitHostnameAcrossNodes|MigrateClientNodeUpdatesV1OwnershipAtomically)` 通过；随后 `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./pkg/cmd/tunnel/server/...` 通过（120.533s）。风险：Server 其他 CRUD/普通事务、启动跨记录不变量及类型化约束映射尚未达 G1 Exit；下一步处理基础查询映射。
- 2026-09-26: slice G1-6 (账户查询映射): 修改 `database_v1.go`、`server_accounts.go`，账户按 ID/用户名读取及账户列表/Client 计数改用 Ent，并在持久化边界转换可空密码字段；`TestServerAccounts` 定向测试与 `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./pkg/cmd/tunnel/server/...` 均通过（全包 97.827s）。风险：账户写入仍是原生 SQL，其他调用簇与启动校验尚待迁移；下一步只迁移账户普通 CRUD/事务。
- 2026-09-26: slice G1-7 (环境管理员初始化写入): 修改 `server_accounts.go`，用普通 Ent 事务维持首次创建与重开更新语义，并以类型化 `CONSTRAINT_UNIQUE` 识别配置用户名冲突；`TestServerAccounts` 与 Server 全包 `go test -count=1 ./pkg/cmd/tunnel/server/...` 通过（122.261s）。风险：其他账户 CRUD 仍需迁移，唯一约束具体归因还需定向错误映射验证；下一步只处理本地账户 CRUD。
- 2026-09-26: slice G1-8 (本地账户创建): 修改 `server_accounts.go`，本地账户单表创建改为 Ent mutation，用户名唯一约束按类型化 SQLite 错误码映射；`TestServerAccounts` 与 Server 全包 `go test -count=1 ./pkg/cmd/tunnel/server/...` 通过（96.159s）。风险：本地账户更新/删除及其他持久化调用簇未迁移；下一步只处理账户更新。
- 2026-09-26: slice G1-9 (本地账户更新): 修改 `server_accounts.go`，角色与密码变更使用普通 Ent 事务、Ent 实体只在持久化边界内转换，提交失败不返回成功；`TestServerAccounts` 与 Server 全包 `go test -count=1 ./pkg/cmd/tunnel/server/...` 通过（110.221s）。风险：账户删除、Client/Tunnel/Node 普通 CRUD、启动不变量仍待迁移；下一步只处理账户删除。
- 2026-09-26: slice G1-10 (本地账户删除): 修改 `server_accounts.go`，在普通 Ent 事务内读取本地账户、计数其 Client、拒绝非空账户并删除空账户；`TestServerAccounts` 与 Server 全包 `go test -count=1 ./pkg/cmd/tunnel/server/...` 通过（105.673s）。风险：账户调用簇已迁移，但 Client/Tunnel/Node 与管理状态仍有原生普通 CRUD；下一步迁移 Client 基础查询。
- 2026-09-26: slice G1-11 (Client 基础查询): 修改 `server_clients.go`，Client 按 ID/Owner/Token 读取和列表改用 Ent，领域映射显式处理可空 Node/时间及重启错误字段；`TestServerControlPlanePersistsTrustedClientLifecycle` 与 Server 全包 `go test -count=1 ./pkg/cmd/tunnel/server/...` 通过（130.959s）。风险：Client 创建和其他更新仍是原生 SQL，Node/Tunnel 查询及启动不变量待迁移；下一步只处理 Client 创建。
- 2026-09-26: slice G1-12 (Client 创建): 修改 `server_clients.go`，单表创建改用 Ent mutation 并在成功后发送已有事件；`TestServerControlPlanePersistsTrustedClientLifecycle` 与 Server 全包 `go test -count=1 ./pkg/cmd/tunnel/server/...` 通过（123.817s）。风险：Client 更新/删除及资源写入、Node/Tunnel 查询和启动不变量尚待迁移；下一步只处理 Client 非资源更新。
- 2026-09-26: slice G1-13 (Client 备注更新): 修改 `server_clients.go`，备注更新改用单步 Ent mutation，成功后才发送既有更新事件；`TestServerControlPlanePersistsTrustedClientLifecycle` 与 Server 全包 `go test -count=1 ./pkg/cmd/tunnel/server/...` 通过（98.534s）。风险：Token、重启、删除等 Client 生命周期写入仍为原生 SQL；下一步只处理 Token 轮换。
- 2026-09-26: slice G1-14 (Client Token 状态转换): 修改 `server_clients.go`，轮换与确认改用普通 Ent 事务，并只在提交后发送对应事件；Client 生命周期定向测试及 Server 全包 `go test -count=1 ./pkg/cmd/tunnel/server/...` 通过（96.353s）。风险：Client 删除、重启结果、资源类写入和其他实体调用簇仍待迁移；下一步只处理 Client 删除。
- 2026-09-26: slice G1-15 (Client 删除): 修改 `server_clients.go`，在原有 immediate 资源事务中以同一连接的 Ent mutation 删除 Client，数据库级联释放 Tunnel/路由，成功提交后再发送删除事件；生命周期与 HTTP 删除定向测试、Server 全包 `go test -count=1 ./pkg/cmd/tunnel/server/...` 通过（97.617s）。风险：Client 重启与其他实体持久化尚未迁移；下一步只处理 Client 重启状态。
- 2026-09-26: slice G1-16 (Client 重启状态): 修改 `server_clients.go`，请求代次与结果确认改用普通 Ent 事务，保留上限、重放幂等和失败详情校验；并发/合并定向测试与 Server 全包 `go test -count=1 ./pkg/cmd/tunnel/server/...` 通过（113.126s）。风险：Client 分配边缘写入、Tunnel/Node/管理状态普通 CRUD 与启动不变量尚待迁移；下一步只迁移 Tunnel 基础查询。
- 2026-09-26: slice G1-17 (Tunnel 基础查询): 修改 `server_tunnels.go`，Tunnel 列表、按 ID 与 Owner 读取改用 Ent，JSON 选项、可空端口和 HTTP 域名在持久化边界转换；Tunnel 定向测试与 Server 全包 `go test -count=1 ./pkg/cmd/tunnel/server/...` 通过（98.292s）。风险：运行时快照仍沿旧 SQL scanner，Tunnel mutation/资源检查、Node 查询及启动不变量仍需迁移；下一步只迁移运行时快照读取。
- 2026-09-26: slice G1-18 (Client runtime 一致性读取): 修改 `server_client_runtime.go`，用普通 Ent 只读事务读取 Client、远端 Node 端点/Token 和 Tunnel，提交后生成既有摘要；本地 runtime 与远端 Token 阶段切换定向测试、Server 全包 `go test -count=1 ./pkg/cmd/tunnel/server/...` 通过（96.445s）。风险：本地 runtime 摘要写入、Tunnel mutation、Node 管理及启动不变量仍待迁移；下一步只处理本地 runtime 摘要写入。
- 2026-09-26: slice G1-19 (本地 runtime 摘要写入): 修改 `server_client_runtime.go`，在普通 Ent 事务中比较摘要、推进本地 Client 修订号并保存新摘要，整体提交或回滚；`TestSyncLocalClientRuntimeRevisionAdvancesOnlyWhenEndpointOrTokenChanges` 与 Server 全包 `go test -count=1 ./pkg/cmd/tunnel/server/...` 通过（98.603s）。风险：Tunnel 资源写入、Node 管理及启动不变量仍待迁移；下一步只迁移 Tunnel 资源读取。
- 2026-09-26: slice G1-20 (Tunnel 端口资源读取): 修改 `server_tunnels.go`，在 immediate 专用连接上用 Ent 查询 Node 端口池及同 Node/协议的已占端口；资源竞争定向测试与 Server 全包 `go test -count=1 ./pkg/cmd/tunnel/server/...` 通过（104.417s）。风险：Tunnel mutation 仍有原生 SQL，Node 管理及启动不变量待迁移；下一步只处理 Tunnel 创建写入。
- 2026-09-26: slice G1-21 (Tunnel 创建写入): 修改 `server_tunnels.go`，在 immediate 事务的同一连接上用 Ent 创建 Tunnel，随后预留 HTTP 路由，失败由外层整体回滚；创建/导入回滚定向测试通过。首轮 Server 全包测试仅 FRP runtime 下载导致实机用例超时；该用例单独重跑通过（6.863s），随后 Server 全包 `go test -count=1 ./pkg/cmd/tunnel/server/...` 通过（95.629s）。风险：Tunnel 更新/删除、Node 管理与启动不变量尚待迁移；下一步只处理 Tunnel 更新写入。
- 2026-09-26: slice G1-22 (Tunnel 更新写入): 修改 `server_tunnels.go`，在原有 immediate 事务中用 Ent 更新 Tunnel 可空字段并替换 HTTP 路由集合；PATCH、协议切换、预留与回滚定向测试以及 Server 全包 `go test -count=1 ./pkg/cmd/tunnel/server/...` 通过（100.587s）。风险：Tunnel 删除和修订写入、Node 管理及启动不变量尚待迁移；下一步只处理 Tunnel 删除。
- 2026-09-26: slice G1-23 (Tunnel 删除): 修改 `server_tunnels.go`，在 immediate 事务中用 Ent 删除 Tunnel，路由经外键级联释放且事件在提交后发送；删除/重用定向测试通过。首轮全包仅 FRP 实机用例在 Client 第二次下载固定 runtime 时超时，单独重跑仍卡下载；风险为网络依赖造成的测试不稳定。下一步修正该用例夹具的重复下载，再复核全包。
- 2026-09-26: slice G1-24 (FRP 实机测试夹具): 修改 `client_forwarding_integration_test.go`，把已通过固定版本校验的 FRP 二进制预置到该测试的 Client 临时 HOME；Client 自身仍执行哈希与版本复核，实机转发行为断言不变。定向实机用例通过（18.975s），Server 全包 `go test -count=1 ./pkg/cmd/tunnel/server/...` 通过（96.698s）。风险：Tunnel 修订写入、Node 管理与启动不变量仍待迁移；下一步只处理 Tunnel/Client 修订写入。
- 2026-09-26: slice G1-25 (Tunnel/Client 修订写入): 修改 `server_tunnels.go`，Tunnel 变更的 Desired Revision 增量与 Applied Revision/Node 确认改用 immediate 连接上的 Ent mutation；资源与修订定向测试、Server 全包 `go test -count=1 ./pkg/cmd/tunnel/server/...` 通过（96.029s）。风险：Client 分配细节、Node registry/管理状态普通 CRUD 和启动不变量仍待迁移；下一步只处理 Node registry 基础查询。
- 2026-09-26: slice G1-26 (Node registry 基础查询): 修改 `server_nodes.go`，远端 Node 按 ID/列表读取通过 Ent Node、RemoteNode 与候选关系，并在边界转换可空字段及公钥；Node 定向测试与 Server 全包 `go test -count=1 ./pkg/cmd/tunnel/server/...` 通过（111.812s）。风险：Node 注册/管理写入、其它原生 CRUD 与启动不变量待迁移；下一步只处理远端 Node 注册。
- 2026-09-26: slice G1-27 (远端 Node 注册): 修改 `server_nodes.go`，在 immediate 事务的同一连接上用 Ent 创建 Node、RemoteNode 与端口池三行；Node 定向测试与 Server 全包 `go test -count=1 ./pkg/cmd/tunnel/server/...` 通过（103.774s）。风险：Node 冲突错误目前仍有笼统映射，管理状态和启动不变量仍待迁移；下一步只移除旧运行时建表。
- 2026-09-26: slice G1-28 (管理表 schema 所有权): 修改 `server_nodes.go`、`server_node_observation.go`，移除构造函数对候选和观察表的运行时 `CREATE TABLE`，这两表只由 v1 SQL 结构资产建立；Node/状态定向测试与 Server 全包 `go test -count=1 ./pkg/cmd/tunnel/server/...` 通过（98.410s）。风险：观察状态普通 CRUD、管理写入与启动不变量待迁移；下一步只处理 Node 观察状态读取。
- 2026-09-26: slice G1-29 (Node 观察读取): 修改 `server_node_observation.go`，已存状态通过 Ent 单表查询并在持久化边界解码，保留 freshness/历史视图；`TestServerNodeObservationSeparatesSavedHistoryFromFreshRuntime` 与 Server 全包 `go test -count=1 ./pkg/cmd/tunnel/server/...` 通过（107.848s）。风险：观察状态写入与其他管理调用尚需迁移；下一步只处理 Node 观察状态写入。
- 2026-09-26: slice G1-30 (Node 观察写入): 修改 `server_node_observation.go`，成功/失败观察由普通 Ent 事务创建或更新；失败保留最近成功快照，提交后才刷新 freshness 并通知观察者。观察定向测试与 Server 全包 `go test -count=1 ./pkg/cmd/tunnel/server/...` 通过（99.106s）。风险：管理候选状态、Node 资源事务及启动不变量仍待迁移；下一步只处理管理候选状态。
- 2026-09-26: slice G1-31 (Node 管理候选状态): 修改 `server_node_metadata.go`、`server_node_coordinator.go`，候选保存/清除、探测失败记录与验证提升改用 Ent；提升在原 immediate 事务中同时更新 RemoteNode 地址并删除候选。管理地址/协调器定向测试与 Server 全包 `go test -count=1 ./pkg/cmd/tunnel/server/...` 通过（138.369s）。风险：Node 元数据其余原生更新、资源事务和启动不变量仍待迁移；下一步只处理 Node 元数据更新。
- 2026-09-26: slice G1-32 (Node 元数据 PATCH): 修改 `server_node_metadata.go`，在 immediate 事务中用 Ent 读取远端 Node、更新名称及端点，并在端点变化时推进相关 Client 修订号；候选地址同事务处理，唯一端点冲突按类型化 `CONSTRAINT_UNIQUE` 映射。元数据定向测试与 Server 全包 `go test -count=1 ./pkg/cmd/tunnel/server/...` 通过（96.987s）。风险：Node 端口池/令牌/删除事务、Client 分配细节与启动不变量仍待迁移；下一步只处理本地 Node 端口池投影。
- 2026-09-26: slice G1-33 (本地 Node 端点与端口池投影): 修改 `server_local_node.go`，在 immediate 事务中用 Ent 检查已占传输端口、更新本地 Node 端点并创建/更新端口池；缩减冲突与本地资源定向测试、Server 全包 `go test -count=1 ./pkg/cmd/tunnel/server/...` 通过（98.147s）。风险：构造函数的本地池兜底仍用 SQL，远端池/令牌/删除和启动不变量尚需迁移；下一步只处理本地池兜底初始化。
- 2026-09-26: slice G1-34 (本地端口池兜底初始化): 修改 `server_clients.go`，ServerControlPlane 构造时用普通 Ent 事务仅在本地池缺失时创建，不覆盖已有池；Client/资源定向测试与 Server 全包 `go test -count=1 ./pkg/cmd/tunnel/server/...` 通过（94.721s）。风险：远端池/令牌/删除、Client 分配其余调用和启动不变量尚待迁移；下一步只处理远端端口池设置。
- 2026-09-26: slice G1-35 (远端池保存前读取): 修改 `server_node_desired.go`，在 immediate 连接上用 Ent 读取远端修订/生命周期/分阶段令牌及目标池外已占传输端口；池缩减/Node 定向测试与 Server 全包 `go test -count=1 ./pkg/cmd/tunnel/server/...` 通过（105.331s）。风险：快照和端口池写入仍是 SQL，令牌/删除与启动不变量待迁移；下一步只处理远端池保存写入。
- 2026-09-26: slice G1-36 (远端池与快照写入): 修改 `server_node_desired.go`，在同一 immediate 连接上用 Ent 更新 RemoteNode 快照/摘要/修订、NodePortPool 边界及 Node 时间；池缩减与令牌定向测试、Server 全包 `go test -count=1 ./pkg/cmd/tunnel/server/...` 通过（96.425s）。风险：令牌状态转换、Node 删除与启动不变量仍待迁移；下一步只处理 Node 令牌读取。
- 2026-09-26: slice G1-37 (Node Token 状态读取): 修改 `server_node_token.go`，阶段与提升事务内的 RemoteNode/Node/Client 读取改用 Ent，原始快照字符串与摘要仍按保存字节核验；Token/端口池定向测试及 Server 全包 `go test -count=1 ./pkg/cmd/tunnel/server/...` 通过（98.749s）。风险：令牌写入、Node 删除和启动不变量待迁移；下一步只处理 Token 阶段写入。
- 2026-09-26: slice G1-38 (Node Token 阶段写入): 修改 `server_node_token.go`，在 immediate 连接上用 Ent 写 staged token、快照/摘要/修订和 Node 时间；阶段/提升定向测试与 Server 全包 `go test -count=1 ./pkg/cmd/tunnel/server/...` 通过（97.244s）。风险：确认提升写入、Node 删除和启动不变量仍待迁移；下一步只处理 Token 确认提升。
- 2026-09-26: slice G1-39 (Node Token 确认提升): 修改 `server_node_token.go`，在 immediate 连接上用 Ent 切换 active token、清空 staged 字段并推进已分配 Client 修订；阶段/提升定向测试与 Server 全包 `go test -count=1 ./pkg/cmd/tunnel/server/...` 通过（104.292s）。风险：Node 删除、Client 分配其余调用、错误映射和启动不变量待迁移；下一步只处理 Node 删除读取。
- 2026-09-26: slice G1-40 (Node 删除前读取): 修改 `server_node_removal.go`，请求删除、完成和 Force Forget 的 Node/RemoteNode 读取及当前/待分配 Client 依赖计数改用 immediate 连接上的 Ent 查询；删除定向测试与 Server 全包 `go test -count=1 ./pkg/cmd/tunnel/server/...` 通过（98.448s）。风险：删除写入仍是 SQL，Client 分配余项、错误映射与启动不变量待迁移；下一步只处理 Node 删除写入。
- 2026-09-26: slice G1-41 (Node 删除写入): 修改 `server_node_removal.go`，在原 immediate 事务中用 Ent 标记 removing、保存 disabled 快照，并在完成/Force Forget 时删除远端 Node；删除定向测试与 Server 全包 `go test -count=1 ./pkg/cmd/tunnel/server/...` 通过（100.051s）。风险：Client Node 分配余项、错误映射与启动不变量仍待迁移；下一步只处理 Client 分配基础读取。
- 2026-09-26: slice G1-42 (Client 分配目标读取): 修改 `server_clients.go`，在 immediate 事务中用 Ent 读取目标 Node 与生命周期，保留 NOT_FOUND/REMOVE_PENDING 错误；Client 分配定向测试与 Server 全包 `go test -count=1 ./pkg/cmd/tunnel/server/...` 通过（97.719s）。风险：待分配/取消写入仍是 SQL，错误映射与启动不变量尚待迁移；下一步只处理 Client 待分配写入。
- 2026-09-26: slice G1-43 (Client 待分配状态写入): 修改 `server_clients.go`，在 immediate 事务中用 Ent 写入离线 pending Node/时间及完成或取消时的清除，资源仍只在线迁移；Client 分配定向测试与 Server 全包 `go test -count=1 ./pkg/cmd/tunnel/server/...` 通过（102.234s）。风险：其它原生 SQL 归属、类型化约束映射和启动不变量待复核；下一步审查剩余 Server 原生查询与写入。
- 2026-09-26: slice G1-44 (Node 展示投影读取): 修改 `server_node_projection.go`，Node 列表/单条投影改用 Ent，观察和可选择性推导保持原领域规则；Node HTTP/投影定向测试与 Server 全包 `go test -count=1 ./pkg/cmd/tunnel/server/...` 通过（96.413s）。风险：FRP Token 元数据仍为原生 SQL，错误映射与启动不变量待完成；下一步只处理内部 FRP Token 元数据。
- 2026-09-26: slice G1-45 (内部 FRP Token 读取): 修改 `server_runtime.go`，持久化 Token 读取改用 Ent Meta.Get，配置显式 Token 的语义不变；Runtime/Token 定向测试与 Server 全包 `go test -count=1 ./pkg/cmd/tunnel/server/...` 通过（102.567s）。风险：首次生成 Token 保存仍为 SQL，启动不变量与错误映射待完成；下一步只处理 Token 保存。
- 2026-09-26: slice G1-46 (内部 FRP Token 保存): 修改 `server_runtime.go`，首次生成和已有空值更新改用 Ent Meta mutation，写入成功后才返回 Token；Runtime/Token 定向测试与 Server 全包 `go test -count=1 ./pkg/cmd/tunnel/server/...` 通过（113.729s）。风险：启动跨记录不变量、类型化约束归因与剩余原生 SQL 边界待核对；下一步只做启动不变量读取验证。
- 2026-09-26: slice G1-47 (启动跨记录不变量): 修改 `database_v1.go`、`state_test.go`，重开时在私有数据库副本上用 Ent 检查 Tunnel/Client Node 一致、hostname 不跨 Node、传输端口落在所属 Node 池；三种受控不一致均拒绝启动且原数据库文件不变。定向 `TestOpenStateRejectsCrossRecordV1InvariantsWithoutRewritingDatabase` 与 Server 全包 `go test -count=1 ./pkg/cmd/tunnel/server/...` 通过（100.797s）。风险：类型化约束映射与其它原生 SQL 归属待复核；下一步只处理 Tunnel 资源约束错误映射。
- 2026-09-26: slice G1-48 (Tunnel 资源约束归因): 修改 `server_tunnels.go`、`server_import.go`、`server_tunnels_test.go`，Tunnel 行和 HTTP 路由写入分别按类型化 `CONSTRAINT_UNIQUE` 与同连接定向资源重查归因；未知或无法归因的约束保留内部错误，不再匹配错误文本。定向 `Test(TunnelConstraintMappingUsesTypedCodeAndResourceConflict|ServerControlPlaneReservesHTTPRoutesTransactionally|ServerControlPlaneAllocatesPortReservationsAndRecordsRevisions|ServerControlPlanePatchesTunnelReservationsAndRevisionsAtomically|ImportFRPCTunnels)`、`git diff --check` 与 Server 全包 `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./pkg/cmd/tunnel/server/...` 通过（111.055s）。风险：账户与 Node 的唯一约束归因仍偏宽；下一步只处理账户唯一约束错误映射。
- 2026-09-26: slice G1-49 (账户唯一约束归因): 修改 `server_accounts.go`、`server_accounts_test.go`，环境管理员及本地账户只在类型化唯一约束且 `username_key` 实际被占用时分别映射 `INVALID_CONFIG` / `USERNAME_TAKEN`；主键、CHECK、外键与无对应重名的唯一约束保留内部错误。定向 `Test(AccountConstraintMappingRequiresUsernameConflict|ServerAccounts)`、`git diff --check` 和 Server 全包 `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./pkg/cmd/tunnel/server/...` 通过（96.208s）。风险：Node 注册及端点更新仍需定向归因，剩余 SQL 归属待最终核对；下一步只处理 Node 注册错误映射。
- 2026-09-26: slice G1-50 (Node 注册约束归因): 修改 `server_nodes.go`、`server_nodes_test.go`，Node ID 主键及远端 Node 公钥/管理地址唯一冲突通过类型化码和同连接重查映射为既有 `NODE_ALREADY_REGISTERED`；其它失败保留内部错误。定向 `Test(ServerNodeRegistryRegisterIsAtomicAndUnconfigured|NodeRegistrationConstraintMappingRequiresMatchingRecord|ServerNodeService)` 与 Server 全包 `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./pkg/cmd/tunnel/server/...` 通过（111.623s），三种实际重复注册都被拒绝且事务无残留。风险：Node 公网端点更新的唯一约束仍需定向重查；下一步只处理该端点错误映射。
- 2026-09-26: slice G1-51 (Node 公网端点约束归因): 修改 `server_node_metadata.go`、`server_node_metadata_test.go`，端点 PATCH 的唯一约束只在同连接查到其他 active Node 占用该 host/port 时映射 `NODE_RESOURCE_CONFLICT`；真实冲突下名称与端点均回滚，未知约束保留内部错误。定向 `Test(ServerNodeEndpointConstraintMappingAndRollback|ServerNodeEndpointPatchNotifiesAssignedOnlineClient|ServerNodeManagementAddressRequiresOriginalIdentity|SyncLocalNodeProjectionRejectsDuplicateNodeEndpoint)`、`git diff --check` 和 Server 全包 `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./pkg/cmd/tunnel/server/...` 通过（91.415s）。风险：G1 收尾证据尚需按 Directed/Repository 顺序核对；下一步只执行 G1 最终验证与 Exit 复核。
- 2026-09-26: slice G1-52 (最终验证与归属复核): 修改文件仅本 Progress Log；定向 `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./pkg/cmd/tunnel/server -run '^Test(OpenState|OpenDatabase|ServerControlPlane(ReservesHTTPRoutesTransactionally|AllocatesPortReservationsAndRecordsRevisions|PatchesTunnelReservationsAndRevisionsAtomically|ImportFRPCTunnelsRejectsSelectionsAndRollsBackReservations)|ClientNodeAssignment(RollsBackOnTargetResourceConflict|RejectsSplitHostnameAndRollsBack)|NodeResourceTransactionsEnforcePortAndHostnameOwnership|ConcurrentHTTPRoutesCannotSplitHostnameAcrossNodes|SyncLocalNodeProjectionUsesStartupSettingsAndRollsBackExcludedPorts|ServerNodeEndpointConstraintMappingAndRollback|TunnelConstraintMappingUsesTypedCodeAndResourceConflict|AccountConstraintMappingRequiresUsernameConflict|NodeRegistrationConstraintMappingRequiresMatchingRecord|ServerNodeRegistryRegisterIsAtomicAndUnconfigured)'` 通过（2.139s）；按 Repository 顺序，最新 Server 全包通过（G1-51，91.415s），`go test -count=1 ./internal/architecture` 通过（2.954s），`go vet ./...` 通过，均使用 `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0`。生产原生 SQL 复核仅见 v1 建库/完整性/schema 检查与 immediate 边界；旧 `database.go` 入口无调用且未改动。风险：无 G1 未决阻塞；下一动作仅判定 G1 Exit、激活直接后继并结束本次 Goal。
- 2026-09-26: G1 Exit 复核更正：现有定向测试确认禁用 HTTP Tunnel 的 hostname 仍被占用，但禁用 TCP Tunnel 仅验证了创建/展示，尚未直接验证端口仍被占用。G1 保持 `active`，G2 保持 `planned`；G1-52 的其余自动化结果有效。风险：禁用传输端口预留缺直接证据；下一步单独补该冲突/回滚用例，然后重跑适用验证并逐项判定 Exit。
- 2026-09-26: slice G1-53 (禁用传输端口预留证据): 修改 `server_tunnels_test.go`，新增禁用 TCP Tunnel 仍占同 Node/协议/端口的定向断言，并检查失败方无 Tunnel、修订号变化及事件。定向 `Test(DisabledTransportTunnelKeepsPortReserved|ServerControlPlaneReservesHTTPRoutesTransactionally)` 通过（1.273s）；按 Repository 顺序，`GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./pkg/cmd/tunnel/server/...` 通过（104.033s），同环境 `go test -count=1 ./internal/architecture` 通过（2.352s），`go vet ./...` 通过。风险：无 G1 未决阻塞；下一动作仅逐项判定 G1 Exit、激活直接后继并结束本次 Goal。
- 2026-09-26: G1 Exit 逐项验收：① `OpenState` 仅在 `server-state-v1` 初始化/重开数据库、Controller 身份与会话；空目录、重复启动、旧 `go-v1` 文件不变、缺失/损坏/结构不符状态拒绝且原文件不变由 `TestOpenState*` / `TestOpenDatabase*` 覆盖。② `State` 持有唯一数据库池及主 Ent client，普通 CRUD/关联查询由 Ent 完成，资源写入在专用连接 `BEGIN IMMEDIATE` 下由外层提交/回滚；G1-1 至 G1-46 每个持久化调用簇均有定向和 Server 全包通过记录，旧 SQL 入口不可达。③ G1-47 的重开夹具证实 Tunnel/Client Node 归属、hostname 单 Node 及已占端口池跨记录不变量，损坏状态拒绝且不改原文件。④ 端口、Hostname、Client 换 Node、禁用 HTTP/TCP Tunnel、端口池缩减、本地 Node 保护、并发竞争与导入回滚由 G1 定向测试及历次 Server 全包覆盖；失败无半完成状态或提交前事件，成功事件提交后发布。⑤ G1-48 至 G1-51 的 Ent/SQLite 扩展码测试覆盖 UNIQUE、PRIMARYKEY、FOREIGNKEY、CHECK 与未知约束；仅在操作及定向重查可归因时映射既有领域错误，其余保留内部错误。⑥ 最终 Server 全包、架构测试和 `go vet ./...` 按计划顺序通过，`git diff --check` 通过；G1 Manual acceptance 为无。判定 G1 `passed`；G2 已激活，尚未开始实施。本次 Goal 到此结束。

### G2: 通过 Server 外部行为门禁

- 2026-09-26: initialized as `planned`; no implementation evidence.
- 2026-09-26: G1 Exit 全部满足后已激活，尚未开始实施；本次 Goal 不进入 G2。

### G3: 完成 Node 持久化与管理状态

- 2026-09-26: initialized as `planned`; no implementation evidence.

### G4: 通过 Node 恢复与最终交付门禁

- 2026-09-26: initialized as `planned`; no implementation evidence.
