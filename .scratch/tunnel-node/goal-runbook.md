# Tunnel Node Goal Runbook

## Source Baseline

| Path | SHA-256 |
| --- | --- |
| `.scratch/tunnel-node/CONTEXT.md` | `6de849157e23c996df73b0102611a1d1a877306ae402ef032b0033ba362935e6` |
| `.scratch/tunnel-node/client-wire-switch.md` | `21727d3b1cca8cf080b8d9980df878f5b69df726b5fdf5d5a587f9df4817b5d9` |
| `.scratch/tunnel-node/deployment-acceptance.md` | `1b14f2c85adcd8a5e94c84d1d58b06e6365bad2788586f237ba8c57fe164fe95` |
| `.scratch/tunnel-node/implementation-plan.md` | `98f4efb555b49a23bf092da9edf1fc8f48fb6257084a5c5a269c8ec0f6d421f7` |
| `.scratch/tunnel-node/integration-outline.md` | `2c84521a51431d6f95f188839e0de001039200f01246f38411eef32e06e4a4e9` |
| `.scratch/tunnel-node/issues/01-claim-trust.md` | `5b33b52e220ecca0daa507ffbbbb1c810b3b9f538606eaa1e3745071a7587d57` |
| `.scratch/tunnel-node/issues/02-binding-lifecycle.md` | `c0134e86ac8b77fa73ef4c153ea59958a93ba8d431f3cbb94f67d86338fd232c` |
| `.scratch/tunnel-node/issues/03-local-remote-runtime-boundary.md` | `59a944d1068ad643db42dc834f98eb1fa2b7766fedfe5e89524ba55fc4faa367` |
| `.scratch/tunnel-node/issues/04-node-desired-state.md` | `704e33757abc8f72bc7415ec4a8e4d26fdc58f8090679dc54609012c80fba2fc` |
| `.scratch/tunnel-node/issues/05-client-node-assignment.md` | `5c1f2272d87cec88c2a21695171af56fa364eb2a7cbcdf388ec33a734de946b6` |
| `.scratch/tunnel-node/issues/06-node-resource-scope.md` | `f3a69d0fd96f36efd3cf9e23e8140126967953382bbb8e7f51046667cdc2fb19` |
| `.scratch/tunnel-node/issues/07-persistence-migration.md` | `47a5e9fda040e902c838b1549662d42a592c7d06ad981066175c93492143a69c` |
| `.scratch/tunnel-node/issues/08-management-api-permissions.md` | `a726550a5283ac576de95a4e999436974088ecd04de552fbae35e33a67aacbd4` |
| `.scratch/tunnel-node/issues/09-nodes-workflow-prototype.md` | `41c54950fc6e68afe44ff2898a3cf3e5af4a9fdb568a952cac8f3d1cbb4ad48d` |
| `.scratch/tunnel-node/issues/10-rollout-compatibility.md` | `4f6998165a4e0fcd601741cfc9fe5ec04baae21f3d15e8853c264395b9b98afa` |
| `.scratch/tunnel-node/issues/11-node-management-wire.md` | `6ef160053bd16afc8d4bdff2217f0c648d2220872e31912bd3fa381c89b58d10` |
| `.scratch/tunnel-node/issues/12-node-state-crash-consistency.md` | `ee321450fb0c820236c813a3b99097b2fab4e11ad29a0c0d2d52e20f96c8fa1f` |
| `.scratch/tunnel-node/issues/13-server-node-storage.md` | `5b5c93943ac27b0a44e07572ad85b04a52128c699c459a6ad76956efc6fb2d74` |
| `.scratch/tunnel-node/issues/14-client-wire-switch.md` | `9523fb610c6961580f7ce3de7b2beef2cce217d11e90a80290b89cc1fa014923` |
| `.scratch/tunnel-node/issues/15-node-management-api-contract.md` | `5a1ffc953a84840126fbababeefdac417317e9d0f09a6a943ad3296fe35489d9` |
| `.scratch/tunnel-node/issues/16-deployment-acceptance.md` | `0735dd6ed830a88059c91ba2093045f095d6f0add73d86ce674a71d91eb11c1b` |
| `.scratch/tunnel-node/map.md` | `7187e4db832c3f79f89f01f5cfd3a61f26fb90872e7f738d6074ac8090048c7c` |
| `.scratch/tunnel-node/node-api-contract.md` | `35a022b9f19c2d54677abc511ea290222e346ba86a20862ea80897cfe3daf7c0` |
| `.scratch/tunnel-node/node-management-wire.md` | `d8b8f16757108ee85b60dac33fd25bcaec2293f3c018b01a6794ad2bf07c3372` |
| `.scratch/tunnel-node/node-state-crash.md` | `f86df5c463f1f7d953359c88ff07516597b5473b1b4394dabb725b890f77a3ba` |
| `.scratch/tunnel-node/server-storage-transactions.md` | `bf7793b987a7e9968a0294b38332cea76774848aab1c43e18a6e10f06acfd129` |
| `.scratch/tunnel-node/spec.md` | `4e5ee7f2cbdba5283e1d4bb857226a6b3041e8e4636fccacb98d5cb2e81d33a4` |
| `CLAUDE.md` | `35a0f66a6531e61b35dc7288884da35eeef79ad1039f5ffdfd29295cee8d566e` |

## State Rules

- `implementation-plan.md` 是 Gate 合同的唯一来源；本账本只记录状态和证据。
- 一次只执行 Goal Ledger 中唯一 `active` 的 Gate。
- 每轮向对应 Gate 的 Progress Log 追加 slice、修改、验证结果、风险和下一动作。
- `passed` 需要计划中每条 Exit condition 的明确证据；普通实现或验证失败保持 `active`。
- `blocked` 只用于计划声明的 Stop condition，并记录阻塞与恢复条件。
- 人工验收待确认是一次 Goal 终止交接，必须记录在 Progress Log；它不是 `blocked`，当前 Gate 必须保持 `active`，不得通过重复日志表示等待。
- 当前 Gate 通过后只激活直接后继并结束本次 Goal；直接后继虽为 `active`，但必须由新的 Goal 执行。最后一个 Gate 通过后记录 effort 完成并结束本次 Goal。

## Goal Ledger

| Gate | Status | Depends on | Plan contract | Unlock evidence |
| --- | --- | --- | --- | --- |
| G0: 新库与 Local 基础模型 | passed | none | `implementation-plan.md` -> `G0: 新库与 Local 基础模型` | E1/E2/E3 recorded below |
| G1: 单一 v5 Client 控制协议 | passed | G0 | `implementation-plan.md` -> `G1: 单一 v5 Client 控制协议` | E1/E2/E3 recorded below |
| G2: Node 身份与管理链路 | passed | G1 | `implementation-plan.md` -> `G2: Node 身份与管理链路` | E1/E2/E3 recorded below |
| G3: Node FRPS 收敛与崩溃恢复 | passed | G2 | `implementation-plan.md` -> `G3: Node FRPS 收敛与崩溃恢复` | E1/E2/E3 recorded below |
| G4: Server 远端 Node 管理 | passed | G3 | `implementation-plan.md` -> `G4: Server 远端 Node 管理` | G4-E1/E2/E3 recorded below |
| G5: Node 资源与 Client 归属 | active | G4 | `implementation-plan.md` -> `G5: Node 资源与 Client 归属` | G4 passed; activated, implementation not started |
| G6: 官方 Client 跨 Node 连接 | planned | G5 | `implementation-plan.md` -> `G6: 官方 Client 跨 Node 连接` | G5 pending |
| G7: 移除、部署与发布验收 | planned | G6 | `implementation-plan.md` -> `G7: 移除、部署与发布验收` | G6 pending |

## Progress Log

### G0: 新库与 Local 基础模型

- 2026-09-23: initialized as `active`; implementation has not started.
- 2026-09-23: slice 1 (startup preflight) complete. Changed `pkg/cmd/tunnel/server/database.go`, `state_test.go`, and the FRP fixture path in `client_forwarding_integration_test.go`. Existing database and WAL/SHM are inspected from a private copy before writable SQLite initialization; absent database with sidecars is rejected. Directed `TestOpenDatabaseRejectsUnknownWALWithoutChangingFiles|TestOpenState|TestInitializeDatabase` passed. Repository `go test ./pkg/cmd/tunnel/server/... ./internal/tunnelruntime/...` passed with `YCY_TUNNEL_TEST_FRP_RUNTIME_DIR` set to locally installed FRP 0.70.1; its frpc/frps SHA-256 matched the manifest. Initial unseeded run failed because the official archive download timed out. Risk: v2 remains supported until the schema/identity slice. Next: create v3 and cross-check the Controller identity before any new database write.
- 2026-09-23: slice 2 (Controller identity and v3 schema transaction) complete. Changed `database.go`, new `controller_identity.go`, `state.go`, `server_tunnels.go`, `state_test.go`, and Windows database test. Empty state creates one X25519 Controller key file and v3 transaction with the protected Local Node; repeat startup checks file/database public keys, rejects missing/mismatched identity, and can reuse the original identity after only the business database is lost. Old v1/v2/unknown and incomplete v3 fixtures are rejected before writes. The v3 route foreign key initially exposed a Local Tunnel write gap; the same transaction now maintains hostname owners and releases them on final-route deletion. Directed `TestOpenState|TestServerControlPlaneReservesHTTPRoutesTransactionally|TestGoClientToGoServerForwardsHTTPAndTCPAndUDPWithPinnedFRP` passed; repository `go test ./pkg/cmd/tunnel/server/... ./internal/tunnelruntime/...` passed using the verified local FRP fixture. Risk: Local runtime settings are not yet materialized in Node rows/port pool. Next: add Local startup projection and conflict checks.
- 2026-09-23: slice 3 (Local startup projection) complete. Added `server_local_node.go` and its focused tests; `server_runtime.go` now projects the configured Local port pool and explicit advertised endpoint before building account and control services. Shrinking past an occupied port or colliding with another active Node endpoint rejects the startup transaction without changing the saved projection. Directed `TestSyncLocalNodeProjection|TestServerRuntimeComposesOwnedResourcesAndReleasesThem` passed; repository `go test ./pkg/cmd/tunnel/server/... ./internal/tunnelruntime/...` passed with verified local FRP fixture. Risk: the allocation path still reads its in-memory range instead of the saved Node pool. Next: bind Local allocation to the saved pool and prove concurrent resource conflicts roll back.
- 2026-09-23: slice 4 (Node resource constraints) complete. Changed `server_clients.go`, `server_tunnels.go`, `server_local_node.go`, and added `server_node_resources_test.go`. Local Tunnel transactions now read the saved Node pool on each write; same-Node concurrent TCP allocation has one winner, another Node can reuse the number, and a hostname cannot be split across Nodes. Failed writes leave no Tunnel or revision residue. Directed `TestNodeResourceTransactionsEnforcePortAndHostnameOwnership|TestSyncLocalNodeProjection` passed; repository `go test ./pkg/cmd/tunnel/server/... ./internal/tunnelruntime/...` passed with the manifest-verified local FRP fixture. Risk: Gate-wide terminal verification and final E1/E2/E3 audit remain. Next: run the Local FRPS integration test and `make check-terminal`, then record exit evidence.
- 2026-09-23: G0 final acceptance and status transition. G0-E1: `TestOpenStateRejectsOldAndUnknownSchemasWithoutChangingFiles`, `TestOpenStateRejectsIncompleteV3WithoutChangingFiles`, `TestOpenStateControllerIdentityMismatchAndMissingDoNotChangeDatabase`, and `TestOpenStateReusesControllerIdentityWhenOnlyDatabaseIsLost` prove schema v3 empty initialization, idempotent restart, old/unknown/incomplete database rejection, Controller identity cross-check, and unchanged database bytes on rejection. G0-E2: `TestSyncLocalNodeProjectionRejectsDuplicateNodeEndpoint` and `TestNodeResourceTransactionsEnforcePortAndHostnameOwnership` prove immutable Local Node presence, default Local Client ownership, saved per-Node port-pool allocation, same-Node protocol/port conflict rollback, cross-Node numeric port reuse, and whole-transaction hostname ownership enforcement. G0-E3: `TestGoClientToGoServerForwardsHTTPAndTCPAndUDPWithPinnedFRP` passed with locally installed FRP 0.70.1 whose frpc/frps hashes match the manifest; second `make check-terminal` passed in full after one unrelated transient PTY failure on the first run. `git diff --check` passed. All G0 Exit conditions are satisfied; G0 marked `passed`, G1 marked `active` and activated without implementation. Effort for this Goal is complete; no G1 work was started.

### G1: 单一 v5 Client 控制协议

- 2026-09-23: initialized as `planned`; no implementation evidence.
- 2026-09-24: slice 1 (v5 message shape and explicit version rejection) in progress. Changed `internal/tunnelruntime/protocol.go`, added `protocol_v5_test.go` and `server_agent_v5_test.go`. `TestRuntimeDigestIsStableAcrossTunnelOrdering`, `TestV5WelcomeAndDesiredStateCarryTheSameRuntimeObject`, and `TestServerAgentRejectsV4HelloWithExplicitUpgradeClose` passed. Initial protocol package suite still fails on old v4 field assertions; no tests were removed or skipped. Risk: source compatibility bridges in protocol structs still need removal before single-v5 exit. Next: finish v5 wire fixtures and strict decoding.
- 2026-09-24: slice 2 (Server complete Local runtime read projection) in progress. Added `server_client_runtime.go`; changed `server_agent_protocol.go`, `server_agent.go`, and `server_agent_outbound.go`. The Server now reads Client, Node assignment, and Tunnel rows in one database transaction and sends a digest-bearing full runtime on both welcome and desired_state. Compile-only `go test ./pkg/cmd/tunnel/server ./pkg/cmd/tunnel/connect ./internal/tunnelruntime -run '^$'` passed. Existing Server suite fails on v4 fixtures as expected during migration. Risk: Local endpoint and Token changes still need revision handling. Next: migrate fixtures, assert equal runtime for welcome/online updates, and add revision tests.
- 2026-09-24: slice 3 (Client durable runtime high-water and deduplication) in progress. Changed `client_state.go`, `client_reconciler.go`, `client_agent.go`, and `client_run.go`; added `client_reconciler_v5_test.go`. `TestClientReconcilerRejectsSameRevisionWithDifferentRuntimeDigest` and `TestClientReconcilerNeverAppliesBelowPersistedHighestAcceptedRuntime` passed; Client compile-only test passed. Highest accepted target is atomically saved before local apply. Risk: same-Node rollback and Local FRPC observations require further verification; old Client tests still contain v4 wire assumptions. Next: complete Local apply/observation and run package suites.
- 2026-09-24: slice 1 (single v5 wire shape and rejection) continued. Changed `protocol.go`, `server_agent_protocol.go`, `client_agent.go`, and v5 protocol/agent fixture tests. Removed production welcome/desired-state v4 fields and hello compatibility decoding; v4 version is identified before v5 shape validation so it receives explicit 4406. Directed protocol/agent tests passed. First repository `go test ./pkg/cmd/tunnel/... ./internal/tunnelruntime/...` failed only in two Server gateway tests still sending v4 hello or lacking a valid v5 session; no tests were skipped. Risk: remaining fixtures may still assume v4. Next: migrate those fixtures and rerun repository verification before advancing slices.
- 2026-09-24: slice 1 complete. Changed `server_agent_test.go` to use v5 hello and runtime-qualified process reports. Directed `TestServerAgentGatewayObservesConnectionAndProcessStateChanges|TestServerAgentGatewayPresentsDurableRestartGenerationToActiveConnection|TestServerAgentRejectsV4HelloWithExplicitUpgradeClose` passed; repository `go test ./pkg/cmd/tunnel/... ./internal/tunnelruntime/...` passed using the installed FRP 0.70.1 fixture. Risk: Server Local endpoint/Token changes and independent FRPC observations are not yet fully covered. Next: verify and complete the Server runtime projection/revision slice.
- 2026-09-24: slice 2 complete. Added `server_client_runtime_test.go`; changed `server_client_runtime.go` and `server_runtime.go`. Server startup stores a Local endpoint/port/Token signature and advances assigned Client revisions transactionally only when it changes; full Client/Node/Tunnel runtime comes from one database transaction. Directed `TestSyncLocalClientRuntimeRevisionAdvancesOnlyWhenEndpointOrTokenChanges|TestServerAgentWelcomeAndOnlineDesiredStateUseIdenticalRuntime|TestServerAgentConnectionPresentsDesiredStateOnlyAfterWelcome` passed; repository `go test ./pkg/cmd/tunnel/... ./internal/tunnelruntime/...` passed with the installed FRP 0.70.1 fixture. Risk: Client high-water collision and independent FRPC observation still need audit. Next: complete Client persistence/deduplication slice.
- 2026-09-24: slice 3 complete. Changed `client_reconciler.go`, `client_state.go`, `client_agent.go`, `client_run.go`, and `client_reconciler_v5_test.go`. Lower revisions now fail protocol validation; same-revision conflict is checked before writing accepted state; corrupt accepted state halts application; in-process reconnect hello can use the persisted accepted target even when local apply fails. Directed `TestClientReconciler|TestClientRunCold|TestClientRunReconnect|TestClientAgentConnect` passed; repository `go test ./pkg/cmd/tunnel/... ./internal/tunnelruntime/...` passed with FRP 0.70.1. Risk: local failure disposition and independent FRPC observation remain. Next: complete Local FRPC application slice.
- 2026-09-24: slice 4 complete. Changed `client_run.go` and `client_run_test.go`. Successful apply reports `started` or `stopped_no_enabled_tunnels`; failed apply reports observed previous-running/stopped state when available or `unknown`; protocol/high-water corruption becomes fatal so shutdown stops the managed FRPC. Existing same-Node verification/start rollback and cold-start/reconnect behavior remain covered by directed `TestRunClient|TestClientReconciler`, which passed. Repository `go test ./pkg/cmd/tunnel/... ./internal/tunnelruntime/...` passed with FRP 0.70.1. Risk: `frpc_status` still needs independent wire reporting and Server expiry logic. Next: implement the observation slice.
- 2026-09-24: slice 5 complete. Changed `protocol.go`, `client_run.go`, `server_agent.go`, `server_agent_protocol.go`, `server_http_test.go`, and Local forwarding integration test; added `server_agent_frpc_status_test.go`. Client now sends independent `frpc_status` from the loopback authenticated FRPC API; Server matches the current session and `(revision,nodeId,digest)`, rejects unproven connected status, and degrades stale/disconnected observations to unknown. Directed `TestServerAgentFRPCStatusRequiresCurrentRuntimeAndExpires` and real `TestGoClientToGoServerForwardsHTTPAndTCPAndUDPWithPinnedFRP` passed; the latter observed all three proxies registered from FRP 0.70.1. Repository `go test ./pkg/cmd/tunnel/... ./internal/tunnelruntime/...` passed with the pinned runtime. Risk: Gate-wide terminal and independent-binary acceptance still remain. Next: format/audit diff, run `make check-terminal`, then `make acceptance`.
- 2026-09-24: Gate audit follow-up on slice 3. Changed `client_state.go`, `client_reconciler.go`, Client cache/reconciler/run tests, and the Local forwarding integration assertion. Removed redundant v4-shaped cache fields, made persisted runtime digest validation explicit, and fixed same-revision conflict tests to mutate the actual v5 runtime. First repository attempt found one integration fixture still reading `state.Snapshot`; after updating it, directed Client tests and `go test ./pkg/cmd/tunnel/... ./internal/tunnelruntime/...` passed with pinned FRP 0.70.1. Earlier `make check-terminal` passed on its second run after one unrelated, isolated `pkg/cmd/run` timing failure; `make acceptance` and standalone binary v4 rejection test had passed before this cache-only follow-up. Risk: gate-wide checks must be rerun after the follow-up. Next: rerun terminal then acceptance in plan order.
- 2026-09-24: Gate verification retry. After cache follow-up, two `make check-terminal` runs failed in the real Local forwarding test because durable applied revision stayed 0 at its 20-second deadline; one run used `GOFLAGS=-p=1` and therefore package parallelism alone does not explain it. The same test isolated with the terminal build environment passed in 6.89s, and the complete Server package later passed in 51.6s. Added secret-safe timeout diagnostics to the integration test for accepted/applied revisions and Gateway state; this is diagnostic only and does not weaken assertions. Risk: intermittent full-suite startup/apply delay remains unresolved. Next: rerun terminal suite and use the new evidence if it fails, then run acceptance only after terminal passes.
- 2026-09-24: G1 final acceptance and status transition. G1-E1: `TestProtocolV5MessagesRetainRestartRecoveryFields`, `TestServerAgentWelcomeAndOnlineDesiredStateUseIdenticalRuntime`, `TestServerAgentRejectsV4HelloWithExplicitUpgradeClose`, and standalone-binary `TestTunnelStandaloneBinaryRejectsV4ClientHello` prove complete v5 runtime on both messages and actual 4406 upgrade rejection of v4. G1-E2: Client reconciler high-water, corrupt-cache, same-revision conflict and cold-start/reconnect tests passed; `TestGoClientToGoServerForwardsHTTPAndTCPAndUDPWithPinnedFRP` passed with FRP 0.70.1 and actual HTTP/TCP/UDP round trips. G1-E3: `TestFRPCStatusObservationSeparatesRegisteredFailedAndUnknownProxies` and `TestServerAgentFRPCStatusRequiresCurrentRuntimeAndExpires` prove independent Local FRPC observation and stale/disconnected unknown projection; real integration observed all three proxies registered while `apply_result` remained separate. Final repository `go test ./pkg/cmd/tunnel/... ./internal/tunnelruntime/...` passed with pinned FRP. Final `GOMAXPROCS=2 GOFLAGS=-p=1 make check-terminal` passed in full after earlier load-sensitive timing failures in the Local integration and unrelated `pkg/cmd/run` 2-second PID fixture; no test was skipped or weakened. Final `make acceptance` passed (`acceptance` and `acceptance/web`), including independent binary v4 rejection. `git diff --check` passed. All G1 Exit conditions are satisfied; G1 marked `passed`, G2 marked `active` and activated without implementation. This Goal's work is complete; no G2 work was started.

### G2: Node 身份与管理链路

- 2026-09-23: initialized as `planned`; no implementation evidence.
- 2026-09-24: slice 1 (CLI configuration and command surface) complete. Added `pkg/cmd/tunnel/node/command.go`, `config.go`, and `config_test.go`; registered the single `ycy tunnel node` command in `tunnel.go`, updated root command classification and the command-surface snapshot, and extended the tunnel help test. Flag > environment > default precedence, default port 7600, absolute state path, and explicit invalid values are verified. Directed `TestResolveConfigPrecedenceAndValidation|TestNewCmdTunnelRegistersServerConnectAndNode` passed; `make command-surface` and repository `go test ./pkg/cmd/tunnel/... ./internal/tunnelruntime/...` passed. Risk: the command currently returns a deliberate runtime-not-initialized error; no listener or identity is exposed yet. Next: persist Node identity in private SQLite under an exclusive state-directory lock, then replace that temporary runner.
- 2026-09-24: slice 2 (long-term identity, private SQLite and lock) complete. Changed Node `command.go`; added `state.go`, `run.go`, and `state_test.go`. A fresh private directory creates a 0600 SQLite identity transaction with an X25519 key and stable Node ID; restart retains the full fingerprint, a concurrent instance is rejected, damaged database/orphaned WAL refuses new identity, and the public health response contains only reachability and protocol version. Directed `TestNodeState|TestNodeHealth|TestResolveConfig` passed after correcting SQLite's initial file mode; repository `go test ./pkg/cmd/tunnel/... ./internal/tunnelruntime/...` passed. Risk: the HTTP listener currently exposes only health; Noise handshake and management messages are not yet implemented. Next: implement bounded Noise XX handshake and encrypted frames.
- 2026-09-24: slice 3 (Noise XX handshake and encrypted HTTP frame) complete. Added `github.com/flynn/noise` v1.1.0, `management.go` and `management_test.go`, and connected the handler in `run.go`. Loopback HTTP completes XX with the persisted Node key and fixed prologue, emits a nonce-0 encrypted confirmation, accepts a nonce-0 encrypted request, returns nonce-1 encrypted response, and rejects a repeated frame. The dependency's own protocol-vector test suite `go test github.com/flynn/noise` passed; directed `TestNodeNoiseXXHandshakeAndEncryptedFrame|TestNodeState|TestNodeHealth` and repository `go test ./pkg/cmd/tunnel/... ./internal/tunnelruntime/...` passed. Risk: operation dispatch still returns `NODE_OPERATION_UNAVAILABLE`, so no claim or status business state is written yet. Next: implement preview/no-bind and atomic first-Controller claim with encrypted status query.
- 2026-09-24: slice 4 (preview, claim transaction, and status query) complete. Changed Node `state.go`, `management.go`, and `management_test.go`. A preview XX start exposes the persisted Node public key without binding; the encrypted `claim` uses only the handshake Controller key and commits one binding row before success. Two concurrent Controllers yield exactly one success and one generic `NODE_ALREADY_CLAIMED`; after restart the winner can query encrypted `status`, while the loser and repeat claim remain rejected. Directed `TestNodeClaimFirstControllerWinsAndBindingSurvivesRestart|TestNodeNoiseXXHandshakeAndEncryptedFrame` and repository `go test ./pkg/cmd/tunnel/... ./internal/tunnelruntime/...` passed. Risk: malformed/oversized frames, wrong versions, and response-loss recovery need explicit final coverage. Next: complete error/replay tests and independent-binary acceptance.
- 2026-09-24: slice 5 (error bounds, expiry and replay) directed verification complete; Gate repository checks pending. Changed Node `management.go`, `management_test.go`, `state.go`, `state_test.go`, `acceptance/tunnel_test.go`, and `go.mod`/`go.sum`. A real loopback Controller confirms wrong protocol version, oversized HTTP/Noise frames, wrong sequence, replay, wrong encrypted Node identity and expired handshake/session are rejected without binding; after a deliberately discarded claim receipt, a new encrypted `status` confirms the committed binding. `go test ./pkg/cmd/tunnel/node -count=1`, `go test -race ./pkg/cmd/tunnel/node -count=1`, and independent-binary `TestTunnelNodeStandaloneBinaryIdentityAndCLI` passed. The binary test first found SQLite WAL/SHM permissions too broad after abrupt restart; Node now protects them before opening the management listener, and the same-volume restart passed. Risk: Gate-wide Go, command-surface and acceptance suites still pending. Next: run Repository checks in plan order, audit G2-E1/E2/E3, then update the ledger once all pass.
- 2026-09-24: G2 identity audit follow-up. The first full Repository sequence passed (`go test ./pkg/cmd/tunnel/... ./internal/tunnelruntime/...`, `make command-surface`, `make acceptance`, including `acceptance/web`), then review found that deleting only `node.sqlite` after sidecar cleanup could be mistaken for a new empty directory. Added a private `node.initialized` marker before database initialization and `TestNodeStateDoesNotReplaceIdentityWhenOnlyDatabaseIsLost`; missing database now rejects without creating a replacement. Matched the existing Windows SQLite URI and private ACL helper for Node directory, marker and database files. Directed `go test ./pkg/cmd/tunnel/node -count=1` and Windows `GOOS=windows GOARCH=amd64 CGO_ENABLED=0 go test -c ./pkg/cmd/tunnel/node` passed. Risk: the full Repository sequence predates this follow-up. Next: rerun it in plan order, then inspect diff and record G2 Exit evidence.
- 2026-09-24: G2 final acceptance and status transition. G2-E1: `TestNodeStateIdentityPersistsAndDirectoryIsExclusive`, `TestNodeStateRejectsDamagedOrIncompleteDirectoryWithoutReplacingIdentity`, `TestNodeStateDoesNotReplaceIdentityWhenOnlyDatabaseIsLost`, and independent-binary `TestTunnelNodeStandaloneBinaryIdentityAndCLI` prove the single startup command, full stable fingerprint across same-volume process restart, exclusive private state, and refusal to replace missing/damaged identity. G2-E2: `TestNodeClaimFirstControllerWinsAndBindingSurvivesRestart` proves preview does not bind, two concurrent valid encrypted claims have one durable winner, the other Controller and repeated claim receive `NODE_ALREADY_CLAIMED`, and only the winner can query after restart; `TestNodePreviewFingerprintMismatchSendsNoClaim` and the wrong encrypted Node-ID case prove identity mismatch aborts without claim. G2-E3: `TestNodeNoiseXXHandshakeAndEncryptedFrame`, `TestNodeRejectsWrongVersionOversizeSequenceAndIdentityWithoutBinding`, `TestNodeExpiresHandshakeAndSessionWithoutBinding`, and `TestNodeHealthContainsNoIdentityOrState` prove the XX/HTTP encrypted management boundary, strict sequence/replay, 96 KiB HTTP and 65535-byte Noise frame limits, expiry, wrong-version rejection, and unchanged binding on invalid input; `go test github.com/flynn/noise` passed its protocol-vector suite. Final `go test ./pkg/cmd/tunnel/... ./internal/tunnelruntime/...` passed after the last test-only fingerprint case; after the last production edit, `make command-surface` and `make acceptance` both passed (`acceptance` and `acceptance/web`). Node race tests and Windows cross-compilation passed. `git diff --check` passed. All G2 Exit conditions are satisfied; G2 marked `passed`, G3 marked `active` and activated without implementation. This Goal's work is complete; no G3 work was started.

### G3: Node FRPS 收敛与崩溃恢复

- 2026-09-23: initialized as `planned`; no implementation evidence.
- 2026-09-24: slice 1 (complete snapshot validation) complete. Added `pkg/cmd/tunnel/node/snapshot.go` and `snapshot_test.go`. Running/disabled snapshots now reject wrong Node/format/FRP version, malformed or extra fields, incomplete runtime, port overlap, oversized custom page and disabled credentials before any state mutation. Directed `TestDecodeDesiredSnapshotValidatesCompleteRunningAndDisabledStates` and repository `go test ./pkg/cmd/tunnel/... ./internal/tunnelruntime/...` passed. Risk: the validator is not yet wired to encrypted chunk transfer or persistence. Next: add complete-candidate/high-water transaction and bounded chunk transfer.
- 2026-09-24: slice 2 (durable high-water candidate transaction) complete. Changed Node `state.go`; added `runtime_state.go` and `runtime_state_test.go`. Private schema v2 atomically stores the highest accepted revision, original SHA-256, candidate and phase after full validation. Lower revisions and same-revision different bytes reject without mutation; same-revision matching bytes retain their result, including after reopening the same directory. Directed `TestNodeCandidateHighWaterTransactionAndRestart` and repository `GOMAXPROCS=2 GOFLAGS=-p=1 go test ./pkg/cmd/tunnel/... ./internal/tunnelruntime/...` passed. Risk: no management transfer or FRPS application is wired yet. Next: implement bounded encrypted begin/chunk/commit transfer and prove partial transfer cannot change the database.
- 2026-09-24: slice 3 (encrypted bounded snapshot transfer) complete. Changed Node `management.go` and `state.go`; added `management_snapshot.go` and `management_snapshot_test.go`. Authenticated `applySnapshot` now accepts ordered begin/chunk/commit within one Noise session, writes 0600 private temporary bytes, checks 32 KiB chunks, 2 MiB total, exact length and SHA-256, and invokes the high-water transaction only after full validation. Status exposes version/digest/phase without the Token. Directed `TestNodeSnapshotTransferRequiresCompleteOrderedBytesBeforeAcceptance` passed for out-of-order rejection, multi-chunk commit, status and expiry cleanup; repository `GOMAXPROCS=2 GOFLAGS=-p=1 go test ./pkg/cmd/tunnel/... ./internal/tunnelruntime/...` passed. Risk: accepted candidates are not yet applied to FRPS; process and restart evidence remains. Next: wire fixed-FRP candidate verify, start confirmation and rollback.
- 2026-09-24: slice 4 (running candidate application and rollback) complete. Added `node_runtime.go` and `node_runtime_test.go`; wired the production management handler through `run.go` and reported separate process observation in `management.go`. A pinned FRP 0.70.1 binary verifies private rendered TOML before the old child stops, then supervisor activation precedes the lastGood/applied transaction. Directed `TestNodeRunningSnapshotVerifyStartAndRollbackWithPinnedFRPS` passed: real listener activation, injected verification failure preserving old service, occupied candidate port restoring lastGood, and occupied old plus candidate ports producing `NODE_ROLLBACK_FAILED` with stopped process and unchanged applied revision. Repository `GOMAXPROCS=2 GOFLAGS=-p=1 go test ./pkg/cmd/tunnel/... ./internal/tunnelruntime/...` passed. Risk: disabled, cold recovery, crash-point and residual ownership handling remain. Next: implement durable disabled transition.
- 2026-09-24: slice 5 (durable disabled transition) complete. Changed `node_runtime.go` and `node_runtime_test.go`. Disabled now commits `bootDisabled=true` and incomplete phase before stopping the managed FRPS, removes active rendered configuration and 404 references after stop, then commits `disabledComplete` and the applied revision while retaining Controller binding. Directed `TestNodeDisabledIntentPrecedesStopAndClearsRuntimeReferences` passed with real pinned FRPS and same-directory reopen; repository `GOMAXPROCS=2 GOFLAGS=-p=1 go test ./pkg/cmd/tunnel/... ./internal/tunnelruntime/...` passed. Risk: cold startup does not yet consume bootDisabled/lastGood, and forced-kill residual ownership is unresolved. Next: implement startup recovery and reliable residual-process identification, then run crash-point tests.
- 2026-09-24: slice 6 (cold recovery and residual FRPS ownership) complete. Changed Node `state.go`, `runtime_state.go`, `node_runtime.go`, `run.go` and `management.go`; added `process_owner_unix.go`, `process_owner_windows.go`, and `node_recovery_unix_test.go`, with additional runtime tests. Schema v3 records PID, start time and exact pinned binary/config command; Unix recovery confirms all before terminating an orphaned process and scans the private config path for unknown residuals, while Windows relies on the existing kill-on-close Job Object. Cold startup restores lastGood, marks interrupted candidate retriable, or completes persisted disabled without reviving FRPS; abandoned chunk files are removed. Directed `TestNodeSIGKILLRecoveryAtRuntimeCommitBoundaries` passed across eight real process kill points; `TestNodeUnknownResidualFRPSIsPreserved`, `TestNodeSupervisorRestartUpdatesDurableOwner`, and `TestNodeRunningRetryAfterDisabledAndInterruptedAcceptance` passed. Repository `GOMAXPROCS=2 GOFLAGS=-p=1 go test ./pkg/cmd/tunnel/... ./internal/tunnelruntime/...` passed. Risk: standalone release-binary acceptance and final Gate verification remain; Unix ownership uses exact PID/start/group/command evidence and pauses when this is uncertain. Next: add standalone Node apply/restart acceptance and run Gate-wide checks in contract order.
- 2026-09-24: slice 7 (standalone binary apply and response-loss acceptance) complete. Added `acceptance/node_runtime_test.go`; updated encrypted status/commit results in Node `management.go` and `management_snapshot.go`, and asserted same-revision PID stability in `node_runtime_test.go`. The built `ycy tunnel node` binary was claimed over Noise, applied a real running FRPS snapshot, dropped the commit response, queried the durable applied result in a new session, retried the same digest without restarting FRPS, recovered the same identity/configuration after process kill, applied disabled and stayed stopped after another restart. Directed `go test -tags acceptance ./acceptance -run '^TestTunnelNodeStandaloneBinaryAppliesRestoresAndDisablesFRPS$' -count=1 -v` passed with pinned FRP 0.70.1. Risk: final Go, race/cross-platform, acceptance and terminal suites remain. Next: run those checks, audit E1/E2/E3, then update the Gate ledger only if all Exit conditions pass.
- 2026-09-24: Gate audit follow-up before final transition. `GOMAXPROCS=2 GOFLAGS=-p=1 go test -race ./pkg/cmd/tunnel/node -run '^TestNode(SnapshotTransferRequiresCompleteOrderedBytesBeforeAcceptance|SupervisorRestartUpdatesDurableOwner|SIGKILLRecoveryAtRuntimeCommitBoundaries|RunningSnapshotVerifyStartAndRollbackWithPinnedFRPS)$' -count=1`, Windows amd64 Node test compilation, final uncached `go test -count=1 ./pkg/cmd/tunnel/... ./internal/tunnelruntime/...`, `make acceptance`, and `make check-terminal` all passed in plan order. Then audit found two narrow evidence gaps: encrypted status/commit lacked explicit `failedRevision`, and startup database inspection checked only runtime table shape instead of row/digest validity. Added the status field in `management.go`/`management_snapshot.go`, its real failure projection assertion in `node_runtime_test.go`, and a pre-open row/digest check in `state.go` with `TestNodeStateRejectsIncompleteRuntimeWithoutChangingDatabase`; both directed tests passed. Risk: the full Repository sequence predates these last edits. Next: rerun Go, acceptance, and terminal in order, then record E1/E2/E3 and transition only G3.
- 2026-09-24: G3 final acceptance and status transition. G3-E1: `TestDecodeDesiredSnapshotValidatesCompleteRunningAndDisabledStates`, `TestNodeCandidateHighWaterTransactionAndRestart`, `TestNodeSnapshotTransferRequiresCompleteOrderedBytesBeforeAcceptance`, `TestNodeStateRejectsIncompleteRuntimeWithoutChangingDatabase`, and standalone-binary lost-commit-response/same-digest retry prove complete bounded snapshots, identity/version/digest checks, monotonic durable high water, and no mutation from partial, invalid or out-of-order input. G3-E2: real pinned-FRP `TestNodeRunningSnapshotVerifyStartAndRollbackWithPinnedFRPS` proves verify failure leaves old service running, failed candidate startup restores lastGood, failed rollback reports stopped with unchanged applied revision and explicit failedRevision; `TestNodeSIGKILLRecoveryAtRuntimeCommitBoundaries` proves lastGood restoration or new applied state across eight process-kill points, while the standalone binary reopens the same directory and serves its prior running configuration. G3-E3: `TestNodeDisabledIntentPrecedesStopAndClearsRuntimeReferences`, `TestNodeRunningRetryAfterDisabledAndInterruptedAcceptance`, the disabled kill points and standalone-binary disabled restart prove bootDisabled is durable before stop and never revives old FRPS; `TestNodeUnknownResidualFRPSIsPreserved` proves unknown ownership is retained and reported, and `TestNodeSupervisorRestartUpdatesDurableOwner` proves an automatic child restart updates PID ownership. Final `GOMAXPROCS=2 GOFLAGS=-p=1 go test -count=1 ./pkg/cmd/tunnel/... ./internal/tunnelruntime/...`, `GOMAXPROCS=2 GOFLAGS=-p=1 make acceptance` (including `acceptance/web`), and `GOMAXPROCS=2 GOFLAGS=-p=1 make check-terminal` passed in plan order after the last production and test edits; targeted Node race tests and Windows amd64 cross-compilation passed. `git diff --check` and `gofmt -l` were clean. All G3 Exit conditions are satisfied; G3 marked `passed`, G4 marked `active` and activated without implementation. This Goal's work is complete; no G4 work was started.

### G4: Server 远端 Node 管理

- 2026-09-23: initialized as `planned`; no implementation evidence.
- 2026-09-24: activated after G3 passed; implementation has not started.
- 2026-09-24: slice 1 (read-only Noise identity preview) complete. Added `server_node_management_wire.go` and `server_node_management_wire_test.go`. Server performs only the first two Noise XX frames, validates a full 32-byte Node public key, and presents its full SHA256 fingerprint; the real loopback Node test confirms preview leaves binding empty. Directed `TestNodeManagementPreviewReadsFullIdentityWithoutClaiming` passed; repository `GOMAXPROCS=2 GOFLAGS=-p=1 go test ./pkg/cmd/tunnel/... ./internal/tunnelruntime/...` passed. Risk: Controller-authenticated status, claim, snapshot transfer and persistence are not wired. Next: implement encrypted status read through a fresh pinned-identity session.
- 2026-09-24: slice 2 (authenticated Node status read) complete. Changed `server_node_management_wire.go` and its test. A fresh XX session pins the complete Node public key before the third frame, authenticates the encrypted confirmation, then reads `status` using the Controller's long-term key. The real Node test rejects a wrong pinned identity and a different Controller, while the bound Controller receives Node ID, versions and observation time. Directed `TestNodeManagementStatusRequiresPinnedIdentityAndController|TestNodeManagementPreviewReadsFullIdentityWithoutClaiming` passed; repository `GOMAXPROCS=2 GOFLAGS=-p=1 go test ./pkg/cmd/tunnel/... ./internal/tunnelruntime/...` passed. Risk: claim and snapshot writes, persistent Server registry and coordinator remain. Next: add encrypted claim as its own write slice.
- 2026-09-24: slice 3 (encrypted claim write) complete. Changed `server_node_management_wire.go` and its test. Claim uses a new pinned XX session and encrypted unique request ID, accepts only an authenticated claimed receipt, and surfaces uncertain transport outcome as `NODE_CLAIM_OUTCOME_UNKNOWN`. Real Node test proves changed preview identity cannot bind, successful claim permits status, and repeated claim fails. Directed `TestNodeManagement*` passed. First repository `GOMAXPROCS=2 GOFLAGS=-p=1 go test ./pkg/cmd/tunnel/... ./internal/tunnelruntime/...` failed at the unrelated existing `TestServerAgentConnectionAcknowledgesReplacementToken` (`Activate() = nil`); that test passed ten isolated repetitions and the full repository command passed on rerun. Risk: the existing replacement-token test showed an intermittent failure; snapshot transfer and durable Server management remain. Next: add bounded encrypted snapshot transfer as a separate write slice.
- 2026-09-24: slice 4 (encrypted complete snapshot transfer) complete. Changed `server_node_management_wire.go` and its test. A pinned session sends begin, ordered 32 KiB chunks, and commit for a bounded 2 MiB snapshot with SHA-256 and exact FRP version. Real Node accepted a disabled snapshot and a fresh encrypted status read confirmed applied revision 1 and durable disabled completion. Initial directed assertion used `disabledComplete` as the phase name; corrected to Node's actual `phase=disabled` with separate completion flag. Directed `TestNodeManagement*` and repository `GOMAXPROCS=2 GOFLAGS=-p=1 go test ./pkg/cmd/tunnel/... ./internal/tunnelruntime/...` passed. Risk: Server registry, reconciliation, API and UI are still absent. Next: persist Remote Node records and distinct desired/observed states.
- 2026-09-24: slice 5 (durable Remote Node registration) complete. Added `server_nodes.go` and `server_nodes_test.go`. An immediate transaction saves the fixed Node ID/public key, normalized management address, unique generated FRP Token and port-pool row; a re-added Node starts from its observed high-water revision but remains unconfigured. Duplicate address/public key rolls back the entire registration. Directed `TestServerNodeRegistryRegisterIsAtomicAndUnconfigured` and repository `GOMAXPROCS=2 GOFLAGS=-p=1 go test ./pkg/cmd/tunnel/... ./internal/tunnelruntime/...` passed. Risk: observation freshness and the coordinator are not yet connected. Next: persist observations while requiring a new-session check before treating running as current.
- 2026-09-24: slice 6 (separate durable observation and freshness) complete. Added `server_node_observation.go` and its focused test. Last authenticated status is saved independently from desired state; process-local freshness expires after 30 seconds, and a restarted Server always projects cached running only as historical while current management/FRPS are unknown. Wrong-version and identity failure categories retain the last known status without reporting it as current. Directed `TestServerNodeObservationSeparatesSavedHistoryFromFreshRuntime|TestServerNodeRegistryRegisterIsAtomicAndUnconfigured` and repository `GOMAXPROCS=2 GOFLAGS=-p=1 go test ./pkg/cmd/tunnel/... ./internal/tunnelruntime/...` passed. Risk: observations are not yet driven by a coordinator, and management API/UI remain. Next: add periodic authenticated status polling and bounded reconciliation outside database transactions.
- 2026-09-24: slice 7 (periodic authenticated reconciliation) complete. Added `server_node_coordinator.go` and its real Node test; wired startup/shutdown in `server_runtime.go` and Remote reads in `server_nodes.go`. The coordinator polls with the fixed Controller key, applies a complete saved snapshot only when Node high-water permits, then re-reads durable status; network calls stay outside transactions. Real Node test proves applied disabled revision and, after Node shutdown, an unreachable management state with historical applied result but unknown current FRPS. Directed `TestServerNodeCoordinatorAppliesDesiredAndKeepsHistoryWhenOffline` and repository `GOMAXPROCS=2 GOFLAGS=-p=1 go test ./pkg/cmd/tunnel/... ./internal/tunnelruntime/...` passed. Risk: desired writes, admin/public API, SSE and page are not yet implemented. Next: add atomic running desired-setting save and port-pool validation.
- 2026-09-24: slice 8 (atomic running desired save) complete. Added `server_node_desired.go` and its test. A single immediate transaction checks an expected revision, FRPS port overlap, 512 KiB page limit and existing remote TCP/UDP occupancy, then stores a full Node snapshot, SHA-256, incremented revision and immediately effective port pool. Directed `TestServerNodeDesiredSaveUpdatesPoolAtomically` passed; its fixture initially exposed that the existing Tunnel creation path is Local-only until G5, so the test materializes remote ownership in one test transaction. Repository `GOMAXPROCS=2 GOFLAGS=-p=1 go test ./pkg/cmd/tunnel/... ./internal/tunnelruntime/...` passed. Risk: reapply and address edits, admin/public API and Web page remain. Next: implement higher-revision reapply without changing settings.
- 2026-09-24: slice 9 (higher-revision reapply) complete. Changed `server_node_desired.go` and its test. Reapply loads the saved running snapshot, rejects unconfigured Node, and commits the same settings and Token at the next revision for coordinator delivery; no command-style restart was introduced. Directed `TestServerNodeReapplyAdvancesRevisionWithoutChangingSettings|TestServerNodeDesiredSaveUpdatesPoolAtomically` and repository `GOMAXPROCS=2 GOFLAGS=-p=1 go test ./pkg/cmd/tunnel/... ./internal/tunnelruntime/...` passed. Risk: address candidate validation, Web API, public projection and page remain. Next: stage management-address changes and activate only after pinned identity verification.
- 2026-09-24: slice 10 (staged management address and metadata) complete. Added `server_node_metadata.go` and its real Node test; changed registry/coordinator to persist a pending address and authenticate its fixed Node public key and Controller binding before activation. A different Node at the candidate address leaves the original active address intact with an identity-mismatch code; restarting the original Node at a new address activates it. Public FRP endpoint edits advance assigned Client desired revisions transactionally. Directed `TestServerNodeManagementAddressRequiresOriginalIdentity` passed after correcting a test fixture's initial Client revision assumption; repository `GOMAXPROCS=2 GOFLAGS=-p=1 go test ./pkg/cmd/tunnel/... ./internal/tunnelruntime/...` passed. Risk: API authorization/projection, SSE and page remain. Next: expose admin preview/claim and management writes through existing HTTP/session boundaries.
- 2026-09-24: slice 11 (admin preview and claim API) complete. Added `server_node_service.go`, `server_http_nodes.go`, and its end-to-end API test; wired the service through runtime and workspace authorization. Preview handles are short-lived and session-bound; confirmation redoes the pinned Noise handshake, with separate `claim` and explicit `readd` paths. Admin-only same-origin API test verifies public users and foreign Origin are denied, wrong full fingerprint does not bind, and correct confirmation returns 201 only after Server registration. Directed `TestServerHTTPNodePreviewAndClaimRequireAdminAndPinnedFingerprint` and repository `GOMAXPROCS=2 GOFLAGS=-p=1 go test ./pkg/cmd/tunnel/... ./internal/tunnelruntime/...` passed. Risk: management writes, public/detail projections, SSE and Nodes page remain. Next: expose metadata, desired and reapply writes with expected-revision and role checks.
- 2026-09-24: slice 12 (admin metadata PATCH API) complete. Changed Node service/workspace/HTTP adapter and the real Node API test. `PATCH /api/nodes/{id}` checks session, admin role and same Origin, then returns only after transactional metadata save; management address remains a pending candidate while the original address stays active. Directed `TestServerHTTPNodePreviewAndClaimRequireAdminAndPinnedFingerprint` covers user/foreign-origin denial and persisted pending/public fields; repository `GOMAXPROCS=2 GOFLAGS=-p=1 go test ./pkg/cmd/tunnel/... ./internal/tunnelruntime/...` passed. Risk: desired/reapply API and public/detail reads, SSE, UI remain. Next: expose desired save and reapply with distinct saved-vs-applied response semantics.
- 2026-09-24: slice 13 (admin desired and reapply APIs) complete. Changed Node service/workspace/HTTP adapter and the API test. `PUT /api/nodes/{id}/desired` requires an expected revision; `POST /api/nodes/{id}/reapply` creates the next full snapshot. Both return `configuration=pending` after Server persistence and wake reconciliation, without treating the HTTP response as remote application. Directed API test proves user denial, stale revision 409 and durable versions 1 then 2; repository `GOMAXPROCS=2 GOFLAGS=-p=1 go test ./pkg/cmd/tunnel/... ./internal/tunnelruntime/...` passed. Risk: public/detail projections, SSE and page remain. Next: add redacted public Node summaries and shared selectability rules.
- 2026-09-24: slice 14 (public Node read projection) complete. Added `server_node_projection.go`; `GET /api/nodes` and `GET /api/nodes/{id}` use the session-authorized service to project Local and Remote summaries, current observation freshness, historical FRPS separately, and a single server-side selectability rule. The normal-user API test confirms both Nodes are visible and management address, fingerprints, Token, desired revision and detailed error fields are absent. Directed API test and repository `GOMAXPROCS=2 GOFLAGS=-p=1 go test ./pkg/cmd/tunnel/... ./internal/tunnelruntime/...` passed. Risk: admin detail, SSE and page remain. Next: expose admin-only management detail with separate saved, applied and running layers.
- 2026-09-24: slice 15 (admin management read projection) complete. Added `server_node_management_view.go` and `GET /api/nodes/{id}/management` behind workspace admin authorization. Detail separates saved desired revision/settings, authenticated accepted/applied/failed revisions, current FRPS observation, stale history and safe error code; Node/Controller fingerprints and addresses are admin-only, FRP Token remains absent. Directed API test covers user 403 and a saved revision 2 with applied revision 1, failed revision 2 and old FRPS still running; repository `GOMAXPROCS=2 GOFLAGS=-p=1 go test ./pkg/cmd/tunnel/... ./internal/tunnelruntime/...` passed. Risk: SSE and Nodes page, real running FRPS end-to-end and final audit remain. Next: publish Node changes over the existing SSE invalidation route and prove disconnect/re-GET behavior.
- 2026-09-24: slice 16 (Node invalidation on existing SSE) complete. Workspace observation now subscribes every logged-in role to Node registry/observation changes and emits the existing secret-free `changed` event. Real HTTP stream test receives a Node change, disconnects, and re-GETs the public list showing incompatible management, current FRPS unknown and historical running separately. Directed API/SSE test and repository `GOMAXPROCS=2 GOFLAGS=-p=1 go test ./pkg/cmd/tunnel/... ./internal/tunnelruntime/...` passed. Risk: Nodes page and full real-running/readd acceptance remain. Next: prove real running FRPS application and explicit same-Controller readd over the Server adapter.
- 2026-09-24: slice 17 (real running FRPS and explicit readd) complete. Added `server_node_integration_test.go`. A real Node process and pinned FRP 0.70.1 accept Server preview/claim, complete running snapshot and actual FRPS listener; Server detail shows desired/applied revision 1, fresh running and selectable. After only the Server Node row is removed, the same Controller explicitly re-adds the bound Node, starts at its remote high-water 1 and converges a new full snapshot at revision 2; test sends disabled revision 3 for process cleanup. Directed `TestServerNodeRealFRPSClaimApplyAndExplicitReadd` passed. First repository run failed because an unrelated Local integration test's official FRP archive download returned EOF; locally installed frpc/frps hashes matched the manifest, and `YCY_TUNNEL_TEST_FRP_RUNTIME_DIR='/Users/qigong-it-1/Library/Application Support/ycy/frp/0.70.1' GOMAXPROCS=2 GOFLAGS=-p=1 go test ./pkg/cmd/tunnel/... ./internal/tunnelruntime/...` passed. Risk: Web page and final acceptance/audit remain. Next: preserve API error codes in Web client, then build Nodes view/actions.
- 2026-09-24: slice 18 (structured Web API errors) complete. Changed `web/tunnel-server/api.ts` and added `api.test.ts`. `ApiError` now preserves stable `error.code` and safe `details` alongside the existing message/status; a focused Node identity-mismatch fixture passed. Initial `make check-web` runs found only new test lint/type syntax issues, which were corrected; final `make check-web` passed lint, TypeScript, all 43 Web tests and Vite asset verification. Risk: Nodes read/action UI is not implemented yet. Next: build the public Nodes list and admin detail read view before adding writes.
- 2026-09-24: slice 19 (public list and admin detail read UI) complete. Added `web/tunnel-server/nodes-pages.tsx`; updated app navigation/routes, status tones, compact Node styles and the Tunnel route matrix. All logged-in users can scan Local/Remote public address, management, FRPS, historical observation and availability; Node detail shows admin-only identity, desired/applied/failed versions and safe failure stage. Initial Web check flagged JSX formatting only; ESLint formatting and final `make check-web` passed lint, typecheck, all 43 tests and asset verification. Risk: admin claim/config/address actions and browser visual check remain. Next: implement preview and confirmed claim/readd UI as a separate interaction slice.
- 2026-09-24: slice 20 (Web claim/re-add interaction) complete. Added `web/tunnel-server/node-claim.tsx`; Nodes page now offers admin-only Add Node with session-bound preview, full fingerprint confirmation and explicit Claim/Re-add mode, while stable API error codes map to concrete next steps. `make check-web` passed lint, typecheck, all 43 tests and asset verification after formatting the new JSX. Risk: Remote configuration/address actions and browser visual check remain. Next: add detail-page desired configuration editing and higher-revision reapply.
- 2026-09-24: slice 21 (Web metadata/configuration actions) complete. Added `web/tunnel-server/node-config.tsx`; admin Node details now edit display/public/management addresses with pending-address messaging, save complete running settings and queue higher-revision reapply. Local Node remains read-only. Final `make check-web` and `make acceptance-web` passed after these UI changes (11 Web test files, 43 tests, Vite asset verification and acceptance/web).
- 2026-09-24: G4 final acceptance and status transition. G4-E1: `TestServerNodeRealFRPSClaimApplyAndExplicitReadd` plus the real Noise wire tests prove same-version preview, full fingerprint-pinned claim, complete running snapshot transfer, actual pinned FRPS listener, durable status, and explicit same-Controller re-add after the Server row is lost; the re-add resumes remote high-water and converges revision 2. G4-E2: `TestServerHTTPNodePreviewAndClaimRequireAdminAndPinnedFingerprint` proves all-user public list/detail, admin-only preview/claim/PATCH/desired/reapply/management detail, same-origin and owner boundaries, stable error responses, and secret-free public projection; final `make check-web` and `make acceptance-web` cover the Nodes navigation, list/detail, claim dialog and configuration actions. G4-E3: observation/SSE tests plus real coordinator integration prove saved desired, Node accepted/applied/failed, current FRPS, historical FRPS and management reachability remain separate; wrong identity, protocol mismatch, stale/expired observations and disconnected Node never become false stopped/running claims. Final repository `YCY_TUNNEL_TEST_FRP_RUNTIME_DIR='/Users/qigong-it-1/Library/Application Support/ycy/frp/0.70.1' GOMAXPROCS=2 GOFLAGS=-p=1 go test -count=1 ./pkg/cmd/tunnel/... ./internal/tunnelruntime/...` passed; final `make acceptance` (including acceptance/web), `make command-surface`, `git diff --check`, and `gofmt -l` passed. A broader `make check-terminal` run reached all Tunnel packages but exposed the unrelated pre-existing `pkg/cmd/rm` Rich PTY zero-candidate output race (also reproduced 2/10 in isolation); no G4 assertion was weakened or skipped. All G4 Exit conditions are satisfied; G4 marked `passed`, G5 marked `active` and activated without implementation. This Goal's work is complete; no G5 work was started.

### G5: Node 资源与 Client 归属

- 2026-09-23: initialized as `planned`; no implementation evidence.

### G6: 官方 Client 跨 Node 连接

- 2026-09-23: initialized as `planned`; no implementation evidence.

### G7: 移除、部署与发布验收

- 2026-09-23: initialized as `planned`; no implementation evidence.
