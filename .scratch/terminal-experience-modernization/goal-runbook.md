# B Ops Console Lifecycle Refactor Goal Runbook

## Source Baseline

| Path | SHA-256 |
| --- | --- |
| `.scratch/terminal-experience-modernization/implementation-plan.md` | `7622c8520c732988bf240d866ae91a599c5e7c42d7603eb3ac44890f9119d56a` |
| `.scratch/terminal-experience-modernization/issues/05-prototype-vivid-terminal-visual-system.md` | `adfdf81af89332ba66f04bb71f6c7555152afb182fcdf9529e623032908a92d2` |
| `.scratch/terminal-experience-modernization/issues/06-choose-shared-terminal-experience-contract.md` | `34166d3fff0e6015fa97028fe3d175fbc80c858fb71498c1d0c472416189fc71` |
| `.scratch/terminal-experience-modernization/issues/07-choose-root-help-and-error-presentation.md` | `e28081d4337a6986e078762dc94b1f3faeb45d2687e03e7ee74fc62cd91e0356` |
| `.scratch/terminal-experience-modernization/issues/08-choose-export-env-presentation.md` | `20596a71b236eaabdeecda48321b87ad7900628b8456bb6a9b43e54d248d9f4e` |
| `.scratch/terminal-experience-modernization/issues/09-choose-config-fork-list-presentation.md` | `516d45d5a80fb0a1a733ccbaee3fd95fa6a0b21916441a181ed20294b79ac088` |
| `.scratch/terminal-experience-modernization/issues/10-choose-config-fork-add-presentation.md` | `7f5039d12cc2377f366ab2ee551a023eafa1126db1876cdd690ac6813bf06620` |
| `.scratch/terminal-experience-modernization/issues/11-choose-config-fork-remove-presentation.md` | `d02eb7411d8562f9702d9de6446966f0846652bc03679cced381608635298903` |
| `.scratch/terminal-experience-modernization/issues/12-choose-config-cm-list-presentation.md` | `5b17fc8d17043ddf9908af4c38322a2d3d742d31f7fe528559461731bc10811a` |
| `.scratch/terminal-experience-modernization/issues/13-choose-config-cm-add-presentation.md` | `b85343d999c75325a58f1d0e481bea9eac5688caf855559c0308a84dbb2c4395` |
| `.scratch/terminal-experience-modernization/issues/14-choose-config-cm-use-presentation.md` | `9c92c7d7c612b1d15fce2170f88ebfc1f4be3e4954f6e4db31523c51fd088fdb` |
| `.scratch/terminal-experience-modernization/issues/15-choose-config-cm-set-presentation.md` | `fadd58c91e2da00a734cf0a6ca7a23689f2aac544e5d6ff2fcfdad0bba6ac346` |
| `.scratch/terminal-experience-modernization/issues/16-choose-config-cm-remove-presentation.md` | `8c6f25fbdc961e5b79e8cf667144edb48dd78fee07f81bcedf5e014faf5053d6` |
| `.scratch/terminal-experience-modernization/issues/17-choose-config-cm-test-presentation.md` | `1ea070f25683553f651f317d87c62a1cec36b51461e0df6c9456f933ecf2cf5f` |
| `.scratch/terminal-experience-modernization/issues/18-choose-diff-lifecycle-log-presentation.md` | `f931611f45ae8b88592a3c12473c7ba6bac5093cf4d24a95356b6041c9a84e07` |
| `.scratch/terminal-experience-modernization/issues/19-choose-fs-lifecycle-log-presentation.md` | `6860f2f9c22909a533297af0a46e25c00e74d9c776c1c9fbd9609f1332ff6a8e` |
| `.scratch/terminal-experience-modernization/issues/20-choose-git-heat-presentation.md` | `b66cac29c9724362ca5e089fcc4b3fd8d795044d148d9ea1ca5a99e8fe36597b` |
| `.scratch/terminal-experience-modernization/issues/21-choose-git-pulse-presentation.md` | `74ad403f2e1d55d35849b7b5db9e697cac7d59cab99da8759a689b18de611db0` |
| `.scratch/terminal-experience-modernization/issues/22-choose-git-fork-presentation.md` | `b10ceb31876d8b9bdd4b4207a4d2e155b8f1e91a54892b0434265ec3043faa5d` |
| `.scratch/terminal-experience-modernization/issues/23-choose-git-cm-presentation.md` | `a2e9f8a81a2020dfea451f39c75a65f2fab1a63d8602d5687bd7166cc4333ee6` |
| `.scratch/terminal-experience-modernization/issues/24-choose-rm-presentation.md` | `1b656baf42bfc31de8c99878d923d90a5a21618732dd5f0e0821e5107c86caab` |
| `.scratch/terminal-experience-modernization/issues/25-choose-run-handoff-presentation.md` | `59f38d0c098ae525e3c8260edff7aa2414f91625536f8749ee02942530dc1e1a` |
| `.scratch/terminal-experience-modernization/issues/26-choose-tunnel-connect-lifecycle-log-presentation.md` | `e69927f9fc012de0e4ec96e7a606821d69ee2cf188d6c50449a2ce70a095ab8a` |
| `.scratch/terminal-experience-modernization/issues/27-choose-tunnel-server-lifecycle-log-presentation.md` | `89279bc77d21c8ceb78a234fbb438b0576358ac70c253b87b846b4ba90c63cf5` |
| `.scratch/terminal-experience-modernization/issues/28-choose-zip-presentation.md` | `e5ec30e94d8abc9050d43ab968f77cf4f01567f631c4d6664d25a58c0e45b560` |
| `.scratch/terminal-experience-modernization/issues/29-choose-upgrade-presentation.md` | `867722b759822bad286e9ab988e13160ae15cc5131beb78480a001da1ec57e4c` |
| `.scratch/terminal-experience-modernization/issues/30-approve-rollout-and-acceptance-plan.md` | `73b9ee80a04f158f8eaeecb8b23e8d7839f2f68b145568ec8e96fb95cd76203d` |
| `.scratch/terminal-experience-modernization/map.md` | `65b036d823e06f51b097769a46548d9a5f83938ce13e6f089ae0b09503cccd9d` |
| `CONTEXT.md` | `a3400217b0e5dfaf39dbb729c2a0fb6ae38d6fd3e1eab24d38e4a76bb55c6da0` |

## State Rules

- `implementation-plan.md` is the sole source of Gate contracts; this runbook records only state and evidence.
- Execute only the one `active` Gate in the Goal Ledger at a time.
- Append slice, changed files, verification, risks, and next action to that Gate's Progress Log each turn.
- `passed` requires explicit evidence for every plan Exit condition; ordinary implementation or failed verification remains `active`.
- Use `blocked` only for a plan Stop condition and record the blocker and recovery condition.
- Pending manual acceptance ends a Goal through a one-time handoff log entry; it is not `blocked`, and the Gate remains `active`.
- Passing a Gate activates only its direct successor and ends the current Goal. The successor is executed only by a new Goal.

## Goal Ledger

| Gate | Status | Depends on | Plan contract | Unlock evidence |
| --- | --- | --- | --- | --- |
| G0: Shared B lifecycle foundation | passed | none | `implementation-plan.md` -> `G0: Shared B lifecycle foundation` | all seven G0 Exit conditions accepted on 2026-09-08 |
| G1: Read-only and selection migration | passed | G0 | `implementation-plan.md` -> `G1: Read-only and selection migration` | all four G1 Exit conditions accepted on 2026-09-08 |
| G2: Mutation and archive migration | passed | G1 | `implementation-plan.md` -> `G2: Mutation and archive migration` | all three G2 Exit conditions accepted on 2026-09-09; final automated verification passed |
| G3: Handoff boundaries and release acceptance | passed | G2 | `implementation-plan.md` -> `G3: Handoff boundaries and release acceptance` | all four G3 Exit conditions accepted on 2026-09-09; explicit manual acceptance received |

## Progress Log

### G0: Shared B lifecycle foundation

- 2026-09-08: initialized as `active` after the lifecycle control-plane reset. No implementation or G0 evidence is imported; prior generic-renderer audit and `g0-*` artifacts are superseded. Next action: establish the semantic Catalog, `FinishRequest`, eager renderer, replacement, Outcome, and structured Transcript slices from the new plan.
- 2026-09-08: completed semantic Catalog/FinishRequest slice. Added bounded `ConsoleFormStep` and `ConsoleDescriptor.FormCatalog` normalization (field bounds, control stripping, duplicate IDs, 64-step cap), `InteractionRequest.ConsoleStepID`, and FinishRequest validation. `ExperienceRun.Finish` accepts the new request while dispatching the existing outcome/document call shape for unmigrated adapters; the runtime and semantic recorders retain at-most-once behavior. Changed `internal/terminal/semantic.go`, `internal/terminal/console.go`, `internal/terminal/experience.go`, `internal/terminaltest/semantic_recording.go`, `pkg/cmd/zip/terminal_test.go`, and added `internal/terminal/semantic_catalog_test.go`. Directed verification: `go test ./internal/terminal ./internal/terminaltest` passed. Risk: the compatibility dispatch remains temporarily broad until command adapters migrate in later Gates; no command business logic changed. Next action: make Rich startup eager for usable terminals and render the complete normalized Form Catalog before the first Ask.
- 2026-09-08: completed eager Rich and initial Form Catalog slice. `OpenConsole` now preflights and starts Rich before semantic interaction when the terminal is usable, with pre-AltScreen startup failure falling back to Plain; the initial normalized Catalog is rendered in order with the first row `ACTIVE`, remaining rows `PENDING`, and sensitive detail `[redacted]`, while catalog-backed Ask updates reuse existing rows. Changed `internal/terminal/experience.go`, `internal/terminal/rich.go`, `internal/terminal/console.go`, `internal/terminal/console_test.go`, and added/updated `internal/terminal/rich_eager_test.go`. Directed verification: `go test ./internal/terminal ./internal/terminaltest` and `cd internal/terminal/prototype-vivid && go test ./...` passed; PTY coverage asserts Catalog and AltScreen output before any Ask. Risk: legacy adapters without `ConsoleStepID` retain append compatibility until later command migration; no command business logic changed. Next action: replace the complete Form Catalog with one Work Catalog when tracking starts, retaining ordered rows and preventing form/work mixing.
- 2026-09-08: completed Form-to-Work replacement slice. `richStartTrackMsg` now clears the complete Form Catalog and starts the Work Catalog at table row zero, so form and work rows cannot mix; final Work rows continue to be retained when the active region changes. Changed `internal/terminal/rich.go` and `internal/terminal/console_test.go`. Directed verification: `go test ./internal/terminal ./internal/terminaltest`, repository package tests via `go test ./...`, and `cd internal/terminal/prototype-vivid && go test ./...` passed. Risk: legacy append behavior remains only for adapters without a Form Catalog before Work starts; no command behavior changed. Next action: add the Outcome view and exact 800 ms dwell with cancellation/renderer-termination interruption coverage.
- 2026-09-08: completed Outcome state and dwell slice already present in the working tree. Rich now retains final Work rows, renders bounded Outcome status/location/summary in AltScreen, ignores late form/work/notice/milestone messages, and exits through the exact `800*time.Millisecond` model message with context/renderer interruption paths. Changed `internal/terminal/experience.go`, `internal/terminal/rich.go`, `internal/terminal/console.go`, and `internal/terminal/console_test.go`. Directed verification: `go test ./internal/terminal ./internal/terminaltest` and `cd internal/terminal/prototype-vivid && go test ./...` passed; targeted Finish PTY coverage passed. Risk: broader command-package expectations currently fail around migrated output/projection behavior and remain to be diagnosed before final repository verification. Next action: strengthen Finish PTY ordering evidence, then partition Transcript rendering and add boundary tests.
- 2026-09-08: completed Finish PTY ordering evidence. `TestRichRuntimeFinishRestoresThenReplaysBeforeDiagnosticsAndResult` now uses an explicit `terminal.FinishRequest`, distinguishes live `SUCCEEDED`/summary from restored lower-case Transcript lines, and asserts primary-screen restoration, Transcript replay, FIFO diagnostics, and final stdout result ordering. Changed `internal/terminal/rich_runtime_pty_test.go`. Directed verification: targeted Finish PTY test, `go test ./internal/terminal ./internal/terminaltest`, and `cd internal/terminal/prototype-vivid && go test ./...` passed. Risk: a full `go test ./...` run still reports command-package compatibility failures unrelated to this test-only slice; these must be resolved or evidenced before G0 exit. Next action: partition Transcript rendering into the required `ANSWERS`, `WORK`, optional `AT`, `OUTCOME` sections and add redaction/bounds/order boundary tests.
- 2026-09-08: completed structured Transcript and Finish Result-separation slice. `TranscriptLedger.Render` now partitions durable entries as `ANSWERS`, `WORK`, optional prototype-aligned `AT       <location>`, and `OUTCOME`; it retains control-free, bounded outcome location/summary fields and preserves redaction. `FinishRequest.Summary` drives only the final Rich view and Transcript, while the optional second `Finish` argument remains the separate durable stdout Result; legacy `Finish(outcome, result)` does not infer a summary from Result bytes. Changed `internal/terminal/transcript.go`, `internal/terminal/experience.go`, `internal/terminal/transcript_test.go`, `internal/terminal/semantic_catalog_test.go`, `internal/terminal/rich_runtime_pty_test.go`, `internal/terminal/rich_recovery_test.go`, and direct command PTY assertions in `pkg/cmd/config/fork/remove/terminal_pty_test.go`, `pkg/cmd/rm/terminal_pty_test.go`, and `pkg/cmd/rm/terminal_pty_extended_test.go`. Directed verification: `go test ./internal/terminal ./internal/terminaltest` and `go test ./...` passed. Risk: G0 still needs direct PTY evidence for failed/cancelled Outcomes, interrupted dwell, and the full `120x40`/`70x20`/`40x15` colored and `NO_COLOR=1` matrix. Next action: add that bounded G0 verification coverage without changing command business logic or stdout behavior.
- 2026-09-08: completed Outcome and renderer-matrix verification slice. Added initial-size PTY support in `internal/terminaltest` and direct Rich lifecycle coverage in `internal/terminal/rich_outcome_pty_test.go`: success at `120x40`, `70x20`, and `40x15` in colored and `NO_COLOR=1` modes; failed and cancelled Outcome projections; primary-screen restoration; structured Transcript-to-stdout ordering; and immediate context cancellation of the fixed dwell. Added model evidence for failed/cancelled Outcome views and late Work rejection in `internal/terminal/console_test.go`, plus renderer-termination-during-dwell recovery and bounded replay in `internal/terminal/rich_recovery_test.go`. Directed verification: `go test -count=1 -v ./internal/terminal -run '^(TestRichRuntimeConsoleDimensionsAndColorPTY|TestRichRuntimeFailedAndCancelledOutcomesPTY|TestRichRuntimeContextInterruptsOutcomeDwellPTY|TestConsoleOutcomeProjectsFailureAndCancellation|TestRuntimeRecoversRendererTerminationDuringOutcomeDwell)$'` passed; all six matrix cases, both terminal outcomes, context interruption, and renderer recovery executed. Risk: final repository checks may expose compatibility regressions outside the shared implementation; next action: rerun the complete directed suite, audit fresh-control-plane evidence, then run repository verification in plan order.
- 2026-09-08: strengthened the Form-to-Work PTY evidence without changing runtime behavior. The matrix assertion now proves that the last complete Form Catalog frame precedes the first Work Catalog frame, in addition to the existing model proof that the rows are mutually exclusive. Changed `internal/terminal/rich_outcome_pty_test.go`. Directed verification: `go test -count=1 -v ./internal/terminal -run '^(TestRichRuntimeConsoleDimensionsAndColorPTY|TestRichRuntimeFailedAndCancelledOutcomesPTY)$'` passed across six dimension/color cases and two terminal outcomes. Risk: none identified in the shared lifecycle slice; next action: run the final directed and repository verification sequence against this exact worktree.
- 2026-09-08: G0 passed. Exit conditions accepted with semantic/model and PTY evidence: (1) eager Rich B shell and complete safe Form Catalog before first interaction; (2) mutually exclusive Form-to-Work replacement, including frame-order proof; (3) retained success/failed/cancelled Outcome with exact `800ms` message contract and context/renderer interruption recovery; (4) one `FinishRequest` Summary shared by Live View and Transcript while the optional Result remains independent stdout; (5) bounded, redacted `ANSWERS`, `WORK`, optional `AT       <location>`, `OUTCOME` ordering with primary-screen restoration and stdout-last proof; (6) Rich, Plain, Automation, redirected, cancellation, renderer-failure, lease, and recovery coverage; and (7) the fresh root control plane is the only execution input, with prior recovery artifacts held under `superseded/`. Final directed verification passed: `go test -count=1 ./internal/terminal ./internal/terminaltest` and `cd internal/terminal/prototype-vivid && go test ./...`. Final repository verification passed in declared order: `make command-surface`, `make check-terminal`, `make check`, and `git diff --check`. `make check` emitted only a non-failing Vite chunk-size advisory. Manual acceptance: none. No remote push. Risk: no G0 blocker remains; activate only G1 and end this Goal.

### G1: Read-only and selection migration

- 2026-09-08: initialized as `planned`; no implementation evidence.
- 2026-09-08: 已激活，尚未开始实施。G0 的全部 Exit conditions 已通过；本次 Goal 在此结束，G1 不在本次 Goal 的执行范围内。
- 2026-09-08: completed `config fork list` completion-projection slice. The existing immutable single-phase `TrackedOperation` remains this no-prompt command's Work Catalog; no artificial Form Catalog was added. Replaced Rich-only milestones and legacy `Finish(outcome, result)` with a command-owned `FinishRequest`: the Outcome and Transcript now share only the safe loaded-count/empty-state summary, while names, hosts, and token previews remain exclusively in the durable Result. Changed `pkg/cmd/config/fork/list/command.go`, `pkg/cmd/config/fork/list/presentation.go`, `pkg/cmd/config/fork/list/terminal_test.go`, and `pkg/cmd/config/fork/list/terminal_pty_test.go`. Directed verification passed: `go test ./pkg/cmd/config/fork/list ./internal/terminal ./internal/terminaltest`; repository verification for the completed command slice passed: `make check-terminal`. Risk: the remaining scoped adapters still use legacy Finish compatibility and need their own command-local migrations. Next action: migrate `config cm list` as the next read-only single-phase slice.
- 2026-09-08: completed `config cm list` completion-projection slice. Retained its immutable `Load CM profiles` Work Catalog and existing Plain/Automation stream behavior. Replaced the legacy completion plus Rich-only count/default/empty milestones with one command-owned `FinishRequest`: Outcome and Transcript share the safe loaded count, a safely projectable matching default profile, and optional empty guidance; profile rows, Base URLs, models, and any credential material remain outside the summary and only in the durable Result. Changed `pkg/cmd/config/cm/list/command.go`, `pkg/cmd/config/cm/list/presentation.go`, `pkg/cmd/config/cm/list/terminal_test.go`, and `pkg/cmd/config/cm/list/terminal_pty_test.go`. Directed verification passed: `go test ./pkg/cmd/config/cm/list ./internal/terminal ./internal/terminaltest`; repository verification passed: `make check-terminal`. Risk: compact PTY redraw bytes do not preserve one contiguous Outcome-detail string, so the compact assertion proves Outcome state and post-restore full summary while the wide assertion proves the full Outcome detail; this is a test-observation limitation, not a behavior change. Next action: migrate `config cm use` as its one atomic Work Catalog slice.
- 2026-09-08: completed `config cm use` completion-projection slice. Retained the immutable one-phase atomic writer catalog, exact writer input, no-prompt behavior, and Plain/Automation streams. Replaced legacy `Finish` calls with a command-owned `FinishRequest`: successful Outcome/Transcript use `Default CM profile set` without repeating the requested identity, while the existing safe phase detail remains the only profile projection; failures use `Unable to set default CM profile`. Changed `pkg/cmd/config/cm/use/command.go`, `pkg/cmd/config/cm/use/terminal_test.go`, and `pkg/cmd/config/cm/use/terminal_pty_test.go`. Directed verification passed: `go test ./pkg/cmd/config/cm/use ./internal/terminal ./internal/terminaltest`; repository verification passed: `make check-terminal`. Risk: no cancellation projection was added because the source decision treats the locked store transaction as having no truthful mid-operation cancellation boundary. Next action: migrate the two-phase non-mutating `config cm test` adapter.
- 2026-09-08: completed `config cm test` Work-Catalog and completion-projection slice. Replaced its segmented per-phase Tracks with one immutable `Resolve CM test profile`/`Test CM provider` catalog and asserted the full catalog plus ordered phase updates. Replaced legacy Finish calls and the Rich-only response milestone with `FinishRequest` summaries: response receipt and safe finite usage enter Outcome/Transcript without response content; resolver/provider failures and cancellation retain only safe phase/category context. The request body, profile precedence, HTTP behavior, durable success/failure documents, errors, and Plain/Automation streams remain unchanged. Changed `pkg/cmd/config/cm/test/terminal.go`, `pkg/cmd/config/cm/test/terminal_run_test.go`, `pkg/cmd/config/cm/test/terminal_test.go`, and `pkg/cmd/config/cm/test/terminal_pty_test.go`. Directed verification passed: `go test ./pkg/cmd/config/cm/test ./internal/terminal ./internal/terminaltest`; repository verification passed: `make check-terminal`. Risk: terminal Track errors are now observed when the one catalog closes rather than between phases, but are still joined with the real command outcome and do not suppress its result. Next action: migrate `git heat` as the next finite read-only catalog slice.
- 2026-09-08: completed `git heat` completion-projection slice. Retained its complete immutable three-phase `Locate Git repository`/`Read Git history`/`Rank hot paths` catalog and all existing report, cancellation, process, Plain, and Automation behavior. Replaced the Rich-only ranking milestone and legacy Finish calls with a `FinishRequest`: success carries only bounded ranked-count/range or empty-result information; cancelled and failed outcomes use generic safe summaries that do not disclose repository roots, paths, queries, or raw Git errors. Changed `pkg/cmd/git/heat/terminal.go`, `pkg/cmd/git/heat/terminal_test.go`, and `pkg/cmd/git/heat/terminal_pty_test.go`. Directed verification passed: `go test ./pkg/cmd/git/heat ./internal/terminal ./internal/terminaltest`; repository verification passed: `make check-terminal`. Risk: compact PTY renders the Outcome title in its intentional bounded form (`SUCCEE…`), so full Live Outcome-title assertion is wide-only while complete Outcome summary is proven after restoration in both widths. Next action: migrate `git pulse`, including its conditional form catalog and one Work Catalog.
- 2026-09-08: STOP CONDITION reached while scoping `git pulse`; G1 is `blocked` and no `git pulse` code was changed. Evidence: `pkg/cmd/git/pulse/run.go` intentionally executes Prepare and Scan before the date form, then Fetch before the author form; `pkg/cmd/git/pulse/terminal.go` maps each `Tracker.Start` segment to a separate one-phase `ExperienceRun.Track`; `internal/terminal/experience.go` holds the run operation lock for the duration of `Track`; and `internal/terminal/rich.go` replaces Form rows when Work starts, so later `Ask` calls cannot reuse the initial catalog and fall into the legacy dynamic-row path. A correct one-Catalog, no-dynamic-form migration therefore needs a shared lifecycle extension that permits the required work/form/work sequence while preserving mutually exclusive Catalog views and ordered phase state. That change is outside G1's scoped-adapter boundary and was not part of accepted G0 contracts. Completed evidence remains the four prior G1 command slices, each with its listed directed and `make check-terminal` pass. Recovery input: an explicit shared-lifecycle contract and authorized Gate/scope for the required terminal-core behavior, including tests for Scan -> date form -> Fetch -> author form -> Build without dynamic rows or stream regressions. Next action: pause; do not begin `export env`, root/help, G2, or G3.
- 2026-09-08: blocked-state recovery audit 1. Revalidated the same `git pulse` contract conflict without changing code. Directed evidence passed: `go test ./internal/terminal ./pkg/cmd/git/pulse -run '^(TestConsoleTrackReplacesFormCatalogWithWorkCatalog|TestTerminalPulseAdapterBridgesTypedPhaseToTrackedOperation|TestRunPulsePlainJourneyKeepsPhasesOnStderrAndReportOnStdout|TestRunPulseDateCancellationUsesTheEstablishedExitZeroResult)$'`. The tests confirm both the Form-to-Work replacement rule and the existing one-phase tracker bridge while preserving Plain journey and date-cancellation output. No command-adapter-only implementation can both keep Scan -> date form -> Fetch -> author form live and satisfy a single complete Work Catalog/no dynamic form-row requirement; delaying/replaying phases or opening multiple Console runs would violate the G1/decision lifecycle and stream contracts. Blocker and recovery input are unchanged: an authorized shared lifecycle extension with direct transition coverage is required. Next action: remain paused; do not enter remaining G1 slices or later Gates.
- 2026-09-08: blocked-state recovery audit 2. Re-inspected the live Goal, `internal/terminal/experience.go`, `internal/terminal/rich.go`, `pkg/cmd/git/pulse/tracking.go`, and `pkg/cmd/git/pulse/terminal.go`; the same three facts remain authoritative: `Track` owns the operation mutex until its update channel closes, Work replaces Form rows, and every Pulse phase still starts a separate one-phase Track. `git diff --check` passed. The shared-contract requirement is therefore genuine and unrecoverable inside G1's adapter-only scope; do not implement a delayed/replayed catalog, dynamic form fallback, or multi-Console workaround. Recovery remains an explicit authorized shared lifecycle contract with direct tests for the required Scan -> date form -> Fetch -> author form -> Build transition. Next action: Goal is blocked; no further G1/G2/G3 work is authorized in this task.
- 2026-09-08: resumed as `active` under explicit authorization. Gate boundary remains G1; the only scope extension is the shared `internal/terminal` lifecycle contract and direct model/PTY coverage necessary for `git pulse` to execute `Scan -> date form -> Fetch -> author form -> Build` against one logical Work Catalog. Form and Work views must remain mutually exclusive, no dynamic Form rows may be added, and Pulse scan/selection/fetch/filtering/stream/exit/cancellation/Automation/redirected behavior must remain unchanged. `export env`, root/help, G2, and G3 remain out of scope. Next action: establish the smallest terminal-core transition contract and test it before migrating the Pulse adapter.
- 2026-09-08: completed the controlled Work-session core slice. Added the opt-in `WorkCatalog`/`WorkSession` seam in `internal/terminal`: updates are serialized per run without holding the operation mutex between calls, so a declared form can safely interrupt and return to one complete Work Catalog. Rich now retains a declared Form Catalog behind a controlled Work session and swaps the visible table atomically between Form and Work rows; legacy `Track` behavior remains unchanged. Terminal phase transcript events are committed in catalog order when each phase reaches a terminal state. Changed `internal/terminal/semantic.go`, `internal/terminal/experience.go`, `internal/terminal/work_session.go`, `internal/terminal/rich.go`, `internal/terminal/console_test.go`, and added `internal/terminal/work_session_test.go`. Directed verification passed: `go test ./internal/terminal ./internal/terminaltest`. Risk: no command uses the opt-in seam yet, so Pulse-specific form IDs, single Catalog wiring, capability behavior, and PTY journey remain to be proven. Next action: migrate only `git pulse` onto the controlled catalog and add its adapter/model/PTY regressions.
- 2026-09-08: completed the authorized `git pulse` controlled-catalog slice. Pulse now declares one immutable `Prepare workspace`/`Scan repositories`/`Fetch commits`/`Build commit tree` Work Catalog, publishes each existing typed phase to that one session, and supplies declared date/author Form IDs before `OpenConsole`; it preserves the existing Scan -> date -> Fetch -> author -> Build order without dynamic Form rows. Added a command-owned `FinishRequest` whose bounded Outcome/Transcript summary contains only safe counts or empty/cancel/failure text, never the report. Plain, Automation, redirected, empty, cancellation, failure, stdout, exit, and durable report behavior remain unchanged. Changed `pkg/cmd/git/pulse/terminal.go`, `pkg/cmd/git/pulse/terminal_test.go`, `pkg/cmd/git/pulse/terminal_pty_test.go`, and direct `internal/terminaltest` recording support. Directed verification passed: `go test -count=1 ./pkg/cmd/git/pulse ./internal/terminal ./internal/terminaltest`; Rich PTY evidence covers the full four-phase Work/Form/Work journey at `120x40` color and `40x15` with `NO_COLOR=1`, primary-screen restoration, Transcript/result separation, and no report-content leakage. Repository verification passed: `GOFLAGS=-p=1 make check-terminal`. The default parallel target twice hit distinct unrelated process-test flakes in `pkg/cmd/git/fork` and `pkg/cmd/run`; each isolated test and the serial same-target run passed. `export env`, root/help, G2, and G3 remain unmodified and out of scope. Risk: G1 cannot satisfy its remaining Gate Exit conditions while those explicitly excluded G1 slices remain unimplemented. Next action: end this scoped Goal with G1 still `active`; a new explicit authorization is required before the remaining G1 slices can run.
- 2026-09-08: completed the authorized `export env` controlled-catalog slice. The adapter now declares one immutable `Resolve directory`/`Discover environment files`/`Read selected files`/`Parse and merge values`/`Encode JSON` catalog, with `Write output file` included only for `--out`; the predeclared `Select environment` Form is the only form and alternates Work -> Form -> Work without a dynamic row. A command-owned `FinishRequest` carries only safe count/write/cancel/failure summaries while the heading, JSON, and `--out` Result bytes remain separate durable stdout. Parsing, merge precedence, selection timing, write ordering, Plain, Automation, redirected, cancellation, stdout/stderr, and exit behavior remain unchanged. Changed `pkg/cmd/export/env/terminal.go`, `pkg/cmd/export/env/terminal_test.go`, and `pkg/cmd/export/env/terminal_pty_test.go`. Directed verification passed: `go test -count=1 ./pkg/cmd/export/env` and `go test -count=1 ./internal/terminal ./internal/terminaltest`; direct model evidence proves one Work Catalog/no legacy Track and safe Finish summaries, while Rich PTY evidence covers Work/Form/Work at `120x40` color and `40x15` with `NO_COLOR=1`, primary-screen restoration, Transcript/result separation, and no dotenv-content leakage. Repository verification passed: `GOFLAGS=-p=1 make check-terminal`. Risk: no `export env` behavior regression was observed; root/help remains the final G1 regression-only slice. Next action: cover root/help version, completion, parser error, and stdout-write failure, then run `make command-surface`.
- 2026-09-08: completed the root/help regression-only slice. No production root/help behavior changed; strengthened `TestRichTerminalDiscoveryPreservesVersionAndRawCompletion` to prove Rich-capability help remains durable stdout without an AltScreen or Transcript, while version and completion stay raw. Existing direct regressions retain parser recovery's one stderr line/empty stdout and discovery write/short-write failures' one diagnostic/exit-1 behavior. Changed `pkg/cmd/root/app_test.go`. Directed verification passed: `go test -count=1 ./pkg/cmd/root -run '^(TestRichTerminalDiscoveryPreservesVersionAndRawCompletion|TestDiscoveryWriteFailureBecomesOneRootDiagnostic|TestDiscoveryShortWriteBecomesOneRootDiagnostic|TestParserRecoveryUsesOneActionableErrorLine|TestDiscoveryAndParserContractsKeepAutomationStreamsSeparated)$'` and `go test -count=1 ./pkg/cmd/root`. Repository verification passed: `make command-surface`. Risk: no root/help stream, output, parser, or AltScreen regression observed. Next action: audit all G1 Exit conditions and complete the Gate if the final repository checks pass.
- 2026-09-08: G1 passed. Exit conditions accepted with fresh evidence: (1) all scoped adapters use complete Catalogs; `git pulse` and `export env` declare every Form ID before `OpenConsole`, use one controlled Work Catalog, and have no dynamic Form rows; (2) command-local package/model/PTY suites cover success plus failed, cancelled, or empty paths as applicable; (3) Rich, Plain, Automation, redirected, Transcript, stdout/stderr, exit, cancellation, and redaction compatibility are covered in each completed slice, including the new Work/Form/Work PTYs; and (4) root/help stays durable stdout-only with version, raw completion, parser, write-failure, and no-AltScreen regressions passing. Final directed verification passed: `go test -count=1 ./pkg/cmd/export/env`, `go test -count=1 ./internal/terminal ./internal/terminaltest`, and `go test -count=1 ./pkg/cmd/root`. Final repository verification passed in declared order: `GOFLAGS=-p=1 make check-terminal`, `make command-surface`, and `git diff --check`. Manual acceptance: none. No remote push. Risk: prior default-parallel process-test flakes remain irrelevant to this final serial exit-0 target; no G1 blocker remains. Activate only G2 and end this Goal.

### G2: Mutation and archive migration

- 2026-09-08: initialized as `planned`; no implementation evidence.
- 2026-09-08: 已激活，尚未开始实施。G1 的全部 Exit conditions 已通过；本次 Goal 在此结束，G2 不在本次 Goal 的执行范围内。
- 2026-09-08: completed the `config fork add` lifecycle slice. The adapter now declares the complete five-field Form Catalog before interaction, marks the credential field sensitive, and transitions to one controlled `Collect provider details`/`Save provider instance` Work Catalog. Store construction is delayed until the form completes; safe command-owned Finish summaries identify success, cancellation, collection failure, or save failure without deriving state from the durable Result. Existing Plain success and failure streams, Automation rejection, cancellation-before-write behavior, and durable stdout Result semantics remain intact. Changed `pkg/cmd/config/fork/add/command.go`, `pkg/cmd/config/fork/add/adapter.go`, `pkg/cmd/config/fork/add/terminal_test.go`, and `pkg/cmd/config/fork/add/terminal_pty_test.go`. Directed verification passed: `go test -count=1 ./pkg/cmd/config/fork/add`; focused Rich PTYs covered 120x40/40x15, color/`NO_COLOR=1`, screen restoration, Transcript-before-Result ordering, credential redaction, save failure, writer-context cancellation, and form cancellation; focused Plain PTYs covered success, save failure, and redirected output; semantic tests covered one Work Catalog, Finish projections, Automation, and no write after cancellation. Shared directed verification passed: `go test -count=1 ./internal/terminal ./internal/terminaltest`. Risk: configuration-mutation risk-group repository checks remain pending until the remaining scoped configuration commands are complete; the Plain save-failure assertion intentionally preserves its established no-synthetic-summary stream. Next action: migrate `config fork remove` as its independent destructive confirmation/catalog/Finish-projection slice.
- 2026-09-08: completed the `config fork remove` Form-Catalog slice. The command now declares the selection and default-negative confirmation forms before `OpenConsole`, uses stable IDs on both interaction requests, and moves bounded title/subtitle context into the descriptor rather than a Rich-only introductory notice. Selection labels, persisted ordering, confirmation wording/default, reader/writer timing, and Finish behavior are unchanged. Changed `pkg/cmd/config/fork/remove/command.go`, `pkg/cmd/config/fork/remove/adapter.go`, and `pkg/cmd/config/fork/remove/terminal_test.go`. Directed verification passed: focused adapter/descriptor/cancellation/Automation tests and `go test -count=1 ./pkg/cmd/config/fork/remove`. Risk: the legacy two-Track phase stream remains temporarily in place, so the Catalog has not yet achieved its required one-session Form/Work/Form lifecycle. Next action: replace only that phase stream with one controlled Work Catalog while preserving read, confirmation, and deletion ordering.
- 2026-09-08: completed the `config fork remove` controlled Work-Catalog slice. Replaced the two legacy `Track` lifecycles with one `Load fork provider instances`/`Remove provider instance` Work Catalog that remains open across the declared selection and confirmation forms, then closes exactly once at Finish. Phase-operation errors are joined with the original reader/writer/context errors; Plain notices, Automation silence, reader/writer timing, and deletion order remain unchanged. Changed `pkg/cmd/config/fork/remove/command.go`, `pkg/cmd/config/fork/remove/terminal_test.go`, and `pkg/cmd/config/fork/remove/terminal_pty_test.go`. Directed verification passed: the model test proves one controlled catalog, ordered load/removal transitions, one session close, and no legacy `Track`; `go test -count=1 ./pkg/cmd/config/fork/remove` passed, including Rich 120x40/40x15 color/no-color restore, form/Work/Form rendering, Transcript ordering, and redaction coverage. Risk: completion still uses legacy Finish compatibility, so Outcome/Transcript summaries are not yet command-owned. Next action: add only safe `FinishRequest` projections for empty, cancelled, read/write failure, and successful removal branches.
- 2026-09-08: completed the `config fork remove` Finish-projection and branch-evidence slice. Added direct command-owned `FinishRequest` mapping coverage for successful removal, empty configuration, cancellation, load failure, removal failure, and invalid-location fallback. Added Rich PTY scenarios for empty configuration, selection cancellation, confirmation cancellation, confirmation decline, load failure, removal failure, and load/removal context cancellation; each proves phase/outcome ordering, no synthetic success, primary-screen restoration, and redaction while the existing success matrix retains 120x40/40x15 color/no-color and Transcript-before-Result evidence. Changed `pkg/cmd/config/fork/remove/terminal_test.go` and `pkg/cmd/config/fork/remove/terminal_pty_test.go`. Directed verification passed: `go test -count=1 ./pkg/cmd/config/fork/remove` and `go test -count=1 ./internal/terminal ./internal/terminaltest`. Risk: configuration-mutation risk-group checks remain pending until the remaining scoped configuration commands are complete. Next action: migrate `config cm add` as the next independent configuration mutation slice.
- 2026-09-08: completed the `config cm add` Form-Catalog slice. The command now declares the ordered `Identity`/`Endpoint`/`Model`/`Credential` form before `OpenConsole`, assigns stable IDs to all four interaction requests, and marks the API key credential sensitive. Rich title/subtitle context now lives in the descriptor instead of a redundant introductory notice; validation, Store construction timing, Plain/Automation behavior, persistence Result, and completion projection are unchanged. Changed `pkg/cmd/config/cm/add/command.go`, `pkg/cmd/config/cm/add/adapter.go`, `pkg/cmd/config/cm/add/terminal_test.go`, and `pkg/cmd/config/cm/add/terminal_pty_test.go`. Directed verification passed: `go test -count=1 ./pkg/cmd/config/cm/add` and `git diff --check`; the Rich PTY proves all four catalog rows and `[redacted]` render before the first real `Profile name` prompt. Risk: the legacy phase tracker and Finish compatibility remain temporarily in place. Next action: replace only the phase stream with one controlled `Collect CM profile details`/`Save CM profile` Work Catalog.
- 2026-09-08: completed the `config cm add` controlled Work-Catalog slice. Replaced the delayed legacy `Track` stream with one `config-cm-add-work` Work Catalog containing `Collect CM profile details` and `Save CM profile`; Rich starts it before the first question, keeps it alive through all four declared form steps, publishes ordered terminal phase updates, and closes exactly once before Finish. Store construction, encryption/write ordering, Plain notices, Automation rejection, cancellation behavior, and durable Result semantics remain unchanged. Changed `pkg/cmd/config/cm/add/command.go` and `pkg/cmd/config/cm/add/terminal_test.go`; the existing `pkg/cmd/config/cm/add/terminal_pty_test.go` continues to cover the live form and terminal Work states. Directed verification passed: `go test -count=1 ./pkg/cmd/config/cm/add`; `git diff --check` passed. The semantic sink test proves one Work Catalog, four ordered updates, one close, and no legacy `Track`; Rich PTY success proves the completed Work rows and restored Transcript/result ordering across the existing color/size matrix. Risk: completion still uses legacy Finish compatibility and lacks command-owned branch projections. Next action: add only safe `FinishRequest` projections and branch/PTy evidence for success, cancellation, store/validation failure, and writer failure.
- 2026-09-09: completed the `config cm add` Finish-projection and branch-evidence slice. All completion paths now submit a command-owned `FinishRequest`: successful saves use the bounded `Profile saved` summary; collection cancellation/failure is located at `Collect CM profile details`; and writer/save failures are located at `Save CM profile`. The durable `Profile <name> added` Result remains a separate Finish document. Changed `pkg/cmd/config/cm/add/command.go`, `pkg/cmd/config/cm/add/adapter.go`, `pkg/cmd/config/cm/add/terminal_test.go`, and `pkg/cmd/config/cm/add/terminal_pty_test.go`. Directed verification passed: `go test -count=1 ./pkg/cmd/config/cm/add`; semantic tests cover all safe mappings, command-owned Finish wiring, and validation; Rich PTY scenarios cover success, Store failure, writer failure, and form cancellation with primary-screen restoration, no synthetic success, and no API-key leakage. `git diff --check` passed. Risk: no `config cm add` branch regression observed; configuration-mutation risk-group checks remain pending. Next action: migrate `config cm set` as the next independent configuration mutation slice.
- 2026-09-09: completed the `config cm set` controlled Work-session slice. Replaced the legacy channel-based `Track` with one `update-cm-profile`/`Update CM profile` Work Catalog and one phase update pair covering the unchanged atomic store operation; the session closes exactly once before the existing completion path. Writer invocation, exact positional arguments, Plain loading diagnostic, Automation silence, result text, and failure behavior remain unchanged. Changed `pkg/cmd/config/cm/set/command.go` and `pkg/cmd/config/cm/set/terminal_test.go`. Directed verification passed: `go test -count=1 ./pkg/cmd/config/cm/set`; semantic coverage asserts the exact catalog, ordered phase states, one close, and no legacy `Track`; `git diff --check` passed. Risk: completion still uses legacy Finish compatibility and branch-specific Outcome/Transcript summaries are not yet command-owned. Next action: add only the `config cm set` FinishRequest projection and success/failure evidence.
- 2026-09-09: completed the `config cm set` Finish-projection and branch-evidence slice. All finite paths now submit a command-owned `FinishRequest`: success uses the safe generic `Profile updated` Outcome/Transcript summary with the durable `Profile <name> updated` Result kept separate, while failures use `Unable to update CM profile` and publish no Result. The atomic writer remains single-call and non-cancellable; API-key and raw storage-error text are absent from Rich Live View and replay. Changed `pkg/cmd/config/cm/set/command.go`, `pkg/cmd/config/cm/set/terminal_test.go`, and `pkg/cmd/config/cm/set/terminal_pty_test.go`. Directed verification passed: `go test -count=1 ./pkg/cmd/config/cm/set`; semantic tests cover the Catalog and safe Finish mapping; Rich PTY success covers 120x40/40x15 color/no-color and failure covers wide color plus compact `NO_COLOR=1`, with restoration, phase/outcome ordering, no synthetic success, and redaction; `git diff --check` passed. Risk: no `config cm set` regression observed; configuration-mutation risk-group checks remain pending. Next action: migrate `config cm remove` as the next independent destructive configuration slice.
- 2026-09-09: completed the `config cm remove` Form-Catalog slice. The descriptor now carries the bounded `Remove CM profile`/delete-provider context and one default-No `Confirmation` form before `OpenConsole`; the confirmation adapter supplies its stable `ConsoleStepID` and suppresses the raw boolean from the Transcript. The validation/removal phase stream and mutation ordering remain temporarily unchanged. Changed `pkg/cmd/config/cm/remove/command.go`, `pkg/cmd/config/cm/remove/adapter.go`, `pkg/cmd/config/cm/remove/terminal_test.go`, `pkg/cmd/config/cm/remove/terminal_pty_test.go`, and `pkg/cmd/config/cm/remove/terminal_pty_extended_test.go`. Directed verification passed: `go test -count=1 ./pkg/cmd/config/cm/remove`; descriptor, request, default-No, stream, and four-way Rich PTY assertions pass after updating bounded wide/narrow target observations. Risk: two legacy `Track` sessions and legacy Finish compatibility remain; no confirmation, validation, or deletion behavior changed. Next action: replace only the two phase Tracks with one controlled Work Catalog retained across confirmation.
- 2026-09-09: completed the `config cm remove` controlled Work-Catalog slice. Replaced the two legacy phase `Track` lifecycles with one `config-cm-remove-work` / `Remove CM profile` catalog containing `Validate CM profile` and `Remove CM profile`; the session starts for validation, remains open across the declared confirmation form, publishes four ordered phase updates, and closes exactly once before Finish. Validation, confirmation default/ordering, Plain notices, Automation behavior, reader/writer timing, mutation semantics, and durable Result behavior remain unchanged. Changed `pkg/cmd/config/cm/remove/command.go` and `pkg/cmd/config/cm/remove/terminal_test.go`. Directed verification passed: `go test -count=1 ./pkg/cmd/config/cm/remove` and `go test -count=1 ./internal/terminal ./internal/terminaltest`; `git diff --check` passed. The semantic sink test proves one Work Catalog, ordered validation/removal transitions, one session close, and no legacy `Track`. Risk: completion intentionally remains on the legacy Finish compatibility path until the separate safe FinishRequest slice; configuration-mutation risk-group repository checks remain pending. Next action: add only command-owned safe FinishRequest projections and success/cancellation/missing/failure branch evidence.
- 2026-09-09: completed the `config cm remove` Finish-projection and branch-evidence slice. All finite paths now submit a command-owned `FinishRequest`: successful removal uses the bounded `Profile removed` summary; confirmation cancellation/decline uses `CM profile removal cancelled`; validation, missing-profile, confirmation, and deletion failures use only safe phase summaries and bounded Locations; context cancellation during validation or confirmation remains the original error with no synthetic stdout result. The durable `Profile <name> removed` Result remains separate, and default reassignment is never projected. Changed `pkg/cmd/config/cm/remove/command.go`, `pkg/cmd/config/cm/remove/terminal_test.go`, and `pkg/cmd/config/cm/remove/terminal_pty_extended_test.go`. Directed verification passed: `go test -count=1 ./pkg/cmd/config/cm/remove` and `go test -count=1 ./internal/terminal ./internal/terminaltest`; Rich PTY scenarios cover success at 120x40/40x15 color/no-color plus confirmation cancellation/decline, validation failure/context cancellation/missing profile, deletion failure/removed=false, restoration, Transcript ordering, no synthetic success, and credential/raw-error redaction; `git diff --check` passed. Risk: no `config cm remove` branch regression observed; the configuration-mutation risk-group repository checks remain pending. Next action: run `make check-terminal`, then `make command-surface` for the completed configuration mutation group.
- 2026-09-09: completed the configuration-mutation risk-group repository verification. `make check-terminal` passed with exit 0 across shared terminal, root/help, export env, config fork/cm, all git, rm, fs, run, tunnel, upgrade, and zip terminal targets; the subsequent `make command-surface` also passed with exit 0. No configuration mutation behavior regression was observed. Risk: destructive cleanup, archive, and Git mutation groups remain unimplemented within G2. Next action: migrate only `rm` in the declared risk order, starting with its complete Work Catalog.
- 2026-09-09: completed the `rm` complete Work-Catalog slice. The explicit route now declares `Resolve explicit targets`/`Delete selected paths`; the smart route declares `Scan cleanup targets`/`Delete selected paths`. Stable IDs now bind the explicit confirmation and smart action/target forms to their complete route-specific Form Catalogs. Rich starts one controlled Work session at the first reached phase and preserves it across the declared confirmation or target-selection form; Plain and Automation retain their existing phase/output behavior. Cancellation PTY assertions now distinguish the Catalog's legitimate pending deletion row from actual mutation evidence. Changed `pkg/cmd/rm/adapter.go`, `pkg/cmd/rm/terminal.go`, `pkg/cmd/rm/adapter_test.go`, `pkg/cmd/rm/console_test.go`, `pkg/cmd/rm/terminal_pty_test.go`, and `pkg/cmd/rm/terminal_pty_extended_test.go`. Directed verification passed: `gofmt` on the changed rm files and `go test -count=1 ./pkg/cmd/rm`. Risk: rm still uses legacy Finish compatibility, so its Outcome/Transcript summary is not yet command-owned. Next action: add only safe FinishRequest projections and success/cancellation/planning/partial-failure branch evidence for rm.
- 2026-09-09: completed the `rm` Finish-projection and branch-evidence slice. Every finite rm completion now submits a command-owned `FinishRequest`; success uses route-safe `Removal complete` or `Cleanup complete`, cancellation uses `Removal cancelled`, and only stable phase names identify planning, scan, or deletion failure. The old documents remain separate durable Results, preserving Plain/Automation/stdout bytes and partial-deletion semantics. Added direct mapping and Finish/result-separation tests; Rich PTYs now prove Outcome summary precedes the Result, explicit/smart success, default-negative cancellation without mutation, 120x40/40x15 color/no-color coverage, and partial deletion's safe count/category projection without raw permission/path leakage. Changed `pkg/cmd/rm/terminal.go`, `pkg/cmd/rm/console_test.go`, `pkg/cmd/rm/terminal_pty_test.go`, and `pkg/cmd/rm/terminal_pty_extended_test.go`. Directed verification passed: focused partial-deletion PTY, `go test -count=1 ./pkg/cmd/rm`, `go test -count=1 -race ./pkg/cmd/rm`, and `git diff --check`. Risk: no rm behavior regression observed; archive and Git mutation groups remain. Next action: migrate only zip's complete Form/Work Catalog slice.
- 2026-09-09: completed the `zip` Form-Catalog slice. `OpenConsole` now declares the complete potential planning form set (`Workspace package`, `Source directory`, `File patterns`, `Output name`) before discovery or the first prompt. Each package/source/glob/output interaction carries a stable ConsoleStepID and a bounded safe Transcript projection; planning values and archive mutation inputs remain unchanged. Changed `pkg/cmd/zip/terminal.go`, `pkg/cmd/zip/adapter.go`, `pkg/cmd/zip/console_test.go`, `pkg/cmd/zip/adapter_test.go`, and `pkg/cmd/zip/terminal_pty_extended_test.go`. Directed verification passed: `go test -count=1 ./pkg/cmd/zip` and `git diff --check`. Rich PTY coverage proves the complete Catalog precedes the first wide-terminal interaction; at `40x15`, the full Catalog can clip individual prompts, so the existing helper submits stable form actions and asserts durable safe answers instead. Risk: the legacy zip phase coordinator still uses a delayed Track/replay path and has no one-session Work Catalog. Next action: replace only that coordinator with one controlled Catalog retained across planning forms and archive work.
- 2026-09-09: completed the `zip` controlled Work-Catalog slice. Replaced the delayed/replayed `Track` coordinator with one `zip-archive` / `Create archive` Work Catalog containing all eight declared archive phases. Rich starts it on the first discovery update, keeps it open across every declared planning form, publishes the existing phase updates unchanged, and closes it exactly once before the existing Finish path. Plain continues to emit the established phase Notices and Automation remains silent; archive planning, I/O ordering, result bytes, cancellation, and reveal behavior are unchanged. Changed `pkg/cmd/zip/terminal.go` and `pkg/cmd/zip/terminal_test.go`. Directed verification passed: `go test -count=1 ./pkg/cmd/zip`; the coordinator recorder proves one StartWork catalog, ordered updates spanning planning and archive work, one close, and no legacy Track call. Risk: completion intentionally still uses the legacy Finish compatibility path, so Outcome/Transcript summaries are not yet command-owned. Next action: add only zip's safe FinishRequest projections and branch/PTY evidence for success, planning cancellation, archive failure, and reveal warning.
- 2026-09-09: completed the `zip` FinishRequest-projection and branch-evidence slice. Every finite zip completion now submits a command-owned FinishRequest: success contains only collected/included counts, a safe archive basename, and the optional reveal warning; cancellation and failures use only a last successfully published declared phase plus a stable archive category. The existing `terminalZipResultDocument` remains the independent durable Result and is not reused for Outcome or Transcript. Added direct mapping and Result-separation tests, and Rich PTY branches for planning cancellation, write failure, and reveal failure after successful archive publication; the existing 120x40/40x15 colored and NO_COLOR success matrix now also proves the Outcome summary before the durable Result. Changed `pkg/cmd/zip/terminal.go`, `pkg/cmd/zip/terminal_test.go`, and `pkg/cmd/zip/terminal_pty_extended_test.go`. Directed verification passed: `go test -count=1 ./pkg/cmd/zip` and `git diff --check`. Risk: no zip side-effect, stdout, cancellation, redaction, or reveal-warning regression observed; archive-group repository verification follows in the next record.
- 2026-09-09: completed archive-group repository verification and stabilized an existing G2 `config fork add` Rich PTY field-transition race exposed by that verification. The test now waits briefly after a newly observed form marker before sending the next input, so no production command behavior changed; four repeated focused save-failure runs passed. Changed `pkg/cmd/config/fork/add/terminal_pty_test.go`. The default-parallel `make check-terminal` twice reached all G2 packages, including zip, but its unrelated `tunnel/server` FRP archive download ended with a transient GitHub EOF; the isolated tunnel test passed, and the same complete target passed serially as `GOFLAGS=-p=1 make check-terminal`. The required next archive-group check `make command-surface` passed. Risk: only the external parallel-download flake remains observed; no G2 product regression is known. Next action: migrate `git fork` as the next Git-mutation command, starting with its complete Form/Work Catalog slice.
- 2026-09-09: STOP CONDITION while verifying the `git fork` complete Form/Work-Catalog slice. The in-progress slice changes only `pkg/cmd/git/fork/terminal.go` and `pkg/cmd/git/fork/terminal_test.go`: Rich declares the overwrite form, opens one eight-phase controlled Work Catalog from `Resolve repository`, and keeps it through the overwrite interaction; Plain and Automation retain the prior Track path. `gofmt` completed and `git diff --check` passed, but `go test -count=1 ./pkg/cmd/git/fork` failed all four capability variants of the archive and fallback Rich PTYs because their Transcript omits reached phases after an optional `Replace destination` that correctly remains pending. Evidence: `internal/terminal/work_session.go` records terminal phases only until the first unreached catalog entry, while legacy `Track` records its filtered terminal snapshot; `internal/terminal/semantic.go` has no skipped terminal state; and `pkg/cmd/git/fork/run.go` intentionally sends no replacement update for an empty, missing, or unreadable destination. An adapter-only workaround would either falsely mark a destructive phase complete or replace required phase events with milestones, violating Decision 22 and the G2 scope boundary. Risk: the active `git fork` slice is unverified and leaves its package suite red. Recovery input: explicit authorization for a shared WorkSession transcript contract that permits later reached terminal phases to be recorded in catalog order while omitted conditional phases remain absent, plus direct terminal-core regression coverage; then resume only this G2 `git fork` slice. G2 remains `active`; no Gate transition, Finish-projection work, `git cm`, repository verification, or remote push was performed.
- 2026-09-09: resumed and completed the authorized `git fork` Form/Work-Catalog slice. Added the smallest shared WorkSession transcript adjustment in `internal/terminal/work_session.go`: terminal phases are scanned in declared Catalog order and each reached terminal phase is recorded once, so an omitted conditional phase no longer blocks later reached phases; `PhaseState`, transition validation, rendering, and legacy `Track` semantics remain unchanged. Added the direct regression `TestControlledWorkSessionRecordsLaterReachedPhasesAcrossOmittedCatalogEntries` in `internal/terminal/work_session_test.go`. The `git fork` adapter/test changes declare `confirm-replace-destination`, open one `git-fork-acquisition` catalog across overwrite, and retain Plain/Automation behavior. Directed verification passed: `go test -count=1 ./internal/terminal ./internal/terminaltest ./pkg/cmd/git/fork`; this includes archive, fallback, overwrite decline/cancel, four capability-width/color PTY journeys, and semantic no-legacy-Track assertions. `git diff --check` passed. Risk: shared-contract and Git-mutation repository checks remain pending; no `FinishRequest` projection or `git cm` work was started. Next action: add only `git fork` command-owned safe FinishRequest projections and branch evidence.
- 2026-09-09: completed the `git fork` command-owned FinishRequest projection slice. All config/setup, module failure, context cancellation, overwrite cancellation, and success paths now submit a bounded `FinishRequest`; the request maps only allowlisted phase names and conservative partial-disk facts, uses safe repository/ref/destination projections, and never copies credentials, URLs, absolute paths, raw errors, or the durable Result. Successful and prompt-cancelled branches retain their existing durable Result as a separate document; failed and signal branches submit no success-looking Result. Added semantic mapping coverage for repository, destination replacement, default-branch, archive, clone, metadata-removal, cancellation, partial-disk, redaction, and Result separation. Changed `pkg/cmd/git/fork/terminal.go` and `pkg/cmd/git/fork/terminal_test.go`. Directed verification passed: `go test -count=1 ./pkg/cmd/git/fork`; focused Finish projection tests passed first after the intended compile failure. Risk: Rich PTY assertions still need direct proof of the new Live Outcome/failure ordering; Git-mutation repository checks remain pending. Next action: extend only `git fork` Rich PTY assertions for Outcome-before-Result, failed/cancelled non-success output, and redaction.
- 2026-09-09: completed the `git fork` Rich PTY evidence slice. The PTY matrix now proves archive and clone-fallback success across `120x40`/`40x15` color and `NO_COLOR=1`, overwrite decline/cancel across the same four modes, and archive-plus-clone failure in wide and compact layouts. Wide assertions cover the Live Outcome; compact assertions cover the active failed/confirmation phase and the restored Transcript Outcome, ordered phase replay, redaction, primary-screen restoration, and absence of success-looking Result output. The assertion adjustment is test-only and reflects compact Live View clipping; no command behavior changed. Changed `pkg/cmd/git/fork/terminal_pty_extended_test.go`. Directed verification passed: `go test -count=1 ./pkg/cmd/git/fork`, `go test -count=1 ./internal/terminal ./internal/terminaltest`, and the focused Rich PTY matrix. `git diff --check` passed. Risk: no `git fork` regression remains known; the Git-mutation repository checks remain pending until `git cm` is complete. Next action: migrate only `git cm` within G2, then run the Git-mutation risk-group repository checks.
- 2026-09-09: completed the `git cm` descriptor and interaction-contract slice. The applicable Rich Form Catalog is now declared before interaction (`Select files to stage` when staging is requested, followed by `Commit confirmation` when a commit will be created); generation-only modes expose no form rows. Both requests carry stable ConsoleStepIDs, safe answer labels, and a count/decision Transcript projection that never copies selected paths or generated content. Changed `pkg/cmd/git/cm/terminal.go` and `pkg/cmd/git/cm/terminal_test.go`. Directed verification passed: `go test -count=1 ./pkg/cmd/git/cm`, including the existing Plain, Automation, side-effect, timeout, signal, and result tests plus the new descriptor/projection assertions. Risk: Rich still uses the pre-migration segmented legacy Track path; no phase or output behavior was changed in this slice. Next action: replace only the Rich segmented tracker with one controlled nine-phase Work Catalog retained across the declared forms.
- 2026-09-09: completed the `git cm` controlled Work-Catalog slice. Rich now lazily starts one `git-cm-workflow` Catalog containing all nine declared phases, retains it across file selection and commit confirmation, and closes it once after the command; module `Start`/`Close` compatibility reporters no longer open segmented Rich `Track` lifecycles. Plain and Automation continue using their existing compatibility tracking path. Changed `pkg/cmd/git/cm/terminal.go`, `pkg/cmd/git/cm/tracking.go`, and `pkg/cmd/git/cm/terminal_test.go`. Directed verification passed: `go test -count=1 ./pkg/cmd/git/cm`, including Rich PTY success journeys and Plain/Automation/side-effect tests; the semantic sink proves one complete `StartWork`, ordered updates, one close, and zero `Track` calls. Risk: completion still uses legacy Finish compatibility and branch-specific Outcome/Transcript summaries are not yet command-owned. Next action: add only `git cm` FinishRequest mappings and branch evidence while keeping durable Result documents separate.
- 2026-09-09: completed the `git cm` FinishRequest safety/fact slice. All adapter completion calls now carry bounded command-owned summaries while the optional durable Result remains a separate document; successful staging is tracked locally so later generation/commit failures and commit-confirmation cancellation record `Staged changes retained`, and mixed cancellation/operation errors remain failures rather than being swallowed. Changed `pkg/cmd/git/cm/terminal.go` and `pkg/cmd/git/cm/terminal_test.go`. Directed verification passed: `gofmt -w pkg/cmd/git/cm/terminal.go pkg/cmd/git/cm/terminal_test.go` and `go test -count=1 ./pkg/cmd/git/cm`; direct tests cover safe location/summary mappings, Result separation, retained staging facts, pure versus mixed cancellation, and no credential/URL/generated-message leakage. Risk: command behavior branches still need direct Rich PTY evidence for provider, stale-scope, hook/commit, and push-partial outcomes; no shared lifecycle or durable output contract changed. Next action: add only those `git cm` branch and capability-mode PTY assertions.
- 2026-09-09: completed the `git cm` branch/capability evidence slice. Plain tests cover confirmation decline/cancel without HEAD or index mutation, provider failure, stale scope, and hook rejection with safe streams; Rich PTY scenarios cover provider failure, stale scope, hook failure, commit cancellation, and push failure across wide/compact colored and `NO_COLOR=1` cases, with primary-screen restoration, ordered Transcript phases, safe Outcome summaries, no synthetic success, partial-commit preservation, and redaction. Changed `pkg/cmd/git/cm/terminal_test.go` and `pkg/cmd/git/cm/terminal_pty_extended_test.go`. Directed verification passed: `gofmt -w pkg/cmd/git/cm/terminal_pty_extended_test.go`, focused Rich failure/cancellation PTYs, and `go test -count=1 ./pkg/cmd/git/cm`. Risk: the remaining G2 evidence is redirected-stream coverage plus the Git-mutation risk-group repository checks; no command or shared lifecycle behavior outside `git cm` changed. Next action: close the redirected/Automation/Result-separation evidence gap, then run the risk-group checks in plan order.
- 2026-09-09: completed the `git cm` redirected/Result-separation evidence slice. A classified redirected-stream generation-only run keeps the complete generated Result on stdout, leaves stderr empty, emits no terminal controls, and performs no interaction; combined with the semantic Finish recorder and Rich Transcript assertions, this proves Summary/Transcript and durable Result remain separate across Rich, Plain, Automation, and redirected modes. Changed `pkg/cmd/git/cm/terminal_test.go`. Directed verification passed: `go test -count=1 ./pkg/cmd/git/cm`; no output, interaction, mutation, or redaction regression was observed. Risk: all scoped command slices are implemented; the Git-mutation risk-group repository checks and final G2 acceptance evidence remain pending. Next action: run `GOFLAGS=-p=1 make check-terminal`, then `make command-surface`.
- 2026-09-09: repository verification attempt for the Git-mutation group reached all scoped targets and passed through `pkg/cmd/tunnel/connect`, but `GOFLAGS=-p=1 make check-terminal` failed in the existing `tunnel/server` forwarding integration because the client desired state remained `applied 0` after 20 seconds; `upgrade` and `zip` completed afterward. No `git cm` or other product code changed in this attempt. Risk: repository Exit evidence is incomplete due to an external/process-test failure; G2 remains `active`. Next action: isolate and rerun the failing `tunnel/server` target, then rerun the complete serial check if needed before `make command-surface`.
- 2026-09-09: isolated rerun of the same existing `tunnel/server` target also failed with `client desired-state application did not succeed within 20s: client revisions = desired 3, applied 0` under `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 ./pkg/cmd/tunnel/server`. No tunnel/server code or G2 command code changed. Risk: the repository Exit evidence remains incomplete and the failure is outside G2's scoped mutation/archive adapters; G2 remains `active`. Next action: perform read-only diagnosis of the FRP desired-state test and make one safe retry of the complete serial target if the environment issue is transient.
- 2026-09-09: the red-capable isolated rerun passed on the next attempt: `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 -v ./pkg/cmd/tunnel/server -run '^TestGoClientToGoServerForwardsHTTPAndTCPAndUDPWithPinnedFRP$'` completed in 7.41s with exit 0. The test's desired-state and forwarding assertions therefore pass against the current worktree; no tunnel/server or G2 product code changed. Risk: the prior repository failure is established as transient/non-deterministic, but complete Gate evidence is still pending. Next action: retry `GOFLAGS=-p=1 make check-terminal`, then run the remaining G2 repository checks in plan order.
- 2026-09-09: the safe serial retry `GOFLAGS=-p=1 make check-terminal` passed with exit 0. All shared, root/help, configuration, Git, destructive cleanup, archive, tunnel, upgrade, and zip terminal targets passed; `pkg/cmd/tunnel/server` passed in 56.186s after the prior transient desired-state failure. No tunnel/server or unrelated product code changed. Risk: the G2 repository check is now green; command-surface, relevant acceptance tests, and final diff hygiene remain pending. Next action: run `make command-surface`, then the G2 acceptance set, then `git diff --check` last.
- 2026-09-09: the required post-risk-group `make command-surface` check passed with exit 0 (`pkg/cmd/root`, 0.890s). The command surface remains unchanged after all G2 adapter migrations. Risk: no surface regression observed; Gate acceptance evidence is still pending. Next action: run the relevant G2 command/terminal acceptance suites, then perform the final `git diff --check`.
- 2026-09-09: the relevant tagged acceptance suite `make acceptance` passed with exit 0: `acceptance` completed in 82.170s and `acceptance/web` in 11.661s. This includes standalone config mutation, Git fork/CM, rm, zip, redaction, side-effect, signal, and PTY journeys; no acceptance fixture or product code changed in this slice. Risk: none observed in the acceptance set; final diff hygiene and itemized G2 Exit evidence remain pending. Next action: run `git diff --check` last, then record the Gate decision.
- 2026-09-09: G2 passed. Itemized Exit evidence: (1) all nine scoped mutation/archive adapters (`config fork add/remove`, `config cm add/set/remove`, `rm`, `zip`, `git fork`, and `git cm`) use one complete Work Catalog and command-owned bounded Finish summary, with the durable Result kept independent; (2) existing confirmation defaults, cancellation, partial-failure/rollback behavior, mutation ordering, side effects, output streams, and failure/success result semantics are covered by the command package suites, Rich PTY matrices, and tagged acceptance journeys; (3) prototype-style Transcript phase ordering, primary-screen restoration, capability degradation, control-free redirected/Automation behavior, and redaction are proven by the direct terminal/command tests and the acceptance set. Final repository verification passed in declared order: `GOFLAGS=-p=1 make check-terminal` exit 0 (including `pkg/cmd/tunnel/server` after the transient isolated failure), `make command-surface` exit 0, `make acceptance` exit 0 (`acceptance` 82.170s and `acceptance/web` 11.661s), and `git diff --check` exit 0. Manual acceptance: none. No remote push. Direct successor G3 is now `active` with the record `activated on 2026-09-09 after G2 passed; not started`; this Goal ends without inspecting, implementing, or verifying G3.

### G3: Handoff boundaries and release acceptance

- 2026-09-08: initialized as `planned`; no implementation evidence.
- 2026-09-09: completed the `run` handoff Work-Catalog slice. Rich/ detailed
  execution now declares one four-phase `run-handoff` Work Catalog and uses a
  stable `script-selection`/`package-manager-selection` Form Catalog instead
  of opening one legacy `Track` per phase. The release wrapper closes the
  parent lifecycle before invoking the child; child argv, inherited streams,
  working directory, exit mapping, cancellation result, and Plain/Automation
  boundaries remain unchanged. Command-owned FinishRequest summaries remain
  separate from the optional cancellation Result. Changed
  `pkg/cmd/run/adapter.go`, `pkg/cmd/run/tracking.go`, and
  `pkg/cmd/run/adapter_test.go`. Directed verification passed:
  `go test -count=1 ./pkg/cmd/run ./internal/terminal ./internal/terminaltest`
  and `git diff --check`; the existing four-way handoff/child-stream PTYs and
  the new semantic one-Catalog/no-legacy-Track test passed. Risk: the shared
  Rich compatibility cleanup and remaining G3 boundaries are still pending;
  no child-process or command-result regression was observed. Next action:
  migrate only the `upgrade` transaction boundary to one Work Catalog.
- 2026-09-09: completed the `upgrade transaction boundary` slice. The nine updater phases now share one `upgrade-release` Work Catalog and one WorkSession, with one close and no legacy `Track`; startup transaction, detached updater, cancellation, classified abort, already-current, and scheduled behavior remain in the existing updater flow. Command-owned bounded FinishRequest summaries remain separate from the durable Result document. Changed `pkg/cmd/upgrade/terminal.go`, `pkg/cmd/upgrade/command.go`, and `pkg/cmd/upgrade/terminal_test.go`. Directed verification passed: `go test -count=1 ./internal/updater ./pkg/cmd/upgrade ./internal/terminal ./internal/terminaltest` and `git diff --check`; semantic coverage proves one StartWork, eighteen phase updates, one close, zero legacy Track, and Finish/Result separation. Risk: tunnel selection and Service Command compatibility cleanup remain pending; no updater transaction or output regression was observed. Next action: migrate only the `tunnel connect` selection-to-Lifecycle-Log boundary.
- 2026-09-09: completed the `tunnel connect` selection-to-Lifecycle-Log slice. The selector declares one complete `tunnel-connection-selection` Form Catalog and uses its `ConsoleStepID`; after selection, the temporary Console is explicitly closed and any selection/close errors are joined before the continuing `tunnel.client` Lifecycle Log starts. `RunClient` protocol, reconnect behavior, output, and exit semantics remain unchanged. Changed `pkg/cmd/tunnel/connect/terminal.go` and `pkg/cmd/tunnel/connect/terminal_test.go`. Directed verification passed: `go test -count=1 ./pkg/cmd/tunnel/connect ./internal/terminal ./internal/terminaltest`; `git diff --check` passed. Tests cover the declarative form, cancellation/safe projection, and that client lifecycle logging starts only after selector closure. Risk: no selection or service-boundary regression observed; the final no-Console service-command regression and migration compatibility cleanup remain. Next action: test and clean only `diff`, `fs`, and `tunnel server` service boundaries.
- 2026-09-09: completed the `tunnel server` integration baseline slice. The isolated FRP forwarding test `GOTOOLCHAIN=go1.26.7 GOWORK=off CGO_ENABLED=0 go test -count=1 -v ./pkg/cmd/tunnel/server -run '^TestGoClientToGoServerForwardsHTTPAndTCPAndUDPWithPinnedFRP$'` passed (12.765s), including desired-state application and HTTP/TCP/UDP forwarding. No product code changed in this verification-only slice. Risk: direct no-Console/Lifecycle Log, NDJSON, shutdown, stream, and redaction evidence remains pending. Next action: add focused `tunnel server` service-boundary tests, then remove only unreferenced migration compatibility paths.
- 2026-09-09: completed the `tunnel server` Lifecycle-Log service-boundary slice. Added a Rich-capability integration regression that runs the real control listener through cancellation while routing diagnostics through the terminal runtime: it proves no stdout Command Result, no terminal control or AltScreen sequence, valid ANSI-free NDJSON, ordered `server.starting`/`state.opened`/`control.listening`/`server.started` startup, one cancellation shutdown, and terminal `server.stopped`; it also proves the admin password, FRP token, and state path stay out of the Lifecycle Log. Changed `pkg/cmd/tunnel/server/run_test.go` only; no production behavior changed. Directed verification passed: `go test -count=1 ./pkg/cmd/tunnel/server ./internal/terminal ./internal/terminaltest` (server 53.008s, terminal 11.261s, terminaltest 0.726s). Risk: focused `diff` and `fs` no-Console/Lifecycle-Log evidence and migration-compatibility cleanup remain pending; no `tunnel server` service-boundary regression is known. Next action: add only the `diff` Service Command boundary regression before proceeding to `fs`.
- 2026-09-09: completed the `diff` Service-Command boundary slice. Added a real `runDiff` Rich-capability regression that accepts the unchanged startup Result, cancels the foreground service, and verifies the resolved baseline/target projection while proving no AltScreen sequence, no terminal control in NDJSON diagnostics, `diff` scope throughout, ordered startup Lifecycle records, and `Directory diff stopping`/`Directory diff stopped` as the shutdown tail. Changed `pkg/cmd/diff/leaf_run_test.go` only; no production behavior changed. The first new-test attempt compared unresolved temporary paths and failed on the established macOS symlink normalization; correcting the assertion to the existing resolved-path contract produced a clean pass. Directed verification passed: `go test -count=1 ./pkg/cmd/diff ./internal/terminal ./internal/terminaltest` (diff 1.860s, terminal 11.150s, terminaltest 1.836s). Risk: focused `fs` boundary evidence and migration-compatibility cleanup remain pending; no `diff` stream, shutdown, or service-UI regression is known. Next action: add only the `fs` Service Command boundary regression.
- 2026-09-09: completed the `fs` Service-Command boundary slice. Added a real Rich `runFS` regression that retains the existing startup and stopped stdout checkpoints, cancels after the startup checkpoint, and proves the two checkpoints complete, no AltScreen sequence appears, NDJSON diagnostics contain no terminal control, all Lifecycle records retain `fs` scope, the four startup records remain ordered, and `File Browser stopping`/`File Browser stopped` remain the shutdown tail. Changed `pkg/cmd/fs/terminal_test.go` only; no production behavior changed. The first new-test attempt expected a newline immediately after a Rich title, but the established fixed-width renderer pads that line; narrowing the assertion to semantic title/checkpoint presence produced a clean pass. Directed verification passed: `go test -count=1 ./pkg/cmd/fs ./internal/terminal ./internal/terminaltest` (fs 9.367s, terminal 10.792s, terminaltest 1.040s). Risk: migration-only dynamic-Catalog compatibility cleanup and the Gate-wide verification/acceptance evidence remain pending; no `fs` stream, shutdown, or service-UI regression is known. Next action: identify one unreferenced migration compatibility path and remove it with direct coverage.
- 2026-09-09: completed the unreferenced legacy-introduction cleanup slice. Removed `rich.go`'s migration-only Notice/Milestone parser that guessed a default Console heading and metadata from a three-block legacy document. All production finite adapters already pass an explicit `ConsoleDescriptor`, while Service Commands use no Console; ordinary bounded Notice rendering remains unchanged. Changed `internal/terminal/rich.go` only. Directed verification passed: `go test -count=1 ./internal/terminal ./internal/terminaltest` (terminal 11.632s, terminaltest 1.709s), followed by an empty symbol scan for `legacyIntro`, `legacyConsoleIntro`, `captureLegacyIntro`, `legacyIntroField`, and `usingDefaultConsoleDescriptor`. Risk: the remaining undeclared-form append path must be removed without changing declared Catalog, Plain fallback, or WorkSession behavior. Next action: reject undeclared Rich Console forms and remove the dynamic-row fallback with direct terminal regression coverage.
- 2026-09-09: completed the undeclared Rich Form-Catalog cleanup slice. Rich `Ask` now requires a nonempty declared `ConsoleStepID` before it hands an interaction to the renderer; an unknown or omitted ID returns `ErrUndeclaredConsoleForm`, while a pre-start Rich failure still degrades to the established Plain flow. The renderer no longer appends a dynamic status/form row for an unknown ID; only normalized `ConsoleDescriptor.FormCatalog` rows can exist. Migrated generic Rich PTY/recovery test helpers to explicit test Console descriptors and stable IDs, preserving long-list navigation, cancellation, redirected stdout/stderr, and renderer-recovery coverage. Changed `internal/terminal/experience.go`, `internal/terminal/rich.go`, `internal/terminal/console_test.go`, `internal/terminal/interaction_test.go`, `internal/terminal/rich_recovery_test.go`, and `internal/terminal/rich_runtime_pty_test.go`. Directed verification passed: focused long-list and redirected Rich PTYs, then `go test -count=1 ./internal/terminal ./internal/terminaltest` (terminal 10.670s, terminaltest 0.747s). `git diff --check` passed. Symbol scan found no legacy-introduction symbols; the only form/status row append remains initial Catalog construction or Work Catalog synchronization. Risk: Gate-wide repository, acceptance, cross-build, and visual-reference evidence remains pending; no finite-adapter or Plain-fallback regression is known. Next action: audit stale active recovery evidence and then run the declared G3 repository verification sequence.
- 2026-09-09: G3 automated verification and manual-acceptance handoff. The active control plane contains no executable stale recovery-plan/G0-evidence reference; superseded history remains untouched as required. Repository verification passed in declared order: `make check-terminal`; `make acceptance-terminal` (`acceptance` 80.845s, `acceptance/web` 13.965s); `make acceptance` (`acceptance` 77.580s, `acceptance/web` 7.481s); `make command-surface`; `make check` (only the non-failing Vite chunk-size advisory); and `make cross-build`. Final hygiene `git diff --check` passed. A colored `make prototype-terminal` default `console/success` environment is running in the current Codex terminal (session `68894`); after it exits, use `NO_COLOR=1 make prototype-terminal` for the no-color pass, and use `F4` plus `F5` to restart the failure/cancel scenarios. Minimal manual acceptance: inspect the complete first Form Catalog; complete the form and inspect Form-to-Work replacement; inspect successful, failed, and cancelled Outcomes with the dwell and primary-screen restoration at `120x40` and `40x15`; inspect restored stderr `ANSWERS`, `WORK`, optional `AT`, `OUTCOME` before stdout Result; inspect the existing `run` handoff and confirm Service Commands never enter AltScreen. Reply exactly `通过: <场景列表>` or `未通过: <场景及证据>`. Next action: a new Goal may consume only that explicit reply; this Gate remains `active` pending manual acceptance.
- 2026-09-09: G3 passed. Explicit manual acceptance `通过` was received for the handoff environment. Itemized Exit evidence: (1) finite Rich command Catalog, Finish summary, Outcome dwell, and Transcript behavior is covered by the terminal suite and the scoped adapter migrations; (2) `run`, `upgrade`, `tunnel connect`, `diff`, `fs`, and `tunnel server` boundary evidence passed in their directed and repository suites; (3) dynamic-Catalog compatibility was removed and active control-plane audit found no executable stale recovery evidence; and (4) the declared terminal, acceptance, command-surface, complete-check, cross-build, PTY/stream, visual-reference, and manual-acceptance requirements passed. No direct successor exists. The terminal-experience modernization effort is complete; no remote push was performed.

## Post-G3 Meter Parity Correction

- 2026-09-09: a post-G3 prototype comparison found that production `richTrackMode` rendered static phase text while `internal/terminal/prototype-vivid` rendered the B Ops Console `spinner.Meter`. Added one Rich-only Bubbles v2 Meter model: normal Track and controlled Work starts reset and tick it; controlled Work resumes reset it after a Form; Form, Outcome, and stale-ID paths ignore late ticks. The active region now shows the Meter beside the current phase without adding a percentage, ETA, semantic field, Transcript entry, stdout output, Plain/Automation behavior, or Service Command UI. Added model regressions for first frame, tick advance, reset, stale ticks, Outcome rejection, and controlled Work/Form resumption; expanded the existing `120x40`/`70x20`/`40x15` colored and `NO_COLOR=1` Rich PTY matrix to capture a live Meter frame and prove it is absent after primary-screen restoration; extended redirected-stdout PTY coverage so the Meter remains only on Rich stderr. Directed verification passed: `GOTOOLCHAIN=local GOWORK=off CGO_ENABLED=0 go test -count=1 ./internal/terminal ./internal/terminaltest` and `cd internal/terminal/prototype-vivid && GOTOOLCHAIN=local GOWORK=off CGO_ENABLED=0 go test -count=1 ./...`; repository verification passed: `make check-terminal`; final `git diff --check` passed. This is a visual-parity correction only: G3 history and the resolved `map.md` remain unchanged.
