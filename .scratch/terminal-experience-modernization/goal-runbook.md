# B Ops Console Lifecycle Refactor Goal Runbook

## Source Baseline

| Path | SHA-256 |
| --- | --- |
| `.scratch/terminal-experience-modernization/implementation-plan.md` | `7622c8520c732988bf240d866ae91a599c5e7c42d7603eb3ac44890f9119d56a` |
| `.scratch/terminal-experience-modernization/issues/05-prototype-vivid-terminal-visual-system.md` | `adfdf81af89332ba66f04bb71f6c7555152afb182fcdf9529e623032908a92d2` |
| `.scratch/terminal-experience-modernization/issues/06-choose-shared-terminal-experience-contract.md` | `34166d3fff0e6015fa97028fe3d175fbc80c858fb71498c1d0c472416189fc71` |
| `.scratch/terminal-experience-modernization/issues/07-choose-root-help-and-error-presentation.md` | `e28081d4337a6986e078762dc94b1f3faeb45d2687e03e7ee74fc62cd91e0356` |
| `.scratch/terminal-experience-modernization/issues/08-choose-export-env-presentation.md` | `20596a71b236eaabdeecda48321b87ad7900628b8456bb6a9b43e54d248d9f4e` |
| `.scratch/terminal-experience-modernization/issues/09-choose-config-fork-list-presentation.md` | `516d45d5a80fb0a1a733ccbaee3fd95fa6a0b21916441a181ed20294b79ac088` |
| `.scratch/terminal-experience-modernization/issues/10-choose-config-fork-add-presentation.md` | `7f5039d12cc2377f366ab2ee551a023eafa1126db1876cdd690ac6813bf06620` |
| `.scratch/terminal-experience-modernization/issues/11-choose-config-fork-remove-presentation.md` | `d02eb7411d8562f9702d9de6446966f0846652bc03679cced381608635298903` |
| `.scratch/terminal-experience-modernization/issues/12-choose-config-cm-list-presentation.md` | `5b17fc8d17043ddf9908af4c38322a2d3d742d31f7fe528559461731bc10811a` |
| `.scratch/terminal-experience-modernization/issues/13-choose-config-cm-add-presentation.md` | `b85343d999c75325a58f1d0e481bea9eac5688caf855559c0308a84dbb2c4395` |
| `.scratch/terminal-experience-modernization/issues/14-choose-config-cm-use-presentation.md` | `9c92c7d7c612b1d15fce2170f88ebfc1f4be3e4954f6e4db31523c51fd088fdb` |
| `.scratch/terminal-experience-modernization/issues/15-choose-config-cm-set-presentation.md` | `fadd58c91e2da00a734cf0a6ca7a23689f2aac544e5d6ff2fcfdad0bba6ac346` |
| `.scratch/terminal-experience-modernization/issues/16-choose-config-cm-remove-presentation.md` | `8c6f25fbdc961e5b79e8cf667144edb48dd78fee07f81bcedf5e014faf5053d6` |
| `.scratch/terminal-experience-modernization/issues/17-choose-config-cm-test-presentation.md` | `1ea070f25683553f651f317d87c62a1cec36b51461e0df6c9456f933ecf2cf5f` |
| `.scratch/terminal-experience-modernization/issues/18-choose-diff-lifecycle-log-presentation.md` | `f931611f45ae8b88592a3c12473c7ba6bac5093cf4d24a95356b6041c9a84e07` |
| `.scratch/terminal-experience-modernization/issues/19-choose-fs-lifecycle-log-presentation.md` | `6860f2f9c22909a533297af0a46e25c00e74d9c776c1c9fbd9609f1332ff6a8e` |
| `.scratch/terminal-experience-modernization/issues/20-choose-git-heat-presentation.md` | `b66cac29c9724362ca5e089fcc4b3fd8d795044d148d9ea1ca5a99e8fe36597b` |
| `.scratch/terminal-experience-modernization/issues/21-choose-git-pulse-presentation.md` | `74ad403f2e1d55d35849b7b5db9e697cac7d59cab99da8759a689b18de611db0` |
| `.scratch/terminal-experience-modernization/issues/22-choose-git-fork-presentation.md` | `b10ceb31876d8b9bdd4b4207a4d2e155b8f1e91a54892b0434265ec3043faa5d` |
| `.scratch/terminal-experience-modernization/issues/23-choose-git-cm-presentation.md` | `a2e9f8a81a2020dfea451f39c75a65f2fab1a63d8602d5687bd7166cc4333ee6` |
| `.scratch/terminal-experience-modernization/issues/24-choose-rm-presentation.md` | `1b656baf42bfc31de8c99878d923d90a5a21618732dd5f0e0821e5107c86caab` |
| `.scratch/terminal-experience-modernization/issues/25-choose-run-handoff-presentation.md` | `59f38d0c098ae525e3c8260edff7aa2414f91625536f8749ee02942530dc1e1a` |
| `.scratch/terminal-experience-modernization/issues/26-choose-tunnel-connect-lifecycle-log-presentation.md` | `e69927f9fc012de0e4ec96e7a606821d69ee2cf188d6c50449a2ce70a095ab8a` |
| `.scratch/terminal-experience-modernization/issues/27-choose-tunnel-server-lifecycle-log-presentation.md` | `89279bc77d21c8ceb78a234fbb438b0576358ac70c253b87b846b4ba90c63cf5` |
| `.scratch/terminal-experience-modernization/issues/28-choose-zip-presentation.md` | `e5ec30e94d8abc9050d43ab968f77cf4f01567f631c4d6664d25a58c0e45b560` |
| `.scratch/terminal-experience-modernization/issues/29-choose-upgrade-presentation.md` | `867722b759822bad286e9ab988e13160ae15cc5131beb78480a001da1ec57e4c` |
| `.scratch/terminal-experience-modernization/issues/30-approve-rollout-and-acceptance-plan.md` | `73b9ee80a04f158f8eaeecb8b23e8d7839f2f68b145568ec8e96fb95cd76203d` |
| `.scratch/terminal-experience-modernization/map.md` | `65b036d823e06f51b097769a46548d9a5f83938ce13e6f089ae0b09503cccd9d` |
| `CONTEXT.md` | `a3400217b0e5dfaf39dbb729c2a0fb6ae38d6fd3e1eab24d38e4a76bb55c6da0` |

## State Rules

- `implementation-plan.md` is the sole source of Gate contracts; this runbook records only state and evidence.
- Execute only the one `active` Gate in the Goal Ledger at a time.
- Append slice, changed files, verification, risks, and next action to that Gate's Progress Log each turn.
- `passed` requires explicit evidence for every plan Exit condition; ordinary implementation or failed verification remains `active`.
- Use `blocked` only for a plan Stop condition and record the blocker and recovery condition.
- Pending manual acceptance ends a Goal through a one-time handoff log entry; it is not `blocked`, and the Gate remains `active`.
- Passing a Gate activates only its direct successor and ends the current Goal. The successor is executed only by a new Goal.

## Goal Ledger

| Gate | Status | Depends on | Plan contract | Unlock evidence |
| --- | --- | --- | --- | --- |
| G0: Shared B lifecycle foundation | passed | none | `implementation-plan.md` -> `G0: Shared B lifecycle foundation` | all seven G0 Exit conditions accepted on 2026-09-08 |
| G1: Read-only and selection migration | passed | G0 | `implementation-plan.md` -> `G1: Read-only and selection migration` | all four G1 Exit conditions accepted on 2026-09-08 |
| G2: Mutation and archive migration | active | G1 | `implementation-plan.md` -> `G2: Mutation and archive migration` | activated on 2026-09-08 after G1 passed; not started |
| G3: Handoff boundaries and release acceptance | planned | G2 | `implementation-plan.md` -> `G3: Handoff boundaries and release acceptance` | G2 pending |

## Progress Log

### G0: Shared B lifecycle foundation

- 2026-09-08: initialized as `active` after the lifecycle control-plane reset. No implementation or G0 evidence is imported; prior generic-renderer audit and `g0-*` artifacts are superseded. Next action: establish the semantic Catalog, `FinishRequest`, eager renderer, replacement, Outcome, and structured Transcript slices from the new plan.
- 2026-09-08: completed semantic Catalog/FinishRequest slice. Added bounded `ConsoleFormStep` and `ConsoleDescriptor.FormCatalog` normalization (field bounds, control stripping, duplicate IDs, 64-step cap), `InteractionRequest.ConsoleStepID`, and FinishRequest validation. `ExperienceRun.Finish` accepts the new request while dispatching the existing outcome/document call shape for unmigrated adapters; the runtime and semantic recorders retain at-most-once behavior. Changed `internal/terminal/semantic.go`, `internal/terminal/console.go`, `internal/terminal/experience.go`, `internal/terminaltest/semantic_recording.go`, `pkg/cmd/zip/terminal_test.go`, and added `internal/terminal/semantic_catalog_test.go`. Directed verification: `go test ./internal/terminal ./internal/terminaltest` passed. Risk: the compatibility dispatch remains temporarily broad until command adapters migrate in later Gates; no command business logic changed. Next action: make Rich startup eager for usable terminals and render the complete normalized Form Catalog before the first Ask.
- 2026-09-08: completed eager Rich and initial Form Catalog slice. `OpenConsole` now preflights and starts Rich before semantic interaction when the terminal is usable, with pre-AltScreen startup failure falling back to Plain; the initial normalized Catalog is rendered in order with the first row `ACTIVE`, remaining rows `PENDING`, and sensitive detail `[redacted]`, while catalog-backed Ask updates reuse existing rows. Changed `internal/terminal/experience.go`, `internal/terminal/rich.go`, `internal/terminal/console.go`, `internal/terminal/console_test.go`, and added/updated `internal/terminal/rich_eager_test.go`. Directed verification: `go test ./internal/terminal ./internal/terminaltest` and `cd internal/terminal/prototype-vivid && go test ./...` passed; PTY coverage asserts Catalog and AltScreen output before any Ask. Risk: legacy adapters without `ConsoleStepID` retain append compatibility until later command migration; no command business logic changed. Next action: replace the complete Form Catalog with one Work Catalog when tracking starts, retaining ordered rows and preventing form/work mixing.
- 2026-09-08: completed Form-to-Work replacement slice. `richStartTrackMsg` now clears the complete Form Catalog and starts the Work Catalog at table row zero, so form and work rows cannot mix; final Work rows continue to be retained when the active region changes. Changed `internal/terminal/rich.go` and `internal/terminal/console_test.go`. Directed verification: `go test ./internal/terminal ./internal/terminaltest`, repository package tests via `go test ./...`, and `cd internal/terminal/prototype-vivid && go test ./...` passed. Risk: legacy append behavior remains only for adapters without a Form Catalog before Work starts; no command behavior changed. Next action: add the Outcome view and exact 800 ms dwell with cancellation/renderer-termination interruption coverage.
- 2026-09-08: completed Outcome state and dwell slice already present in the working tree. Rich now retains final Work rows, renders bounded Outcome status/location/summary in AltScreen, ignores late form/work/notice/milestone messages, and exits through the exact `800*time.Millisecond` model message with context/renderer interruption paths. Changed `internal/terminal/experience.go`, `internal/terminal/rich.go`, `internal/terminal/console.go`, and `internal/terminal/console_test.go`. Directed verification: `go test ./internal/terminal ./internal/terminaltest` and `cd internal/terminal/prototype-vivid && go test ./...` passed; targeted Finish PTY coverage passed. Risk: broader command-package expectations currently fail around migrated output/projection behavior and remain to be diagnosed before final repository verification. Next action: strengthen Finish PTY ordering evidence, then partition Transcript rendering and add boundary tests.
- 2026-09-08: completed Finish PTY ordering evidence. `TestRichRuntimeFinishRestoresThenReplaysBeforeDiagnosticsAndResult` now uses an explicit `terminal.FinishRequest`, distinguishes live `SUCCEEDED`/summary from restored lower-case Transcript lines, and asserts primary-screen restoration, Transcript replay, FIFO diagnostics, and final stdout result ordering. Changed `internal/terminal/rich_runtime_pty_test.go`. Directed verification: targeted Finish PTY test, `go test ./internal/terminal ./internal/terminaltest`, and `cd internal/terminal/prototype-vivid && go test ./...` passed. Risk: a full `go test ./...` run still reports command-package compatibility failures unrelated to this test-only slice; these must be resolved or evidenced before G0 exit. Next action: partition Transcript rendering into the required `ANSWERS`, `WORK`, optional `AT`, `OUTCOME` sections and add redaction/bounds/order boundary tests.
- 2026-09-08: completed structured Transcript and Finish Result-separation slice. `TranscriptLedger.Render` now partitions durable entries as `ANSWERS`, `WORK`, optional prototype-aligned `AT       <location>`, and `OUTCOME`; it retains control-free, bounded outcome location/summary fields and preserves redaction. `FinishRequest.Summary` drives only the final Rich view and Transcript, while the optional second `Finish` argument remains the separate durable stdout Result; legacy `Finish(outcome, result)` does not infer a summary from Result bytes. Changed `internal/terminal/transcript.go`, `internal/terminal/experience.go`, `internal/terminal/transcript_test.go`, `internal/terminal/semantic_catalog_test.go`, `internal/terminal/rich_runtime_pty_test.go`, `internal/terminal/rich_recovery_test.go`, and direct command PTY assertions in `pkg/cmd/config/fork/remove/terminal_pty_test.go`, `pkg/cmd/rm/terminal_pty_test.go`, and `pkg/cmd/rm/terminal_pty_extended_test.go`. Directed verification: `go test ./internal/terminal ./internal/terminaltest` and `go test ./...` passed. Risk: G0 still needs direct PTY evidence for failed/cancelled Outcomes, interrupted dwell, and the full `120x40`/`70x20`/`40x15` colored and `NO_COLOR=1` matrix. Next action: add that bounded G0 verification coverage without changing command business logic or stdout behavior.
- 2026-09-08: completed Outcome and renderer-matrix verification slice. Added initial-size PTY support in `internal/terminaltest` and direct Rich lifecycle coverage in `internal/terminal/rich_outcome_pty_test.go`: success at `120x40`, `70x20`, and `40x15` in colored and `NO_COLOR=1` modes; failed and cancelled Outcome projections; primary-screen restoration; structured Transcript-to-stdout ordering; and immediate context cancellation of the fixed dwell. Added model evidence for failed/cancelled Outcome views and late Work rejection in `internal/terminal/console_test.go`, plus renderer-termination-during-dwell recovery and bounded replay in `internal/terminal/rich_recovery_test.go`. Directed verification: `go test -count=1 -v ./internal/terminal -run '^(TestRichRuntimeConsoleDimensionsAndColorPTY|TestRichRuntimeFailedAndCancelledOutcomesPTY|TestRichRuntimeContextInterruptsOutcomeDwellPTY|TestConsoleOutcomeProjectsFailureAndCancellation|TestRuntimeRecoversRendererTerminationDuringOutcomeDwell)$'` passed; all six matrix cases, both terminal outcomes, context interruption, and renderer recovery executed. Risk: final repository checks may expose compatibility regressions outside the shared implementation; next action: rerun the complete directed suite, audit fresh-control-plane evidence, then run repository verification in plan order.
- 2026-09-08: strengthened the Form-to-Work PTY evidence without changing runtime behavior. The matrix assertion now proves that the last complete Form Catalog frame precedes the first Work Catalog frame, in addition to the existing model proof that the rows are mutually exclusive. Changed `internal/terminal/rich_outcome_pty_test.go`. Directed verification: `go test -count=1 -v ./internal/terminal -run '^(TestRichRuntimeConsoleDimensionsAndColorPTY|TestRichRuntimeFailedAndCancelledOutcomesPTY)$'` passed across six dimension/color cases and two terminal outcomes. Risk: none identified in the shared lifecycle slice; next action: run the final directed and repository verification sequence against this exact worktree.
- 2026-09-08: G0 passed. Exit conditions accepted with semantic/model and PTY evidence: (1) eager Rich B shell and complete safe Form Catalog before first interaction; (2) mutually exclusive Form-to-Work replacement, including frame-order proof; (3) retained success/failed/cancelled Outcome with exact `800ms` message contract and context/renderer interruption recovery; (4) one `FinishRequest` Summary shared by Live View and Transcript while the optional Result remains independent stdout; (5) bounded, redacted `ANSWERS`, `WORK`, optional `AT       <location>`, `OUTCOME` ordering with primary-screen restoration and stdout-last proof; (6) Rich, Plain, Automation, redirected, cancellation, renderer-failure, lease, and recovery coverage; and (7) the fresh root control plane is the only execution input, with prior recovery artifacts held under `superseded/`. Final directed verification passed: `go test -count=1 ./internal/terminal ./internal/terminaltest` and `cd internal/terminal/prototype-vivid && go test ./...`. Final repository verification passed in declared order: `make command-surface`, `make check-terminal`, `make check`, and `git diff --check`. `make check` emitted only a non-failing Vite chunk-size advisory. Manual acceptance: none. No remote push. Risk: no G0 blocker remains; activate only G1 and end this Goal.

### G1: Read-only and selection migration

- 2026-09-08: initialized as `planned`; no implementation evidence.
- 2026-09-08: 已激活，尚未开始实施。G0 的全部 Exit conditions 已通过；本次 Goal 在此结束，G1 不在本次 Goal 的执行范围内。
- 2026-09-08: completed `config fork list` completion-projection slice. The existing immutable single-phase `TrackedOperation` remains this no-prompt command's Work Catalog; no artificial Form Catalog was added. Replaced Rich-only milestones and legacy `Finish(outcome, result)` with a command-owned `FinishRequest`: the Outcome and Transcript now share only the safe loaded-count/empty-state summary, while names, hosts, and token previews remain exclusively in the durable Result. Changed `pkg/cmd/config/fork/list/command.go`, `pkg/cmd/config/fork/list/presentation.go`, `pkg/cmd/config/fork/list/terminal_test.go`, and `pkg/cmd/config/fork/list/terminal_pty_test.go`. Directed verification passed: `go test ./pkg/cmd/config/fork/list ./internal/terminal ./internal/terminaltest`; repository verification for the completed command slice passed: `make check-terminal`. Risk: the remaining scoped adapters still use legacy Finish compatibility and need their own command-local migrations. Next action: migrate `config cm list` as the next read-only single-phase slice.
- 2026-09-08: completed `config cm list` completion-projection slice. Retained its immutable `Load CM profiles` Work Catalog and existing Plain/Automation stream behavior. Replaced the legacy completion plus Rich-only count/default/empty milestones with one command-owned `FinishRequest`: Outcome and Transcript share the safe loaded count, a safely projectable matching default profile, and optional empty guidance; profile rows, Base URLs, models, and any credential material remain outside the summary and only in the durable Result. Changed `pkg/cmd/config/cm/list/command.go`, `pkg/cmd/config/cm/list/presentation.go`, `pkg/cmd/config/cm/list/terminal_test.go`, and `pkg/cmd/config/cm/list/terminal_pty_test.go`. Directed verification passed: `go test ./pkg/cmd/config/cm/list ./internal/terminal ./internal/terminaltest`; repository verification passed: `make check-terminal`. Risk: compact PTY redraw bytes do not preserve one contiguous Outcome-detail string, so the compact assertion proves Outcome state and post-restore full summary while the wide assertion proves the full Outcome detail; this is a test-observation limitation, not a behavior change. Next action: migrate `config cm use` as its one atomic Work Catalog slice.
- 2026-09-08: completed `config cm use` completion-projection slice. Retained the immutable one-phase atomic writer catalog, exact writer input, no-prompt behavior, and Plain/Automation streams. Replaced legacy `Finish` calls with a command-owned `FinishRequest`: successful Outcome/Transcript use `Default CM profile set` without repeating the requested identity, while the existing safe phase detail remains the only profile projection; failures use `Unable to set default CM profile`. Changed `pkg/cmd/config/cm/use/command.go`, `pkg/cmd/config/cm/use/terminal_test.go`, and `pkg/cmd/config/cm/use/terminal_pty_test.go`. Directed verification passed: `go test ./pkg/cmd/config/cm/use ./internal/terminal ./internal/terminaltest`; repository verification passed: `make check-terminal`. Risk: no cancellation projection was added because the source decision treats the locked store transaction as having no truthful mid-operation cancellation boundary. Next action: migrate the two-phase non-mutating `config cm test` adapter.
- 2026-09-08: completed `config cm test` Work-Catalog and completion-projection slice. Replaced its segmented per-phase Tracks with one immutable `Resolve CM test profile`/`Test CM provider` catalog and asserted the full catalog plus ordered phase updates. Replaced legacy Finish calls and the Rich-only response milestone with `FinishRequest` summaries: response receipt and safe finite usage enter Outcome/Transcript without response content; resolver/provider failures and cancellation retain only safe phase/category context. The request body, profile precedence, HTTP behavior, durable success/failure documents, errors, and Plain/Automation streams remain unchanged. Changed `pkg/cmd/config/cm/test/terminal.go`, `pkg/cmd/config/cm/test/terminal_run_test.go`, `pkg/cmd/config/cm/test/terminal_test.go`, and `pkg/cmd/config/cm/test/terminal_pty_test.go`. Directed verification passed: `go test ./pkg/cmd/config/cm/test ./internal/terminal ./internal/terminaltest`; repository verification passed: `make check-terminal`. Risk: terminal Track errors are now observed when the one catalog closes rather than between phases, but are still joined with the real command outcome and do not suppress its result. Next action: migrate `git heat` as the next finite read-only catalog slice.
- 2026-09-08: completed `git heat` completion-projection slice. Retained its complete immutable three-phase `Locate Git repository`/`Read Git history`/`Rank hot paths` catalog and all existing report, cancellation, process, Plain, and Automation behavior. Replaced the Rich-only ranking milestone and legacy Finish calls with a `FinishRequest`: success carries only bounded ranked-count/range or empty-result information; cancelled and failed outcomes use generic safe summaries that do not disclose repository roots, paths, queries, or raw Git errors. Changed `pkg/cmd/git/heat/terminal.go`, `pkg/cmd/git/heat/terminal_test.go`, and `pkg/cmd/git/heat/terminal_pty_test.go`. Directed verification passed: `go test ./pkg/cmd/git/heat ./internal/terminal ./internal/terminaltest`; repository verification passed: `make check-terminal`. Risk: compact PTY renders the Outcome title in its intentional bounded form (`SUCCEE…`), so full Live Outcome-title assertion is wide-only while complete Outcome summary is proven after restoration in both widths. Next action: migrate `git pulse`, including its conditional form catalog and one Work Catalog.
- 2026-09-08: STOP CONDITION reached while scoping `git pulse`; G1 is `blocked` and no `git pulse` code was changed. Evidence: `pkg/cmd/git/pulse/run.go` intentionally executes Prepare and Scan before the date form, then Fetch before the author form; `pkg/cmd/git/pulse/terminal.go` maps each `Tracker.Start` segment to a separate one-phase `ExperienceRun.Track`; `internal/terminal/experience.go` holds the run operation lock for the duration of `Track`; and `internal/terminal/rich.go` replaces Form rows when Work starts, so later `Ask` calls cannot reuse the initial catalog and fall into the legacy dynamic-row path. A correct one-Catalog, no-dynamic-form migration therefore needs a shared lifecycle extension that permits the required work/form/work sequence while preserving mutually exclusive Catalog views and ordered phase state. That change is outside G1's scoped-adapter boundary and was not part of accepted G0 contracts. Completed evidence remains the four prior G1 command slices, each with its listed directed and `make check-terminal` pass. Recovery input: an explicit shared-lifecycle contract and authorized Gate/scope for the required terminal-core behavior, including tests for Scan -> date form -> Fetch -> author form -> Build without dynamic rows or stream regressions. Next action: pause; do not begin `export env`, root/help, G2, or G3.
- 2026-09-08: blocked-state recovery audit 1. Revalidated the same `git pulse` contract conflict without changing code. Directed evidence passed: `go test ./internal/terminal ./pkg/cmd/git/pulse -run '^(TestConsoleTrackReplacesFormCatalogWithWorkCatalog|TestTerminalPulseAdapterBridgesTypedPhaseToTrackedOperation|TestRunPulsePlainJourneyKeepsPhasesOnStderrAndReportOnStdout|TestRunPulseDateCancellationUsesTheEstablishedExitZeroResult)$'`. The tests confirm both the Form-to-Work replacement rule and the existing one-phase tracker bridge while preserving Plain journey and date-cancellation output. No command-adapter-only implementation can both keep Scan -> date form -> Fetch -> author form live and satisfy a single complete Work Catalog/no dynamic form-row requirement; delaying/replaying phases or opening multiple Console runs would violate the G1/decision lifecycle and stream contracts. Blocker and recovery input are unchanged: an authorized shared lifecycle extension with direct transition coverage is required. Next action: remain paused; do not enter remaining G1 slices or later Gates.
- 2026-09-08: blocked-state recovery audit 2. Re-inspected the live Goal, `internal/terminal/experience.go`, `internal/terminal/rich.go`, `pkg/cmd/git/pulse/tracking.go`, and `pkg/cmd/git/pulse/terminal.go`; the same three facts remain authoritative: `Track` owns the operation mutex until its update channel closes, Work replaces Form rows, and every Pulse phase still starts a separate one-phase Track. `git diff --check` passed. The shared-contract requirement is therefore genuine and unrecoverable inside G1's adapter-only scope; do not implement a delayed/replayed catalog, dynamic form fallback, or multi-Console workaround. Recovery remains an explicit authorized shared lifecycle contract with direct tests for the required Scan -> date form -> Fetch -> author form -> Build transition. Next action: Goal is blocked; no further G1/G2/G3 work is authorized in this task.
- 2026-09-08: resumed as `active` under explicit authorization. Gate boundary remains G1; the only scope extension is the shared `internal/terminal` lifecycle contract and direct model/PTY coverage necessary for `git pulse` to execute `Scan -> date form -> Fetch -> author form -> Build` against one logical Work Catalog. Form and Work views must remain mutually exclusive, no dynamic Form rows may be added, and Pulse scan/selection/fetch/filtering/stream/exit/cancellation/Automation/redirected behavior must remain unchanged. `export env`, root/help, G2, and G3 remain out of scope. Next action: establish the smallest terminal-core transition contract and test it before migrating the Pulse adapter.
- 2026-09-08: completed the controlled Work-session core slice. Added the opt-in `WorkCatalog`/`WorkSession` seam in `internal/terminal`: updates are serialized per run without holding the operation mutex between calls, so a declared form can safely interrupt and return to one complete Work Catalog. Rich now retains a declared Form Catalog behind a controlled Work session and swaps the visible table atomically between Form and Work rows; legacy `Track` behavior remains unchanged. Terminal phase transcript events are committed in catalog order when each phase reaches a terminal state. Changed `internal/terminal/semantic.go`, `internal/terminal/experience.go`, `internal/terminal/work_session.go`, `internal/terminal/rich.go`, `internal/terminal/console_test.go`, and added `internal/terminal/work_session_test.go`. Directed verification passed: `go test ./internal/terminal ./internal/terminaltest`. Risk: no command uses the opt-in seam yet, so Pulse-specific form IDs, single Catalog wiring, capability behavior, and PTY journey remain to be proven. Next action: migrate only `git pulse` onto the controlled catalog and add its adapter/model/PTY regressions.
- 2026-09-08: completed the authorized `git pulse` controlled-catalog slice. Pulse now declares one immutable `Prepare workspace`/`Scan repositories`/`Fetch commits`/`Build commit tree` Work Catalog, publishes each existing typed phase to that one session, and supplies declared date/author Form IDs before `OpenConsole`; it preserves the existing Scan -> date -> Fetch -> author -> Build order without dynamic Form rows. Added a command-owned `FinishRequest` whose bounded Outcome/Transcript summary contains only safe counts or empty/cancel/failure text, never the report. Plain, Automation, redirected, empty, cancellation, failure, stdout, exit, and durable report behavior remain unchanged. Changed `pkg/cmd/git/pulse/terminal.go`, `pkg/cmd/git/pulse/terminal_test.go`, `pkg/cmd/git/pulse/terminal_pty_test.go`, and direct `internal/terminaltest` recording support. Directed verification passed: `go test -count=1 ./pkg/cmd/git/pulse ./internal/terminal ./internal/terminaltest`; Rich PTY evidence covers the full four-phase Work/Form/Work journey at `120x40` color and `40x15` with `NO_COLOR=1`, primary-screen restoration, Transcript/result separation, and no report-content leakage. Repository verification passed: `GOFLAGS=-p=1 make check-terminal`. The default parallel target twice hit distinct unrelated process-test flakes in `pkg/cmd/git/fork` and `pkg/cmd/run`; each isolated test and the serial same-target run passed. `export env`, root/help, G2, and G3 remain unmodified and out of scope. Risk: G1 cannot satisfy its remaining Gate Exit conditions while those explicitly excluded G1 slices remain unimplemented. Next action: end this scoped Goal with G1 still `active`; a new explicit authorization is required before the remaining G1 slices can run.
- 2026-09-08: completed the authorized `export env` controlled-catalog slice. The adapter now declares one immutable `Resolve directory`/`Discover environment files`/`Read selected files`/`Parse and merge values`/`Encode JSON` catalog, with `Write output file` included only for `--out`; the predeclared `Select environment` Form is the only form and alternates Work -> Form -> Work without a dynamic row. A command-owned `FinishRequest` carries only safe count/write/cancel/failure summaries while the heading, JSON, and `--out` Result bytes remain separate durable stdout. Parsing, merge precedence, selection timing, write ordering, Plain, Automation, redirected, cancellation, stdout/stderr, and exit behavior remain unchanged. Changed `pkg/cmd/export/env/terminal.go`, `pkg/cmd/export/env/terminal_test.go`, and `pkg/cmd/export/env/terminal_pty_test.go`. Directed verification passed: `go test -count=1 ./pkg/cmd/export/env` and `go test -count=1 ./internal/terminal ./internal/terminaltest`; direct model evidence proves one Work Catalog/no legacy Track and safe Finish summaries, while Rich PTY evidence covers Work/Form/Work at `120x40` color and `40x15` with `NO_COLOR=1`, primary-screen restoration, Transcript/result separation, and no dotenv-content leakage. Repository verification passed: `GOFLAGS=-p=1 make check-terminal`. Risk: no `export env` behavior regression was observed; root/help remains the final G1 regression-only slice. Next action: cover root/help version, completion, parser error, and stdout-write failure, then run `make command-surface`.
- 2026-09-08: completed the root/help regression-only slice. No production root/help behavior changed; strengthened `TestRichTerminalDiscoveryPreservesVersionAndRawCompletion` to prove Rich-capability help remains durable stdout without an AltScreen or Transcript, while version and completion stay raw. Existing direct regressions retain parser recovery's one stderr line/empty stdout and discovery write/short-write failures' one diagnostic/exit-1 behavior. Changed `pkg/cmd/root/app_test.go`. Directed verification passed: `go test -count=1 ./pkg/cmd/root -run '^(TestRichTerminalDiscoveryPreservesVersionAndRawCompletion|TestDiscoveryWriteFailureBecomesOneRootDiagnostic|TestDiscoveryShortWriteBecomesOneRootDiagnostic|TestParserRecoveryUsesOneActionableErrorLine|TestDiscoveryAndParserContractsKeepAutomationStreamsSeparated)$'` and `go test -count=1 ./pkg/cmd/root`. Repository verification passed: `make command-surface`. Risk: no root/help stream, output, parser, or AltScreen regression observed. Next action: audit all G1 Exit conditions and complete the Gate if the final repository checks pass.
- 2026-09-08: G1 passed. Exit conditions accepted with fresh evidence: (1) all scoped adapters use complete Catalogs; `git pulse` and `export env` declare every Form ID before `OpenConsole`, use one controlled Work Catalog, and have no dynamic Form rows; (2) command-local package/model/PTY suites cover success plus failed, cancelled, or empty paths as applicable; (3) Rich, Plain, Automation, redirected, Transcript, stdout/stderr, exit, cancellation, and redaction compatibility are covered in each completed slice, including the new Work/Form/Work PTYs; and (4) root/help stays durable stdout-only with version, raw completion, parser, write-failure, and no-AltScreen regressions passing. Final directed verification passed: `go test -count=1 ./pkg/cmd/export/env`, `go test -count=1 ./internal/terminal ./internal/terminaltest`, and `go test -count=1 ./pkg/cmd/root`. Final repository verification passed in declared order: `GOFLAGS=-p=1 make check-terminal`, `make command-surface`, and `git diff --check`. Manual acceptance: none. No remote push. Risk: prior default-parallel process-test flakes remain irrelevant to this final serial exit-0 target; no G1 blocker remains. Activate only G2 and end this Goal.

### G2: Mutation and archive migration

- 2026-09-08: initialized as `planned`; no implementation evidence.
- 2026-09-08: 已激活，尚未开始实施。G1 的全部 Exit conditions 已通过；本次 Goal 在此结束，G2 不在本次 Goal 的执行范围内。

### G3: Handoff boundaries and release acceptance

- 2026-09-08: initialized as `planned`; no implementation evidence.
